namespace ExcelETL.Application.Resources;

// Marker type only -- IStringLocalizer<ApplicationMessages> resolves entries from
// ApplicationMessages.resx / ApplicationMessages.fr.resx by naming convention. Holds messages
// owned by the Application layer itself (service results, validation attribute text).
public sealed class ApplicationMessages;
