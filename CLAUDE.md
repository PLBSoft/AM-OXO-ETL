# Project Instructions: AM-OXO-ETL Microservice

## Role
**Senior Lead .NET Software Engineer & Software Architect**

## Methodology
- Strict Test-Driven Development (TDD - Test First)
- Clean Architecture
- SOLID Principles

---

## GLOBAL CONTEXT & SYSTEM OVERVIEW

You are tasked with building a modern, isolated, and highly maintainable micro-service designed to offload a heavy Excel data-extraction workload from a legacy application. The system will be hosted on a dedicated on-premise Windows Server 2022.

### Key Applications:
- **Legacy Application:** ASP.NET MVC 5, C#, .NET Framework 4.8. It captures raw uploaded Excel files and ingests the processed files. No business logic modifications are allowed here.
- **New Application:** A greenfield independent system built using the latest modern .NET LTS release (.NET 10). It exposes a secure ASP.NET Core Web API for synchronous Machine-to-Machine (M2M) communication and a Blazor Web App (Server Interactivity) for administration.

---

## FUNCTIONAL & DATA FLOW SPECIFICATIONS

### Ingestion & Transfer
Legacy app securely streams the `.xlsx` file synchronously over HTTP to the new .NET Web API.

### Processing
The backend extracts form-style data from heavily merged cells based on dynamic coordinates.

### Persistence
The generated structured workbook is archived on the local Windows Server filesystem. Execution is logged in the database.

### Egress
The generated workbook is returned synchronously in the HTTP response body.

### Constraints
- Input files contain no macros or encryption
- Output must be exactly 4-5 distinct `.xlsx` sheets
- CSV and ZIP formats are explicitly banned

---

## TECHNICAL STACK & CONSTRAINTS

### Core Frameworks
- ASP.NET Core Web API
- Blazor Web App (Server Interactivity model)

### Architecture
Clean Architecture (Onion Pattern). **Both the Web API and the Blazor admin app access data exclusively through the Application-layer services/repositories** — never by talking to each other over HTTP, and never by injecting `DbContext` (or any Infrastructure type) directly into a controller or a Razor component. All EF Core access lives behind repository interfaces defined in `Application` and implemented in `Infrastructure`. This is a strict rule, not a suggestion — short-circuiting the service/repository layer for convenience is exactly the kind of violation to flag rather than implement (see "Architectural Oversight" below).

### Database
- Microsoft SQL Server (Local on-premise)
- Entity Framework Core (Code-First Fluent API)

### Licensing Constraint (CRITICAL)
**100% strict adherence to free, open-source software (OSS) licenses (MIT, Apache 2.0). Commercial libraries are strictly forbidden.**
- **ClosedXML** is the mandatory library for all Excel manipulation

### Security
- Web API guarded by robust API Key validation
- Blazor admin guarded by ASP.NET Core Identity (Local SQL Server)

### Performance & HTTP
Because processing is synchronous and can take several seconds, explicit configuration of HTTP Timeouts is mandatory:
- Kestrel configuration
- Web API timeouts
- Legacy HttpClient timeouts

---

## QUALITY ASSURANCE & TDD STRATEGY

Apply a strict Test-First (Red-Green-Refactor) lifecycle:
1. Write failing unit/integration tests before implementation
2. Use xUnit/NUnit, Moq, FluentAssertions
3. Explicitly test reading values from merged range coordinates in Excel
4. Ensure all business logic is test-covered before deployment

### Repository test conventions
- Test projects live under `tests/`, one per `src/` project, mirroring its internal folder structure 1:1 (a repository in `src/ExcelETL.Infrastructure/Identity/` is tested in `tests/ExcelETL.Infrastructure.Tests/Identity/`).
- **FluentAssertions is pinned to 7.0.0** — v8+ is commercially licensed, which the OSS constraint above forbids. Always verify a NuGet package's license before adding or upgrading it.
- Repositories backed by an EF Core `DbContext` are tested against the **real EF Core InMemory provider**, not mocked. Each test class gets its own tiny `internal sealed class Test{ContextName}DbContextFactory(string databaseName) : IDbContextFactory<TContext>` colocated in the same test folder, constructed with a GUID-suffixed database name per test class for isolation — there is no shared/generic factory helper.
- Repositories that depend on ASP.NET Core Identity's `UserManager<T>`/`RoleManager<T>` (rather than an injected `IDbContextFactory<T>` directly) are tested with **Moq** instead, since those managers aren't meaningfully exercised against the InMemory provider alone.
- Assertions use FluentAssertions exclusively (`.Should().Be(...)`, `.Should().BeEquivalentTo(...)`, `.Should().ThrowAsync<T>()`, etc.) — never xUnit's `Assert.*`.

### Execution discipline per TDD step
Full detail and ticket-authoring guidance: `docs/recommandations-tickets-tdd.md`.
- Reserve deep/high-effort reasoning for the **Refactor** step — Red and Green are mechanical and don't need it.
- Run tests with minimal noise: `dotnet test --verbosity quiet` (or `--logger "console;verbosity=minimal"`), filtered to the class/test under iteration via `--filter` rather than the whole suite on every cycle.
- Prefer targeted diffs over re-reading whole files after each edit, unless the file is short or a full-picture view is actually needed.
- On an unexpected test failure (Blazor/bUnit rendering, timing, generated markup, etc.), stop and inspect the diff instead of looping uncontrolled retries — then re-run one precise, targeted pass.

---

## I18N (ENGLISH/FRENCH) STRATEGY

The solution is being incrementally refactored to support en-US (default) and fr-FR, one Clean Architecture layer at a time (Domain → Application → Infrastructure → WebAPI → BlazorAdmin). Resource format is `.resx`.

### Scope: user-facing business errors only
Only translate messages a **user or admin actually sees** (validation failures, business-rule violations, HTTP error payloads, UI text). Do **not** touch developer-facing invariant violations — `ArgumentNullException.ThrowIfNull`, assertion/guard-clause messages, log-only diagnostic text aimed at engineers. Those stay in plain English BCL exceptions; they are out of i18n scope entirely.

### Domain layer stays framework-free
`ExcelETL.Domain` must never reference `Microsoft.Extensions.Localization` or any other localization package — that would put a framework dependency in the innermost layer, violating Clean Architecture. Instead:
- Domain throws `DomainValidationException` / `DomainArgumentOutOfRangeException` / `DomainRuleViolationException` (in `ExcelETL.Domain/Exceptions`), each carrying a `DomainErrorCode` enum value plus the raw `Args` that were interpolated into the (English) `Message`.
- The `DomainErrorCode` member name doubles as the resource key.

### Application layer owns the resource tables
`ExcelETL.Application/Resources/` holds the `.resx` files, resolved via `IStringLocalizer<T>` marker classes (no generated designer — resolution is by naming convention, so no Visual Studio tooling is required):
- `DomainErrorMessages.resx` / `.fr.resx` — the single translation table for every `DomainErrorCode`, shared by both WebAPI and BlazorAdmin so the mapping is never duplicated between the two hosts.
- `ApplicationMessages.resx` / `.fr.resx` — Application-owned messages (service results, Data Annotations validation text via `ErrorMessageResourceType`/`ErrorMessageResourceName`).

`Microsoft.Extensions.Localization.Abstractions` (interfaces only, no ASP.NET Core coupling) is an accepted Application-layer dependency, consistent with the existing `Microsoft.Extensions.Logging.Abstractions` reference.

### Host wiring
- **WebAPI**: `RequestLocalizationOptions` negotiates culture from `Accept-Language` only (it's M2M — no cookie/query-string relevance). The `GlobalExceptionHandler` resolves a thrown `DomainErrorCode` + `Args` against `DomainErrorMessages` to build the localized HTTP error payload.
- **BlazorAdmin**: default provider order (query string, then culture cookie, then `Accept-Language`) so an admin's language choice persists. A circuit is long-lived, so switching language requires a real navigation — done via the `/culture/set` minimal API endpoint (`CultureEndpointRouteBuilderExtensions`), not a component re-render.

### Workflow
Work proceeds milestone by milestone (one Clean Architecture layer, or one batch of Razor components, at a time). At the end of each milestone: stop, summarize, and wait for validation plus the next milestone's files before continuing. Each milestone's deliverable includes an EN/FR resource key table.

---

## ARCHITECTURAL OVERSIGHT (CRITICAL)

The user is not a professional software architect and expects to make mistakes in architectural or strategic direction from time to time. **Proactively flag it** — before implementing — whenever a requested change, an instruction in this file, or an existing pattern in the codebase:
- Contradicts Clean Architecture, SOLID, or another established best practice
- Contradicts the architecture already in place elsewhere in this solution (e.g. one app going through repositories while another talks to EF Core directly)
- Introduces inconsistency between the Web API and the Blazor admin app's data-access patterns
- Trades away testability, layering, or maintainability for short-term convenience without the user asking for that trade-off explicitly

Say so directly, explain the concrete downside, and propose the alternative — then wait for a decision rather than silently complying or silently "fixing" it. Do not assume a past instruction (including one written in this file) is correct just because it's already written down; instructions can contain mistakes too.

---

## Utilisation du navigateur (Browser / Claude in Chrome)

Réserver la vérification navigateur aux cas où un rendu visuel ou une
interaction UI doit réellement être validé : mise en page Blazor,
comportement d'un composant interactif, régression visuelle après un
changement CSS/markup.

Pour les modifications suivantes, préférer la compilation + les tests
unitaires/bUnit existants plutôt que le navigateur :
- réordonnancement de liens/éléments dans le NavMenu ou une sidebar
- renommage, refactoring, changement de logique métier sans impact visuel
- modifications de configuration, de seeders, de migrations EF
- toute tâche où le résultat attendu est déjà couvert par un test

En cas de doute, demander avant de lancer le navigateur plutôt que
de l'utiliser par défaut.

---

## CURRENT SOLUTION STATE (living reference — read this before exploring the codebase; update it at the end of each milestone)

### Projects
- `src/`: `ExcelETL.Domain`, `ExcelETL.Application`, `ExcelETL.Infrastructure`, `ExcelETL.Hosting` (composition-root-only Serilog wiring shared by the two hosts below — see "Lot G3"), `ExcelETL.WebAPI`, `ExcelETL.BlazorAdmin`
- `tests/`: mirrors each `src/` project 1:1 (`ExcelETL.Domain.Tests`, etc.)
- `legacy/`: `NewApiPingService`, `ExcelProcessingClientService` (+ `.Tests`) — the .NET Framework 4.8 legacy app's own HTTP client for calling the new API; the style precedent for any new M2M `HttpClient` code (multipart build, timeout translation, header setup).

### Extraction pipeline (new feature area)
- Source docs (renamed to drop date suffixes in the 2026-07-18 doc refactor -- see `docs/convention-nommage-documents.md`; each is a "living" doc, edited in place, no version history stacked in the body): `docs/spec-extraction-fichier-source-oxo.md` (feuille par feuille), `docs/modele-domaine-import-profile.md` (catalogue de primitives + modèle pivot), `docs/tickets-tdd-extraction.md` (Lots A→E, now framed as a regression/business-rule reference rather than a forward plan), `docs/glossaire-ef6-legacy-AMAR-ModelCF.md` (confirmed real `TypeElement`/`Colonne` values from the OXO database — treat this as the source of truth over any literal casing/spelling elsewhere). Test fixtures (real client `.xlsx` files): `tests/Fixtures/`.
- **v6 client clarifications (2026-07-16), landed across Domain + Application in the same session as C2/C3**: `TypeElement.Nom` is used everywhere, including for the parent Equipement (previously assumed to be `.Code`) — see the `EquipementPivot`/`ImportProfile` bullets below. `"PROLOCK VANNES"`/`"DEPROLOCK VANNES"` (ISOLEMENT) confirmed unconditional for every Isolement regardless of `TypeElement`. PLATINES' DEB/FIN variants: back to `DEBUT`-only (final). `"VANNE"` (ISOLEMENT, real D8570 fixture) confirmed absent from the OXO referential — handled by the existing non-blocking-warning policy, no dedicated rule. `ConditionalPointRuleEvaluator` comparison now trims + ignores case (see Lot B4 bullet below).
- This was originally built as a **second, separate pipeline** alongside the older `ExtractionConfig`/`CellMapping`/`SheetConfig` one — different Domain folder (`Extraction/`), deliberately not merged. **As of Lot K4, the older pipeline (and every file backing it) has been removed entirely** — this OXO pipeline is now the only one, exposed over HTTP at `POST /api/oxo/process` (see "Web API surface" below).
- `src/ExcelETL.Domain/Extraction/Primitives/`: the 5-primitive catalogue read-time model — `DirectCell`, `RepeatingBlockLocator`/`BlockFieldDefinition` (the generic repeating-block reader, covers all 6 source sheets incl. PROCEDURE's step=1 special case), the `TextTransform` hierarchy (`RawValue`/`SubstringAfter`/`Concat`+`ConcatPart`(`Literal`/`FieldRef`)), `ConditionalPointRule`/`ConditionOperator`. All `sealed record`s validating via `DomainValidationException`/`DomainArgumentOutOfRangeException`, same convention as the existing `Entities/`. `RepeatingBlockLocator` and `Concat` hold a list property (`Fields`/`Parts`) — both override `Equals`/`GetHashCode` with `SequenceEqual` because default record equality on `IReadOnlyList<T>` is reference equality, not structural; every other primitive is plain scalar fields so default record equality suffices.
- `src/ExcelETL.Domain/Extraction/Pivot/`: the extraction *output* model — `EquipementPivot` (`TypeElementNom`, **renamed from `TypeElementCode` at v6** — client confirmed source and target both use `TypeElement.Nom` throughout, never `.Code`, even for the parent Equipement; **gained a `Localisation` `init` property, Lot D prerequisite** — same "loc1" broadcast as `IsolementPivot` below, purely additive, existing 3-arg constructor unaffected), `IsolementPivot` (its `Localisation` has an `init` accessor specifically so the future `loc1` broadcast, Lot D, can do `isolement with { Localisation = ... }` on an already-built instance without re-running the other fields' validation; **only `Repere`/`TypeElementNom` are required non-blank** — `Designation` and `PositionALaPose` (added at v6, ISOLEMENT's `H20:O21` "Position MAD") are both deliberately left unvalidated: the real D8570 fixture has an ISOLEMENT row with a blank `Designation` that must still be extracted normally (§3.2's non-blocking-warning policy, not a rejection), and only ISOLEMENT has a source cell for `PositionALaPose` at all — PLATINES/ORIFICES CAPACITES/AUTRES JOINTS TOUCHES/DIVERS have none, so their `IsolementPivot`s necessarily leave it blank; a sheet-specific service may still enforce its own stricter "blank is reportable" policy before constructing the pivot, as `IsolementExtractionService` does for `PositionALaPose`), `PointPivot`, `TacheMultiplePivot` (only `Action` is required non-empty — it's PROCEDURE's stop-condition field so always populated, including for `EstFactice` placeholder rows; `Acteur`/`Risques`/`TypeTacheMultipleCode` are deliberately left unvalidated since factice rows can leave them blank per spec §1.2), `ExtractionErrorCode` (intentionally only 3 members for now — `RequiredFieldMissing`/`UnparsableValue`/`UnrecognizedTypeElement`, more will be added as Lot B/C need them, not pre-guessed), `ExtractionError`, `ImportResult` (a plain `sealed class`, not a record — **a second deliberate exception to the project's "no generic Result pattern" rule**, alongside `IdentityOperationResult`; needed because per-block extraction errors accumulate while processing continues, which doesn't fit "throw and stop"). `ImportResult`'s constructor does *not* enforce "Equipement null ⇒ other collections empty" (the whole-file-rejection invariant from the model doc §3.1) — that's tested at the orchestrator level instead (Lot D, via mock verification that the other 5 sheet services are never called), not baked into the Domain constructor. A **second, purpose-built DTO for isolement-style sheets' partial results**, `IsolementSheetExtractionResult` (`Isolements`/`Points`/`Errors`, no `Equipement`), lives in Application (`Extraction/Oxo/`) rather than reusing `ImportResult` — see the Lot C2 bullet below for why.
- `src/ExcelETL.Domain/Extraction/Profile/`: `ImportProfile` (aggregate root, extends `Entity`, has `Id`) and `SheetExtractionRule` (plain class, no `Id` — a configuration value owned by its `ImportProfile`, not an entity in its own right). `ImportProfile.ReperePrefix` defaults to `"MAD-OXO-"` (`ImportProfile.DefaultReperePrefix`) via a constructor overload, per the spec's "paramétrable, défaut MAD-OXO-". `ImportProfile.EquipementTypeElementNom` (**added at v6**, no default overload — the client corrected the earlier `"MAD"` assumption to `"MAD TRAVAUX"` for a MAD dossier, confirmed in the OXO database, and this value is profile-specific, never a service constant) carries the Equipement parent's `TypeElement.Nom`; required non-blank like `ReperePrefix`. `SheetExtractionRule` validates its `SheetName` matches its `Locator.Sheet` (`DomainRuleViolationException`) — a consistency check beyond what the ticket explicitly asked for, added because the two are otherwise redundant data that could silently diverge. `SheetExtractionRule.PointRules` may legitimately be empty (means "always create the Point", not an error) — only `ImportProfile.SheetRules` itself is required non-empty (at least one rule). **`SheetExtractionRule.UnconditionalColonneNames`** (**added at v6, resolves the previously-open PointRules-grouping question** — see Lot C2 bullet below) is a second, independent list of Colonne names created for every extracted row with no condition at all (e.g. ISOLEMENT's `"PROLOCK VANNES"`/`"DEPROLOCK VANNES"`) — `ConditionalPointRule` has no way to represent "no condition" (it always carries a real `SourceFieldName`/`ComparisonValue`), so this couldn't be folded into `PointRules` itself. Also required non-null, empty is valid (e.g. DIVERS, Lot C6, where every Colonne is conditional).
- Persistence (EF Core `ImportProfile`) is deliberately deferred — Lot A-D are being validated against a hardcoded in-memory profile first, per the ticket doc's proposed sequencing. Don't add an `ExtractionProfileConfiguration`/repository until that's explicitly requested.
- **Lot B (Application-layer generic engine, done)** lives in `src/ExcelETL.Application/Extraction/Oxo/` — a **new subfolder**, deliberately not flat in the pre-existing `src/ExcelETL.Application/Extraction/` (that's the old `ExtractionConfig` pipeline; putting new OXO code there would collide both on disk and in namespace — see `docs/etat-des-lieux-technique.md`'s "deux dossiers `Extraction/`" note). `IWorkbookReader`: `ReadCellValue(sheet, range)` / `SheetExists(sheet)`. `TextTransformEvaluator`/`RepeatingBlockReader`/`ConditionalPointRuleEvaluator` are all stateless, tested against `Mock<IWorkbookReader>` or plain dictionaries — no real file I/O in their own tests (that's Lot E1 + the per-sheet integration tests below). `RepeatingBlockReader`'s range math (column split + row offset) is factored into a standalone static `BlockFieldRangeCalculator`, added while building Lot C1 so `ProcedureExtractionService` could reuse the same math without going through `RepeatingBlockReader`'s required-field policy (see Lot C1 note below) or duplicating it.
  - None of these three evaluators return an `ExtractionError` directly — they return a plain `(Value, ErrorMessage)`/`(ShouldCreatePoint, WarningMessage)` tuple (just a 2-tuple, not a new named Result type) because they have no `Sheet`/`BlockIdentifier` context to build one with. Wrapping a message into a real `ExtractionError` is the caller's job (Lot C, which does have that context) — don't push `ExtractionError` construction down into these evaluators later without a reason.
  - `RepeatingBlockReader` reads a block's `StopFieldName` cell *first*, before any other field, and bails immediately if it's blank — so a block that fails the stop check is never misreported as "partially empty"; it's simply never read beyond its stop field. A block whose stop field is populated but another field is blank produces one `ExtractionError` (`RequiredFieldMissing`) and is skipped, and reading continues on the next block.
  - `UnknownFieldReferenceException` (new `ApplicationErrorCode` member, follows the existing per-exception `IHasApplicationErrorCode` pattern — there is no shared `ApplicationValidationException` base class in this codebase, despite the ticket doc's conventions section implying one) is thrown — not returned as an `ExtractionError` — whenever a `Concat`'s `FieldRef` or a `ConditionalPointRule`'s `SourceFieldName` names a field that isn't in the already-extracted-fields dictionary. That's treated as a profile/configuration bug (the `SheetExtractionRule` doesn't line up with itself), not per-row file data noise.
  - `RepeatingBlockReadResult` (`Blocks`/`Errors`) is a plain DTO, matching the pre-existing `ExtractionResult`'s precedent — not a third exception to the "no generic Result pattern" rule (that count stays at 2: `IdentityOperationResult`, `ImportResult`).
  - **`ConditionalPointRuleEvaluator`'s comparison trims and ignores case** (fixed post-v6, per spec §7): real fixtures have trailing spaces (`"SOUPAPE "`) and casing variance vs. the confirmed base values. A genuine spelling difference (`"POINT DE FEU"` vs `"POINT FEU"`) is **not** normalized away by this and remains a legitimate non-match, covered by the existing non-blocking-warning path.
- **Lot E1 (`ClosedXmlWorkbookReader`, done ahead of its ticket slot)** lives in `src/ExcelETL.Infrastructure/Excel/`, the first real `IWorkbookReader` implementation. Built alongside Lot C1 rather than deferred, because the Lot C ticket requires each per-sheet service to be tested against the 3 real fixtures, which needs a working reader to open them. Opens the `XLWorkbook` once per instance and owns its disposal (`IDisposable`, not part of `IWorkbookReader` itself — Lot B/C consumers never need to know). Reads a range's top-left cell via `GetString()`, returning `null` for blank (mirrors what the Mock-based tests already assume). Reuses the pre-existing `WorksheetNotFoundInWorkbookException` (from the old `ExtractionConfig` pipeline, `ExcelETL.Application.Extraction` namespace) for an unknown sheet, rather than declaring a near-duplicate. Its own tests use small in-memory workbooks built with ClosedXML directly (merged range, blank cell, missing sheet) — real-fixture coverage comes from the per-sheet integration tests instead (see below).
- **Lot C1 (`ProcedureExtractionService`, done)** lives in `src/ExcelETL.Application/Extraction/Oxo/Procedure/`. Reads PROCEDURE's header (`M2:O2` repère, `P2:Q2` numéro révision, `R2:T2` date révision — all 3 ranges hardcoded in the service, not part of `SheetExtractionRule`, since the spec only calls the repère *prefix* profile-configurable, not the header cell coordinates themselves) into `EquipementPivot`, plus its `TacheMultiple` repeating block (`Step=1`, stop field `Action`/`C:L`) into `TacheMultiplePivot`s, and creates 2 unconditional `PointPivot`s ("TRAVAUX COMPLET"/"TRAVAUX DETAIL") for the Equipement per spec §1.3 — no `ConditionalPointRuleEvaluator` involved, these aren't conditioned on anything.
  - **Deliberately does not use `IRepeatingBlockReader`** for the TacheMultiple block, despite `RepeatingBlockLocator`/`Step=1` otherwise fitting: every field in a TacheMultiple row *except* `Action` is optional at the pivot level, not just `Ordre` (the documented "ligne de mise en page" → `EstFactice` rule, spec §1.2) but `Acteur`/`Risques`/`TypeTacheMultipleCode`/`DateValidation` too (`TacheMultiplePivot` itself leaves them unvalidated — confirmed against the real fixtures, e.g. C7401 has section-header rows with blank `Ordre` scattered well past row 9, and populated `Acteur`("ADF")/`Risques` values are the exception, not the rule). `RepeatingBlockReader`'s shared policy (any non-stop field blank ⇒ `ExtractionError`, block dropped) would wrongly reject/drop valid rows, so `ProcedureExtractionService` walks the block itself, calling `BlockFieldRangeCalculator.BuildRange` directly for the range math only. Field names read from `sheetRule.Locator.Fields` by name (constants in `ProcedureFieldNames`) so the field list stays profile-configurable exactly like every other sheet's locator.
  - **Whole-file rejection** (model doc §3.1): if `M2:O2` is blank, doesn't start with the configured repère prefix, or `R2:T2` is blank/unparsable, `Extract` returns immediately with `Equipement = null` and exactly one blocking `ExtractionError` — the TacheMultiple block is never read in that case (verified by `Mock.Verify(..., Times.Never)` in the unit tests).
  - **Date handling**: `R2:T2`/`T:U` cells are parsed via `DateTime.TryParseExact` (`"dd/MM/yyyy HH:mm:ss"` / `"dd/MM/yyyy"`, invariant culture) into `DateOnly`, then reformatted as `dd/MM/yyyy` for `Designation` — deliberately **not** a raw pass-through of `IWorkbookReader.ReadCellValue`'s string, because probing the real C7401 fixture showed ClosedXML's `GetString()` on a date cell returns `"12/12/2025 00:00:00"` (a full datetime, locale/number-format dependent), not a clean `dd/mm/yyyy` string — the spec's format requirement is enforced in code, not assumed from the source file's display formatting.
  - **`Extract` takes `equipementTypeElementNom` as a parameter** (reworked post-v6; originally hardcoded to the literal `"MAD"`). The client confirmed this value (`"MAD TRAVAUX"` for a MAD dossier) is profile-specific (`ImportProfile.EquipementTypeElementNom`), never a service constant — an explicit test (`Extract_UsesEquipementTypeElementNomFromProfile_NotAHardcodedConstant`) asserts the service passes through whatever value it's given, as an architecture guard-rail per the ticket's own requirement.
  - Tested against `Mock<IWorkbookReader>` (`ProcedureExtractionServiceTests`, Application.Tests) and, per the Lot C ticket's requirement, against all 3 real fixtures via the real `ClosedXmlWorkbookReader` (`ProcedureExtractionServiceIntegrationTests`, **Infrastructure.Tests**, not Application.Tests — it needs both the Application service and the Infrastructure reader, and Infrastructure already references Application, so putting it there avoids adding a new Application.Tests → Infrastructure reference).
- **Lot C2 (`IsolementExtractionService`, done)** lives in `src/ExcelETL.Application/Extraction/Oxo/Isolement/`. Reads ISOLEMENT's block (`Step=7`, stop field `Identification`/`B:E`), composing each Isolement's `Repere` as `{K6:T6}-{Identification}` — `K6:T6` read directly off the ISOLEMENT sheet itself, **not** the PROCEDURE-derived `Equipement.Repere` passed in as a parameter: confirmed against all 3 real fixtures that ISOLEMENT's `K6:T6` is a shorter, different value (e.g. `"C7401"` vs PROCEDURE's composed `"38-C7401"`) — the spec calls for composing from `K6:T6` specifically, so the service is fully self-contained, no cross-sheet context needed. Returns `IsolementSheetExtractionResult`, not `ImportResult` — reusing `ImportResult` here would blur its null-`Equipement` = whole-file-rejection meaning, since isolement-style sheets never produce an `Equipement` at all (a non-rejection reason for null would be indistinguishable from the real rejection case at Lot D's merge step).
  - **Resolves the `PointRules`-grouping question**: creates a `PointPivot` for every name in `sheetRule.UnconditionalColonneNames` (`"PROLOCK VANNES"`/`"DEPROLOCK VANNES"`, unconditionally, regardless of `TypeElement`) **plus** groups `sheetRule.PointRules` by `ColonneName` and evaluates via `ConditionalPointGroupEvaluator` (see below) — ISOLEMENT has exactly one conditional group, the `"ZÉRO ENERGIE..."` Colonne.
  - **Deliberately does not use `IRepeatingBlockReader`**, for a narrower reason than PROCEDURE: the real D8570 fixture has a row (Identification `"V4"`, TypeElement `"VANNE"`, confirmed absent from the OXO referential) with a blank `Designation` cell that must still be extracted normally (§3.2's non-blocking-warning policy, not a rejection) — so `Designation` is read leniently while `Identification`/`TypeElement`/`PositionALaPose` stay required (blank ⇒ `RequiredFieldMissing`, block skipped), a mixed policy `RepeatingBlockReader`'s uniform strictness can't express. Walks the block itself via the shared `BlockFieldRangeCalculator`.
  - **Every non-`"ZERO ENERGIE"` isolement legitimately produces an `UnrecognizedTypeElement` warning** in the 3 real fixtures (none contain a `ZERO ENERGIE`-typed row in this sheet) — not a bug, expected given ISOLEMENT has only one conditional Colonne (see `ConditionalPointGroupEvaluator` below).
  - Verified against all 3 real fixtures: 8/15/3 Isolements for C7401/D8570/G6306B; D8570's `"VANNE"` row is asserted explicitly per the ticket's requirement (extracted normally, blank `Designation`, the 2 unconditional Points still created, one warning, no `"ZERO ENERGIE"` Point).
- **`ConditionalPointGroupEvaluator`** (`Extraction/Oxo/`, static helper, extracted while starting Lot C6) evaluates every conditional Colonne-group for one isolement and **aggregates the warning across the whole sheet**: a warning fires only when *none* of the groups matched, not whenever any single group's own condition fails to match. For a sheet with one conditional group (ISOLEMENT, AUTRES JOINTS TOUCHES) this is behaviorally identical to evaluating that group alone; it's required correctness for DIVERS (multiple mutually-exclusive conditional types) — without it a SOUPAPE isolement there would incorrectly also warn for not matching INSTRUMENTATION's/POINT FEU's groups. `IsolementExtractionService` and `AutresJointsTouchesExtractionService` were both retrofitted to call it (no behavior change, their tests passed unmodified) so all Lot C conditional-Point sheets share one aggregation rule rather than three copies.
- **Lot C3/C4 (`UnconditionalIsolementSheetExtractionService`, done)** lives in `src/ExcelETL.Application/Extraction/Oxo/` (flat, not a `Platines/`-scoped folder — **renamed from `PlatinesExtractionService`** once building C4 confirmed ORIFICES CAPACITES' cell ranges/step/offsets are byte-identical to PLATINES per the spec: same `K6:U6` repere echo, `Step=8`, `B17:E18`/`H16:V17`/`B20:E22`). Every field is genuinely required (confirmed against all 3 real fixtures, no blanks observed) and every Colonne is unconditional for both sheets, so this delegates the block walk to the shared `IRepeatingBlockReader` and needs no `IConditionalPointRuleEvaluator` dependency at all — configured purely via `SheetExtractionRule` (sheet name differs, Colonne list differs: 7 for PLATINES — the `FIN` variants stay deliberately excluded per spec, no test covers them as nominal — vs. 4 for ORIFICES CAPACITES). Reuses `IsolementFieldNames` (`Identification`/`Designation`/`TypeElement`) rather than a sheet-specific field-names class, since they're identical across sheets. Verified against all 3 real fixtures: PLATINES 15/21/5 Isolements for C7401/D8570/G6306B, no errors; ORIFICES CAPACITES 0/5/2 (C7401's sheet is present but entirely empty for that dossier — a legitimate zero-isolement result, not an error), all `"TROU D'HOMME"`, no errors.
- **Lot C5 (`AutresJointsTouchesExtractionService`, done)** lives in `src/ExcelETL.Application/Extraction/Oxo/AutresJointsTouches/`. Reads its block (`Step=7`), 2 unconditional Colonnes plus 1 conditional (`"POSE ÉTIQUETTES"` unless `TypeElement = "TUBING"`) via the same `UnconditionalColonneNames` + `ConditionalPointGroupEvaluator` pattern as ISOLEMENT. Every field is genuinely required (confirmed against all 3 real fixtures), so — unlike ISOLEMENT — this delegates the block walk to the shared `IRepeatingBlockReader`. **Discovered while probing the real fixtures**: the Equipement repere echo does **not** live at `"K6:U6"` as the spec documents for this sheet — that cell is blank in all 3 files; the actual value sits at a plain, unmerged `N6` instead. Hardcoded as sheet-specific business knowledge (like PROCEDURE's header ranges), not trusted from the spec's stated range. Verified against all 3 real fixtures: C7401's sheet is empty (0 Isolements, same "unused sheet" pattern as C4); D8570 has 13 `TUYAUTERIE` (all get `"POSE ÉTIQUETTES"`); G6306B has 2 `TUYAUTERIE` + 2 `TUBING` (the latter correctly excluded, each producing one non-blocking warning).
- **Lot C6 (`DiversExtractionService`, done — completes Lot C)** lives in `src/ExcelETL.Application/Extraction/Oxo/Divers/`. Reads its block (`Step=3`), every Colonne conditional across 4 mutually-exclusive `TypeElement` values (`INSTRUMENTATION`×1/`ZERO ENERGIE`×1/`SOUPAPE`×2/`POINT FEU`×3 — 7 `ConditionalPointRule`s, zero unconditional) via `ConditionalPointGroupEvaluator` — **the sheet that aggregation fix exists for**. Every field required, uses the shared `IRepeatingBlockReader`. Also reads **`loc1`** (`B6:E6`, raw value, no transformation per spec §6) into a dedicated `DiversSheetExtractionResult` (not the shared `IsolementSheetExtractionResult` — `Loc1` is meaningless for the other 4 isolement-style sheets) for Lot D's orchestrator to broadcast onto the Equipement and every Isolement of the whole run; this service does **not** apply it to its own Isolements. Same `N6` repere-echo discrepancy as C5. **Verified directly against the real G6306B fixture**, which happens to cover all 4 `TypeElement` values in a single 4-row sheet, including the client-confirmed `"POINT DE FEU"` (real cell) vs. `"POINT FEU"` (confirmed base value) spelling mismatch — produces zero PF Points and **exactly one** aggregate warning for that Isolement, proving the aggregation fix end-to-end against real data, not just a hand-built unit test. D8570 (13× `ZERO ENERGIE`, all matching) and C7401 (0 rows, but `loc1` = `"ZONE 1"` still read from the header) round out the 3-fixture coverage.
- **Lot C complete.**
- **Lot D (`ImportPipelineOrchestrator`, done — pipeline complete)** lives in `src/ExcelETL.Application/Extraction/Oxo/`. Runs PROCEDURE first; if its `Equipement` is `null` (whole-file rejection, model doc §3.1), returns that same `ImportResult` immediately — verified by `Mock.Verify(..., Times.Never)` that the other 5 services are never even invoked, not just that their output is discarded. Otherwise runs ISOLEMENT/PLATINES/ORIFICES CAPACITES/AUTRES JOINTS TOUCHES/DIVERS, aggregates every `IsolementPivot`/`PointPivot`/`ExtractionError` (`TachesMultiples` come only from PROCEDURE), and broadcasts DIVERS's `loc1` onto the Equipement and every Isolement of the run via `with { Localisation = loc1 }` — applied unconditionally ("sans exception" per spec §1.5), even when `loc1` is blank (a no-op against the default-empty `Localisation`).
  - **Sheet roles are resolved from `ImportProfile.SheetRules` by matching `SheetName` against 6 fixed literal names** (`PROCEDURE`/`ISOLEMENT`/`PLATINES`/`ORIFICES CAPACITES`/`AUTRES JOINTS TOUCHES`/`DIVERS`), not a role enum — `SheetExtractionRule` has no explicit "role" tag independent of its configured name, and all 3 real fixtures plus the ticket's planned hardcoded profile use exactly these tab names. Revisit only if a client profile ever needs to rename a tab while keeping the same logical role; nothing today requires it.
  - `PLATINES` and `ORIFICES CAPACITES` both resolve to `IUnconditionalIsolementSheetExtractionService` (the shared C3/C4 service), called twice with two different `SheetExtractionRule`s — a unit test (`Times.Exactly(2)`) guards against accidentally collapsing them into one call.
  - **Lot D2 integration tests** (`ImportPipelineOrchestratorIntegrationTests`, Infrastructure.Tests) run the real C1-C6 services + `ClosedXmlWorkbookReader` against all 3 real fixtures with a hardcoded `ImportProfile` assembling every sheet's already-validated cell ranges — also the regression guard-rail the ticket calls for going forward. Isolement totals per file are the sum of each sheet's independently-confirmed count and matched on the first run: C7401 23 (8+15+0+0+0), D8570 67 (15+21+5+13+13, including the `"VANNE"` isolement), G6306B 18 (3+5+2+4+4). Assertions favor "no `RequiredFieldMissing` anywhere" and "`loc1` present on every Isolement" over hand-computed total Point/warning counts, which would be fragile arithmetic for no real benefit — the ticket only asks for isolement counts, known values, and absence of blocking errors.
- **Pipeline complete (Lot A→D).**
- **Lot E2 (EF Core persistence of `ImportProfile`, done)**: `IImportProfileStore` lives in `src/ExcelETL.Application/Extraction/Oxo/` (not the flat `Extraction/` folder `IExtractionConfigRepository` uses — deliberately deviated from the ticket's literal instruction to keep OXO Application code consistently under `Extraction/Oxo/`, per the same disk/namespace-collision rationale as Lot B/C/D; confirmed with the user before implementing). `EfImportProfileStore` (`src/ExcelETL.Infrastructure/Persistence/Repositories/`) follows `ExtractionConfigRepository`'s short-lived-DbContext-per-method pattern. `DeleteAsync`/unknown-id `GetByIdAsync` are no-ops/`null`, not exceptions (no ticket requirement either way, chosen for symmetry with simple CRUD).
  - **`SaveAsync` is a true upsert, keyed by `profile.Id`** — editing an existing profile so it matches a client's source Excel layout is this ETL's core workflow ("on ne recrée pas un nouveau profil à chaque fois"), confirmed by the user after an initial (wrong) "insert-only, revisit later" implementation. `Entity` (`Domain/Common/`) gained a second constructor, `protected Entity(Guid id)` (guards `id == Guid.Empty` with a plain `ArgumentException` — a developer-invariant check, not a user-facing business error, so no `DomainErrorCode`/localization per the i18n-scope rule above), alongside the existing parameterless one that mints a fresh `Guid.NewGuid()`. `ImportProfile` gained a matching 5-arg constructor (`Guid id, name, reperePrefix, equipementTypeElementNom, sheetRules`) that the two "create new" constructor overloads now funnel into (avoids tripling the validation logic) — this is how an edited profile is reconstructed under its original Id and handed back to `SaveAsync`. `SheetExtractionRule` (and everything nested under it) still has no identity of its own by design, so there's no meaningful old-vs-new nested-row diff to compute on update: `EfImportProfileStore.SaveAsync` removes the existing row's whole owned graph and inserts the new one under the same Id, as **two separate `SaveChangesAsync` calls** — tracking a `Removed` and an `Added` instance under the same key at the same time throws in EF Core's change tracker (confirmed empirically), so the delete must commit before the insert is attempted.
  - **EF mapping is owned types, not sibling entities** (unlike `ExtractionConfig`/`SheetConfig`/`CellMapping`, which are full `Entity`s in their own tables): `SheetExtractionRule` (`OwnsMany`, own table `ImportProfileSheetRules` + shadow `int Id` key, since it has no natural key), nested `RepeatingBlockLocator` (`OwnsOne`, table-split into the same row) whose own `Fields` is a second-level `OwnsMany` (`ImportProfileSheetRuleBlockFields`), and `PointRules` (`OwnsMany`, `ImportProfileSheetRulePointRules`) — both keyed the same shadow-`int Id` way. `UnconditionalColonneNames` (`IReadOnlyList<string>`) is a primitive collection, not an owned entity — EF Core 10 maps it to a `nvarchar(max)` JSON column on SqlServer, no extra configuration needed. All in `ImportProfileConfiguration.cs`.
  - **Required a Domain-layer change beyond the ticket's Infrastructure-only scope, discovered empirically**: EF Core's constructor-binding materialization cannot bind a constructor parameter to an entity-collection navigation at all ("Navigations to related entities... cannot be bound", confirmed by the actual EF exception) — so `RepeatingBlockLocator.Fields`, `SheetExtractionRule.PointRules`, and `ImportProfile.SheetRules` (all previously plain get-only auto-properties assigned straight from the constructor's `IReadOnlyList<T>` parameter) were changed to a private mutable `List<T>` backing field + a private parameterless constructor, exactly mirroring `ExtractionConfig._sheets`/`SheetConfig._cellMappings`'s existing pattern minus a public `AddX()` method (these three types are meant to stay fully immutable post-construction from any *caller's* perspective — the private ctor exists purely for EF's reflection-based materialization to overwrite immediately afterward, same trick `Entity.Id` already relies on). Scalar auto-properties (`Name`, `Sheet`, `SourceFieldName`, etc.) needed no change — EF's default backing-field convention already handles those. `UnconditionalColonneNames`/`ConditionalPointRule`/`BlockFieldDefinition` needed no change either (no entity-collection param in their constructors).
  - Tests: `EfImportProfileStoreTests` (Infrastructure.Tests, real EF Core InMemory provider, `TestDbContextFactory` reused from the existing `Persistence/` folder) cover full nested round-trip (Locator/Fields/PointRules/UnconditionalColonneNames), a dedicated `EquipementTypeElementNom` regression guard (the field this ticket was originally written to protect), multiple sheet rules persisting independently (no FK/data bleed across owned collections), `GetAllAsync` ordering, and not-found/no-op paths for `GetByIdAsync`/`DeleteAsync`.
  - Migration `20260717113850_AddImportProfile` adds `ImportProfiles`, `ImportProfileSheetRules`, `ImportProfileSheetRuleBlockFields`, `ImportProfileSheetRulePointRules` (all FK cascade-delete to their owner) — not yet applied to any real SQL Server database.
- **Lot F1.1 (`ImportProfiles.razor` list page, done)** lives in `src/ExcelETL.BlazorAdmin/Components/Pages/Admin/`, following `Mappings.razor`'s direct-repository-injection pattern (`IImportProfileStore`, not `DbContext`). Route is `/import-profiles` (**deliberately not** the ticket's literal `/admin/profils-import`/`/nouveau` — every sibling admin page uses a flat, single-English-word route segment despite living under the `Admin/` folder on disk, so this stays consistent with that existing convention rather than the ticket's suggested path; only the UI labels are localized, not the URL). "Créer" (`create-profile-button`) navigates to `/import-profiles/new` — a placeholder route with no page behind it yet (that's F1.2, the construction form) — rather than an inline create form, because unlike `ExtractionConfig` (name-only to create), `ImportProfile`'s constructor requires a non-empty `SheetRules` list, so there's no valid "create with just a name" flow to offer inline. "Dupliquer" (`duplicate-profile-button-{id}`) builds a new `ImportProfile` via the 4-arg (name, reperePrefix, equipementTypeElementNom, sheetRules) constructor — mints a fresh `Guid` automatically — with the name suffixed by a localized `ImportProfiles_DuplicateSuffix` string (`"(Copy)"`/`"(Copie)"`), then calls `SaveAsync` (insert, new Id) and reloads the list; no server navigation. `IImportProfileStore`/`EfImportProfileStore` newly registered `AddScoped` in `Program.cs` (shares the existing `IDbContextFactory<ExcelEtlDbContext>` registration — no new DbContext needed). Nav link added to `NavMenu.razor`/`.razor.css` (`bi-collection` icon) inside the existing admin-only `AuthorizeView` block alongside Users/Upload Test. Tests (`ImportProfilesTests.cs`) follow `MappingsTests.cs`'s real-EF-InMemory-provider convention, not a mock. Only a list page — no create/edit form yet; `/import-profiles/new` is an intentional dead link until F1.2.
- **Lot F1.2/F1.3 (`ImportProfileEditor.razor` construction form, done)** lives alongside `ImportProfiles.razor`, at route `/import-profiles/new` (the placeholder route F1.1's "Créer" button already linked to). One page, not incremental persistence: unlike `ExtractionConfig` (Mappings.razor), `ImportProfile` has no in-place mutation API -- it's built once via its constructor from a complete `SheetRules` list -- so this page accumulates root fields (`Name`/`ReperePrefix`/`EquipementTypeElementNom`) and a locally-held `List<SheetExtractionRule>` in component state, only calling `IImportProfileStore.SaveAsync` once, on a final "Save profile" click. `ReperePrefix` is prefilled with `ImportProfile.DefaultReperePrefix`; `EquipementTypeElementNom` deliberately has **no** default (client-confirmed 2026-07-17: no hardcoded "MAD TRAVAUX" suggestion in the component, echoing the exact anti-pattern already fixed once in `ProcedureExtractionService`, Lot C1).
  - "Add sheet rule" is its own nested mini-form: a repeating-block locator (`SheetName`/`FirstBlockStartRow`/`Step`/`StopFieldName`) plus three sub-lists built up field-by-field (`BlockFieldDefinition`, `UnconditionalColonneNames` as plain strings, `ConditionalPointRule`), each held in a `NewSheetRuleModel`/`NewBlockFieldModel`/`NewPointRuleModel` local class (mirrors `Mappings.razor`'s `NewSheetModel`/`NewMappingModel` convention). Clicking "Add sheet rule" constructs a real `RepeatingBlockLocator` then `SheetExtractionRule` -- validation (e.g. `Step > 0`) is enforced by the Domain constructors themselves, not duplicated client-side; a thrown `DomainValidationException`/`DomainArgumentOutOfRangeException`/`DomainRuleViolationException` is caught and localized via `BusinessExceptionLocalizer`, and the pending sub-form is deliberately **not** cleared on error (so the user can fix just the offending field) -- only reset on success.
  - **Discovered gap, fixed as part of this work**: `DomainErrorMessages.resx`/`.fr.resx` had zero entries for any of the 27 `DomainErrorCode` members the OXO extraction Domain layer (`Primitives`/`Pivot`/`Profile`) added -- the i18n milestones only ever covered the original `ExtractionConfig`/`SheetConfig`/`CellMapping`/`ExtractionHistory` surface. Added the 17 entries this form can actually throw (`ImportProfile_*`, `SheetExtractionRule_*`, `RepeatingBlockLocator_*`, `BlockFieldDefinition_*`, `ConditionalPointRule_*`); the remaining 10 (`DirectCell_*`, `SubstringAfter_*`, `Concat_*`, `FieldRef_*`, `EquipementPivot_*`, `IsolementPivot_*`, `PointPivot_*`, `TacheMultiplePivot_*`, `ExtractionError_*`) are still missing -- none are reachable from any host yet (the OXO pipeline isn't wired to a host), so left alone rather than speculatively translated; revisit when F2 or a future host surfaces them to a user.
  - Tests (`ImportProfileEditorTests.cs`) cover root-field defaults, the three empty-required-field validation errors (Name/ReperePrefix/EquipementTypeElementNom, each verified to reach the domain constructor's actual check order rather than a client-side duplicate), the `ImportProfile_NoSheetRules` case, a full add-sheet-rule happy path asserting the nested `Locator`/`Fields`/`UnconditionalColonneNames`/`PointRules` round-trip, the `Step <= 0` Domain-invariant mirror, and an end-to-end save-and-navigate-back-to-list test.
  - Not yet built: editing an existing profile (`/import-profiles/{id}` -- no ticket for this yet, F1.1's list page has no "Edit" action either).
- **Lot F2 (`ImportProfileTest.razor`, done -- Lot F complete)** lives alongside the other `ImportProfiles*` pages, at route `/import-profiles/test`. Picks a persisted `ImportProfile` (dropdown, `IImportProfileStore`) then an uploaded `.xlsx` (`InputFile`) is run **in process**: `new ClosedXmlWorkbookReader(fileStream)` (Infrastructure) straight into `IImportPipelineOrchestrator.Run(...)` (Lot D) -- no HTTP round trip to the Web API at all, unlike `UploadTest.razor`/`ExcelProcessingClient`, which this page deliberately does not reuse (ticket's explicit instruction; the two pages now demonstrate both integration styles side by side under `Components/Pages/Admin/`).
  - **First host to ever need the 6 per-sheet extraction services + `ImportPipelineOrchestrator` wired via DI** -- newly registered `AddSingleton` in `Program.cs` (`ITextTransformEvaluator`, `IConditionalPointRuleEvaluator`, `IRepeatingBlockReader`, `IProcedureExtractionService`, `IIsolementExtractionService`, `IUnconditionalIsolementSheetExtractionService`, `IAutresJointsTouchesExtractionService`, `IDiversExtractionService`, `IImportPipelineOrchestrator`) -- all stateless, so Singleton; WebAPI still only exposes the older `ExtractionConfig` pipeline and has none of these.
  - **Blocking vs. non-blocking is read directly off `ImportResult`, not a separate flag**: `Equipement is null` means the whole file was rejected (model doc §3.1) -- rendered as a single "File rejected" alert listing `Errors` -- otherwise every entry in `Errors` is a non-blocking warning (e.g. D8570's `"VANNE"` -> `UnrecognizedTypeElement`), rendered in its own "Non-blocking warnings" table alongside the successfully-extracted `Equipement`/`Isolements`/`Points`/`TachesMultiples` read-only tables. No separate blocking/non-blocking classification exists anywhere in Domain -- the orchestrator's contract already implies it.
  - Tests (`ImportProfileTestTests.cs`) reuse the exact same hardcoded profile as `ImportPipelineOrchestratorIntegrationTests` (Infrastructure.Tests) -- same already-validated cell ranges for all 6 sheets -- and run against all 3 real fixtures via the same `tests/Fixtures/` directory-walk-up helper, so this UI is proven against the same configuration the pipeline's own regression guard-rail exercises, per the ticket's explicit F2.2 requirement. Also covers the upload-mechanics gate (no profile selected -> error, no processing attempted) and a synthetic minimal rejected workbook (blank PROCEDURE header, built inline with ClosedXML) for the blocking-rejection path, since building an intentionally-invalid real fixture wasn't necessary just to hit that one check.
- **Lot F complete.** No target-`.xlsx`-writing work has started (out of scope for the extraction module, see `docs/tickets-tdd-extraction.md`'s closing section).
- **Lot G1 (`ILogger` instrumentation of the OXO pipeline, done)**: the audit-flagged gap ("`Extraction/Oxo/` has zero logging") is now closed for the extraction path. `ImportPipelineOrchestrator` takes `ILogger<ImportPipelineOrchestrator>`: `LogInformation` at the start and successful end of a `Run` (profile name, then isolement/point/error counts), `LogWarning` when PROCEDURE rejects the whole file, and the entire method body (after the `ArgumentNullException` guard clauses) is wrapped in `try`/`LogError`/`rethrow` for unexpected exceptions (e.g. `FindRule`'s `.First()` throwing if a profile is missing a sheet rule) — mirrors `ProcessExcelFileService`'s existing try/log/rethrow shape, the only prior precedent in the codebase. All 5 sheet services (`ProcedureExtractionService`, `IsolementExtractionService`, `UnconditionalIsolementSheetExtractionService`, `AutresJointsTouchesExtractionService`, `DiversExtractionService`) take their own `ILogger<T>` and log every `ExtractionError` they produce (including ones surfaced from `IRepeatingBlockReader.Read(...).Errors`, which the reader itself still doesn't log -- Lot B stays untouched, sheet-level services are where the sheet/business context lives) through a new shared static helper, `ExtractionErrorLogging.Log(ILogger, ExtractionError)` (`Extraction/Oxo/ExtractionErrorLogging.cs`): `ExtractionErrorCode.UnrecognizedTypeElement` (the explicitly non-blocking case, model doc §3.2) logs at `Warning`; `RequiredFieldMissing`/`UnparsableValue` (a block, or for PROCEDURE the whole file, being skipped/rejected) log at `Error`.
  - **Test convention decision (2026-07-18, user-confirmed)**: the ticket that requested this lot assumed `Mock<ILogger<T>>` + `Verify` was already an established pattern in this repo -- an exhaustive grep found zero occurrences; every existing `ILogger<T>` dependency in every test project (`ProcessExcelFileServiceTests`, `ClosedXmlExtractionServiceTests`, `LoginTests`, etc.) uses `NullLogger<T>.Instance` with no assertions on log calls. Flagged to the user before writing any test; **confirmed to follow the existing convention** rather than introduce a new one -- logging is treated as an observability side effect, not TDD-driven business logic, consistent with how `ProcessExcelFileService` originally added its own logging. All existing unit/integration tests for the 5 services + orchestrator (Application.Tests, Infrastructure.Tests) and `ImportProfileTestTests` (BlazorAdmin.Tests, DI-based) were updated to supply `NullLogger<T>.Instance` for the new constructor parameter; no test asserts on logging behavior itself.
  - DI: no `Program.cs` changes needed in either host -- `AddSingleton<TInterface, TImpl>()` resolves the new `ILogger<T>` constructor parameters automatically via the framework's already-registered logging services.
- **Lot G2 (structured log content: duration/sheet count/element count/coordinate, done)**: `ImportPipelineOrchestrator.Run` wraps its body in a `Stopwatch`, added right after the start log. The completion log gained `{ElapsedMs}`, `{SheetCount}` (a literal `SheetsProcessedOnSuccess = 6` -- always exactly 6 once PROCEDURE succeeds, no sheet-name collection exists in this class to count instead), `{TotalElementCount}` (`isolements.Count + points.Count + procedureResult.TachesMultiples.Count`), and the pre-existing per-category counts; the rejection (`LogWarning`) and unexpected-failure (`LogError`) paths also gained `{ElapsedMs}`. The per-`ExtractionError` logging G1 already added (`ExtractionErrorLogging.Log`, `{Code}`/`{Sheet}`/`{BlockIdentifier}`/`{Message}`) already satisfied the "exception type" requirement (`Code`) -- G2's own ticket asked to verify whether `BlockIdentifier` is a genuine merged-cell coordinate before logging it as one.
  - **Coordinate question resolved (2026-07-18, user-confirmed)**: `ExtractionError.BlockIdentifier` is not a reliable cell coordinate today -- checked every construction site: `ProcedureExtractionService`'s whole-file rejection passes a real cell range (`"M2:O2"`), `RepeatingBlockReader` passes a bare row number (`blockStartRow.ToString()`), and `IsolementExtractionService`/`AutresJointsTouchesExtractionService`/`DiversExtractionService` all pass the Isolement's business repère (e.g. `"D8570-V4"`). This is a *re-opening* of a decision already made at the model's original design (`modele-domaine-import-profile.md` §3 deliberately generalized the cahier des charges' "merged cell coordinate" into "repère ou n° de ligne"), not a new gap. Presented as a fork (log `BlockIdentifier` as-is vs. open a "G2bis" to thread a genuine coordinate through the Domain model + 5 services + `RepeatingBlockReader`) -- **user chose to keep `BlockIdentifier` as-is**, no Domain change, no G2bis. `ExtractionErrorLogging` (from G1) needed no changes as a result.
  - **New test double**: `CapturingLogger<T>`/`CapturedLogEntries` (`tests/ExcelETL.Infrastructure.Tests/Excel/CapturingLogger.cs`) -- a hand-rolled `ILogger<T>` that records `(LogLevel, formatted message, Exception?)` into a shared sink, mirroring the existing `FakeHttpMessageHandler` precedent (avoids adding `Microsoft.Extensions.Logging.Testing`/`Microsoft.Extensions.Diagnostics.Testing`, neither referenced anywhere in the repo, for one test file's worth of need). Deliberately *not* the `Mock<ILogger<T>>` + `Verify` pattern rejected in G1 -- this captures real formatted output from a full pipeline run for black-box assertions, not per-call-site invocation verification.
  - **`ImportPipelineOrchestratorLoggingIntegrationTests`** (Infrastructure.Tests, duplicates the same hardcoded profile/fixture-path helper as `ImportPipelineOrchestratorIntegrationTests` rather than sharing it, matching this repo's no-shared-test-helper convention): asserts the G6306B completion log contains `"in {N}ms"`, `"6 sheet(s) processed"`, and the exact total element count; asserts the D8570 run's `"VANNE"` isolement produces a `Warning`-level log entry (via `IsolementExtractionService`'s own `ILogger`) containing `UnrecognizedTypeElement` and the isolement's repère -- proving the non-blocking-warning case is visible in logs, not only in `ImportResult.Errors`.
- **Lot G3 (shared Serilog host config, done -- decision recorded 2026-07-19, see `docs/tickets-tdd-corrections-audit-coherence.md`)**: closes the gap the 2026-07-19 status snapshot (`docs/etat-avancement-lot-g-logging-oxo-2026-07-19.md`) flagged as never decided. **Decision**: *"Le pipeline OXO n'a pas de persistance de log dédiée ; il s'appuie sur `ILogger<T>` + la configuration Serilog de l'hôte appelant. Tout hôte qui résout un service du pipeline OXO doit appeler `AddOxoHostLogging(...)` pour garantir l'écriture dans `SystemLogs`."* No dedicated persistence for the OXO pipeline (no `ExtractionHistory`-like table/migration) -- the pipeline stays exactly as G1 left it, `ILogger<T>`-only in `ExcelETL.Application`/`ExcelETL.Infrastructure`, with no reference to the concrete Serilog package anywhere in those two projects. The problem G3 actually closes is duplication: before this lot, `ExcelETL.WebAPI/Program.cs` and `ExcelETL.BlazorAdmin/Program.cs` each carried their own independently-typed `UseSerilog(...)` block (console + `MSSqlServer`/`SystemLogs` sinks, `Enrich.WithProperty("Application", ...)`) -- nothing guaranteed a third host resolving OXO pipeline services would reproduce it correctly.
  - **New project, `src/ExcelETL.Hosting/`**: a small class library referenced **only** by the two host projects (`ExcelETL.WebAPI`, `ExcelETL.BlazorAdmin`), not by `Application`/`Infrastructure`/`Domain` -- keeps the "no concrete Serilog dependency below the host" rule intact while still de-duplicating the config. `SerilogHostLoggingExtensions.AddOxoHostLogging(this IHostBuilder, string applicationName, string connectionString)` is the call site each `Program.cs` now uses (`builder.Host.AddOxoHostLogging("ExcelETL.WebAPI", connectionString)` / `"ExcelETL.BlazorAdmin"`); the actual sink/enrichment setup is factored into a second public method, `Configure(LoggerConfiguration, applicationName, connectionString, IConfiguration)`, so it's testable without needing a real `IHostBuilder`/`WebApplicationBuilder`. Both host `.csproj` files dropped their own direct `Serilog.AspNetCore`/`Serilog.Sinks.MSSqlServer` `PackageReference`s (now pulled in transitively via the new `ProjectReference`) and gained a `ProjectReference` to `ExcelETL.Hosting`.
  - **Tests (`tests/ExcelETL.Hosting.Tests/SerilogHostLoggingExtensionsTests.cs`)**: call `Configure(...)` directly (no `IHostBuilder` involved) with `Serilog:EnableMsSqlServerSink=false` (same test-safe switch the two hosts' own tests already relied on) so nothing opens a real SQL Server connection. `[Theory]` over both host names (`"ExcelETL.WebAPI"`/`"ExcelETL.BlazorAdmin"`) proves the shared method behaves identically regardless of caller: the `Application` enrichment property matches whichever name was passed, `MinimumLevel.Override("Microsoft.AspNetCore", Warning)` filters/keeps events the same way for both, and building+logging never throws even with an unreachable connection string. This is the "non-duplication" guard-rail the ticket asked for -- it fails if a future edit reintroduces two independently-drifting copies of this configuration.
  - No regressions: `ImportPipelineOrchestratorLoggingIntegrationTests` (Lot G1/G2) and the full existing suite all still pass unmodified after the `Program.cs` rewiring -- see the test run below.
- **Lot G4 (auto-apply EF Core migrations on host startup, done -- decision recorded 2026-07-21)**: closes the gap that caused a real incident -- `AddImportProfile`/`AddExportProfile` (Lot E2/I6) had been merged for days but never applied to the dev LocalDB, so `/import-profiles` 500'd with `Invalid object name 'ImportProfiles'`. **Decision (user-confirmed)**: applying migrations was previously a manual `dotnet ef database update` step, defensible while the project was pure greenfield dev; now that the client is starting to use a test environment, that manual step is a standing risk, so **both hosts now apply their own pending migrations automatically on every startup**, relying on EF Core's built-in distributed migration lock (`sp_getapplock` on `__EFMigrationsLock`, observed directly in the SQL log during this lot) rather than an enforced host-startup order -- confirmed safe for two hosts sharing `ExcelEtlDbContext` (WebAPI + BlazorAdmin): whichever starts first applies pending migrations, the other blocks briefly on the lock then finds nothing left to do.
  - **New shared extension, `ExcelETL.Hosting/DatabaseMigrationHostExtensions.cs`** (same "define once, both hosts call it" precedent as `AddOxoHostLogging`, Lot G3): `IServiceProvider.MigrateIfEnabledAsync<TContext>(IConfiguration)` resolves `TContext` via its registered `IDbContextFactory<TContext>` (not `AddDbContext` directly -- neither host registers `ExcelEtlDbContext` that way) and calls `Database.MigrateAsync()`, gated by two checks: the `Database:AutoMigrate` config switch (default `true`) and `Database.IsRelational()`. `ExcelETL.Hosting` gained a `Microsoft.EntityFrameworkCore.Relational` package reference for this (still no reference to `Infrastructure`/`Domain`, stays a pure host-composition-root helper).
  - **Call sites**: `ExcelETL.WebAPI/Program.cs` calls it for `ExcelEtlDbContext` only, right before `app.Run()`. `ExcelETL.BlazorAdmin/Program.cs` calls it for both `ExcelEtlDbContext` and `ApplicationIdentityDbContext`, placed **before** the existing `IdentitySeeder.SeedAsync()` call (seeding needs the Identity schema to already exist).
  - **Why `IsRelational()` alone isn't enough for tests**: `ExcelProcessEndpointTests`/`HistoryDownloadEndpointTests` swap `ExcelEtlDbContext` to the EF Core InMemory provider via `ConfigureTestServices` -- `IsRelational()` correctly skips those. But `HealthPingTests`/`ApiKeyAuthenticationTests` (WebAPI) never swap the DbContext at all (they don't touch the DB, so today's lazy `IDbContextFactory` registration never opens a connection) -- an unconditional `Migrate()` call would newly force these to require a real, reachable SQL Server just to start the host, which they never needed before. All 4 `WebApplicationFactory<Program>`-based test fixtures in the solution now explicitly set `builder.UseSetting("Database:AutoMigrate", "false")`, mirroring the pre-existing `IdentitySeeding:Enabled` switch's precedent (`HistoryDownloadEndpointTests` already had that exact problem solved for seeding, same shape of fix applied here for migration).
  - **Verified against the real LocalDB `ExcelEtl` database** (not just unit tests): rolled back to before `AddImportProfile`/`AddExportProfile`, ran `dotnet run` for `ExcelETL.WebAPI`, confirmed in the startup log that it acquired the migration lock and applied both migrations before `Application started`, then confirmed via `dotnet ef migrations list` that both are applied. Full suite (622 tests) green after the `Program.cs`/test-fixture changes.
  - **Deliberately out of scope for this lot**: no pre-migration backup step, no CI/CD-gated migration pipeline. Acceptable while this is a test/pre-prod environment with no real client data at stake; flagged to revisit once the client's data in this environment has real value.
- **Lot I (target-workbook writing pipeline, done -- ticket doc `docs/tickets-tdd-ecriture-fichier-cible.md`)**: the symmetric counterpart to the read side (Lot A-E) -- an `ImportResult` (pivot) + a new `ExportProfile` in, a generated `.xlsx` out. **Not wired to any host as of this lot** (Blazor UI came later as Lot J; Web API exposure is still out of scope), so no `Program.cs` changes in either host at this point.
  - **I1/I2 (Domain, `src/ExcelETL.Domain/Generation/`)**: `ExportProfile`/`SheetGenerationRule`/`ColumnDefinition`/`PointColumnDefinition` (`Generation/Profile/`) are **all `sealed record`s with structural equality** -- a deliberate, ticket-mandated departure from the import side's `ImportProfile : Entity`/`SheetExtractionRule` (plain class) pattern. `ExportProfile` still carries its own `Guid Id` (plain property, not `Entity`-derived, since `Entity`'s identity-only `Equals`/`GetHashCode` override would conflict with record equality) so Lot I6's store can upsert by Id, using the same "reconstruct under an existing Id" constructor-overload trick as `ImportProfile`. `ColumnDefinition.Source` is `PivotFieldRef?` -- `null` is a valid, tested state ("column reserved in the schema, no rule wired yet, writes an empty cell"). `SheetGenerationRule` validates no duplicate `Header` across its `ColumnDefinitions`+`PointColumnDefinitions` combined, no duplicate `ColonneNom` among `PointColumnDefinitions`, and (added by I2) that every non-null `ColumnDefinition.Source`'s pivot type matches the rule's own `PivotSource` -- **checked at profile-construction time** (`DomainRuleViolationException`), not silently at generation time, per the ticket's explicit requirement. `PivotFieldRef` (`Generation/Fields/`, 9 members incl. `EquipementLocalisation`/`IsolementLocalisation` for the `loc1` broadcast) + `PivotFieldResolver.Resolve(EquipementPivot|IsolementPivot, PivotFieldRef)` give reflection-free typed field access; `PivotFieldResolver.GetPivotSource` is what `SheetGenerationRule` calls for its cross-validation.
  - **I3 (Application, `src/ExcelETL.Application/Generation/`)**: `GeneratedRow`/`GeneratedSheet`/`GeneratedWorkbook` are the ClosedXML-free intermediate output (plain records, list properties, no `SequenceEqual` override needed -- mirrors `RepeatingBlockReadResult`'s precedent, since tests use `BeEquivalentTo` rather than object equality). `SheetGenerationEngine : ISheetGenerationEngine` turns `(ImportResult, ExportProfile)` into a `GeneratedWorkbook`: header order is descriptive columns then Point columns; a Point column is marked with its `MarkValue` only when a `PointPivot` exists matching **both** `ParentRepere` and `ColonneNom` for that row. **User-confirmed decision**: when `ImportResult.Equipement is null` (upstream whole-file rejection), the Equipement-sourced sheet is still emitted with its header row and **zero data rows** (not omitted entirely) -- keeps the generated workbook's sheet count stable regardless of upstream outcome, symmetric to the import side's "an empty sheet is a legitimate result" precedent (ORIFICES CAPACITES, Lot C4). `IWorkbookWriter` (`Write(GeneratedWorkbook, Stream)`) is the Infrastructure-facing abstraction.
  - **I4 (Infrastructure, `src/ExcelETL.Infrastructure/Excel/ClosedXmlWorkbookWriter.cs`)**: new, explicitly **not** built on the legacy `ClosedXmlGeneratorService` (POC, consumes `ExtractionResult`/`ExtractedSheet`, not the OXO pivot model) per the ticket's own "hors périmètre" list. Tested against the real ClosedXML library (never mocked), same convention as `ClosedXmlWorkbookReader` (Lot E1). `TargetWorkbookFileNameBuilder.Build(equipementRepere, generatedAt)` implements the spec doc's `MAD_{Equipement.Nom}_{AAAAMMDDHHmmss}.xlsx` naming convention as its own testable function, separate from the write path -- `EquipementPivot` has no literal `Nom` property (only `Repere`/`Designation`/`TypeElementNom`/`Localisation`), so `Repere` (the glossary-confirmed `BaseElement` business search key) stands in for the spec's informal "Nom", documented inline as an interpretation call.
  - **I5 (`GenerationPipelineIntegrationTests`, Infrastructure.Tests)**: full chain (`ClosedXmlWorkbookReader` -> `ImportPipelineOrchestrator` -> `SheetGenerationEngine` -> `ClosedXmlWorkbookWriter` -> reread via ClosedXML) against all 3 real fixtures, duplicating the hardcoded `ImportProfile` from `ImportPipelineOrchestratorIntegrationTests` per this repo's no-shared-test-helper convention. The test `ExportProfile` is a **deliberate partial approximation** of the client's real `OXO_TRAME_IMPORT_MAD.xlsx` (2 sheets, `Parents`/`Enfants`) -- not full column fidelity, per the ticket's own "approximation de travail" framing; only a representative subset of descriptive/Point columns is mapped. Confirms known repère/`loc1` values land in generated cells, and that D8570's `"VANNE"` isolement (non-blocking `UnrecognizedTypeElement` warning upstream) still appears as an ordinary `Enfants` row. **Discovered while writing this test**: a generated-sheet `Repere` (Numéro/Numéro-equivalent) column is **not** a safe row-lookup key on its own -- it's composed independently per source sheet (`{K6:T6}-{Identification}`), so two different isolement-style sheets can coincidentally produce the same composed `Repere` text for otherwise-unrelated rows; the D8570 `"VANNE"` lookup disambiguates on `Repere` **and** `TypeElementNom` together.
  - **I6 (EF Core persistence, done -- Lot I complete)**: `IExportProfileStore` (`src/ExcelETL.Application/Generation/`, alongside the other Lot I Application types) / `EfExportProfileStore` (`src/ExcelETL.Infrastructure/Persistence/Repositories/`) mirror `IImportProfileStore`/`EfImportProfileStore` exactly -- short-lived `DbContext` per method, two-round-trip upsert keyed by `profile.Id` (delete-then-insert across two `SaveChangesAsync` calls, same change-tracker constraint as the import side). Owned-type EF mapping (`ExportProfileConfiguration.cs`): `SheetGenerationRule` (`OwnsMany`, own table + shadow `int Id`), nested `ColumnDefinitions`/`PointColumnDefinitions` (both second-level `OwnsMany`, their own tables, FK `SheetGenerationRuleId`). `ColumnDefinition.Source` is mapped `HasConversion<string>()` **without** `IsRequired()` so `null` round-trips as a genuine `null`, not a default enum value -- covered by a dedicated regression test (`SaveAsync_WithNullColumnSource_PersistsAndReloadsAsNull_NotADefaultValue`), the ticket's explicit requirement. Migration `20260718092214_AddExportProfile` adds `ExportProfiles`/`ExportProfileSheetRules`/`ExportProfileSheetRuleColumnDefinitions`/`ExportProfileSheetRulePointColumnDefinitions` (FK cascade-delete to their owner) -- not yet applied to any real SQL Server database.
- **Lot J (Blazor export-profile screens, done -- ticket doc `docs/tickets-tdd-blazor-profil-export.md`, symmetric to Lot F on the import side)**: wires Lot I's generation pipeline into BlazorAdmin. All 3 pages live in `Components/Pages/Admin/`, mirroring the `ImportProfiles`/`ImportProfileEditor`/`ImportProfileTest` file layout and TDD/bUnit conventions exactly.
  - **J1 (`ExportProfiles.razor`, route `/export-profiles`)**: list + create/edit/duplicate nav, `IExportProfileStore`-backed. Unlike `ImportProfiles.razor` there's no `EquipementTypeElementNom` column (`ExportProfile` has no such root field) and there's an `#edit-export-profile-button-{id}` per row navigating to `/export-profiles/{id}/edit` (J2) -- Import's list page has no edit action at all, since Lot F never added import-profile editing.
  - **J2 (`ExportProfileEditor.razor`, two `@page` routes on one component: `/export-profiles/new` and `/export-profiles/{Id:guid}/edit`)**: **deliberately supports editing an existing profile, unlike `ImportProfileEditor.razor`** -- the ticket's explicit rationale is that a target-format export profile gets adjusted regularly (columns going from unmapped to mapped, new sheets), so forcing duplicate-then-rename per tweak would be a real usability cost, not an architectural nicety worth copying from Lot F by default. `[Parameter] public Guid? Id { get; set; }` stays `null` on the `/new` route; `OnInitializedAsync` loads via `GetByIdAsync` when set, prefilling `_name` and the `_sheetRules` list, and tracks the loaded profile's `Id` separately (`_editingId`) so `SaveProfileAsync` reconstructs the profile under the **same** Id (`ExportProfile`'s `Guid`-taking constructor) rather than minting a new one. An unknown `Id` on the edit route sets a `_notFound` flag that suppresses the whole form (no exception, no empty-form flash). The "add sheet rule" sub-form's column `Source` select is filtered live to only the `PivotFieldRef` members matching the sheet's chosen `PivotSource` (`PivotFieldResolver.GetPivotSource`), with an explicit "not mapped yet" option mapping to `Source = null` -- keeps the UI from ever building a profile `SheetGenerationRule`'s own constructor would reject anyway (I2's cross-validation), though that constructor check still runs and is still what actually enforces it. Only 2 root-field validation tests (empty name, no sheet rules) vs Import's 4, since `ExportProfile` has only `Name` as a root field.
  - **Gap discovered and fixed while building J2**: `DomainErrorMessages.resx`/`.fr.resx` had **zero** entries for any of the 10 `DomainErrorCode` members Lot I's Generation domain types added (`ExportProfile_*`, `SheetGenerationRule_*`, `ColumnDefinition_*`, `PointColumnDefinition_*`) -- `BusinessExceptionLocalizer` was silently falling back to the raw resource-key string (e.g. literal text `"ExportProfile_EmptyName"` rendered to the user) for every one of them until J2's own tests caught it. Same class of gap as the one F1.2 fixed for the import-side OXO extraction errors; the two gaps were independent (different Domain folders, different ticket authors), not a repeat of the same oversight.
  - **J3 (`ExportProfileTest.razor`, route `/export-profiles/test`)**: covers both ends of the pipeline on one page, since generation needs an `ImportResult` as input. An **import**-profile select plus file upload (`e.File.OpenReadStream` → `ClosedXmlWorkbookReader` → `IImportPipelineOrchestrator.Run`, identical flow to `ImportProfileTest.razor`/F2) is followed by an **export**-profile select plus a "Generate workbook" button (`ISheetGenerationEngine.Generate` → `IWorkbookWriter.Write` into a `MemoryStream`), both entirely in process -- no HTTP anywhere, verified by a dedicated test that greps the component's own source text for `HttpClient`/`ExcelProcessingClient`/`IExcelDownloadInterop`. **Generation is structurally blocked, not just logically disabled, when the import is rejected** (`ImportResult.Equipement is null`): the export-profile select and Generate button simply aren't part of the render tree in that branch, proven by a `Mock<ISheetGenerationEngine>` test asserting `Generate` is never invoked. The download link needs no JS interop at all (unlike `UploadTest.razor`'s `IExcelDownloadInterop`) -- the generated bytes are already in memory, so it's a plain `<a href="data:application/vnd.openxmlformats-officedocument.spreadsheetml.sheet;base64,...">`. One HTML `<table id="generated-sheet-{sheetName}-table">` per `GeneratedSheet`, matching the ticket's "no need to visually reproduce Excel" note. Reuses the same hardcoded `ImportProfile` fixture as `ImportProfileTestTests`/`ImportPipelineOrchestratorIntegrationTests` and the same hardcoded `ExportProfile` fixture (`Parents`/`Enfants`) as `GenerationPipelineIntegrationTests`, per this repo's no-shared-test-helper convention -- confirms the D8570 `"VANNE"` isolement (non-blocking `UnrecognizedTypeElement` warning upstream) still lands as an ordinary row in the generated `Enfants` preview, mirroring I5's requirement at the UI level.
  - **J4 (DI, `Program.cs`)**: `IExportProfileStore`/`EfExportProfileStore` registered `AddScoped` (matches `IImportProfileStore`'s lifetime); `ISheetGenerationEngine`/`SheetGenerationEngine` and `IWorkbookWriter`/`ClosedXmlWorkbookWriter` registered `AddSingleton` (stateless, matches the 9 OXO pipeline services' lifetime). No DI-registration integration test was added -- no precedent for one exists anywhere in `BlazorAdmin.Tests` (the import-pipeline services aren't tested that way either), so per the ticket's own fallback instruction this is verified by reading `Program.cs` plus the fact that `ExportProfileTestTests` exercises the same services end-to-end via its own bUnit-local DI setup.
  - Two new nav-icon CSS classes added to `NavMenu.razor.css` for the Lot J nav links (`bi-file-earmark-spreadsheet-nav-menu` for the list page, `bi-download-nav-menu` for the test page) -- Import's `bi-collection-nav-menu`/`bi-play-circle-nav-menu` icons were deliberately not reused, to keep the two lots visually distinct in the sidebar.
  - **Lot J complete.**
- **Lot K (Web API exposure of the OXO pipeline, migration and POC retirement, done — ticket doc `docs/tickets-tdd-migration-webapi-oxo.md`)**: closes the gap that had blocked K4 for months — the OXO pipeline had no HTTP route, so the old `ExtractionConfig` pipeline (`ExcelController`/`ProcessExcelFileService`) couldn't be retired even after it was fully superseded.
  - **K0/K0bis**: audit found zero `ILogger` usage in `SheetGenerationEngine`/`ClosedXmlWorkbookWriter` (the generation half of the pipeline) — added, mirroring `ImportPipelineOrchestrator`'s Lot G1/G2 convention exactly (start/success/error logs with elapsed time, `NullLogger<T>.Instance` in tests, no logging assertions).
  - **K1/K2**: `POST /api/oxo/process` (`OxoController`, `src/ExcelETL.WebAPI/Controllers/OxoController.cs`) is the OXO pipeline's HTTP entry point — see the "Web API surface" bullet below for its contract. `ProcessOxoFileService` (`src/ExcelETL.Application/Extraction/Oxo/`) is the new HTTP-facing orchestration service, the OXO counterpart of `ProcessExcelFileService`: resolves both profiles (`ImportProfileNotFoundException`/`ExportProfileNotFoundException`, new `ApplicationErrorCode` members), runs the pipeline, distinguishes rejection from success, generates + archives (reuses `IFileStorageService`). `TargetWorkbookFileNameBuilder` moved from `ExcelETL.Infrastructure.Excel` to `ExcelETL.Application.Generation` (pure string logic, no ClosedXML dependency — Application needed it and can't reference Infrastructure).
  - **Real bug found and fixed during K1**: `ClosedXmlWorkbookReader.ReadCellValue` rendered date-typed cells via ClosedXML's `GetString()`, which uses `CultureInfo.CurrentCulture` — under the WebAPI host, `RequestLocalizationOptions` negotiates `en-US` by default per request, producing a date string `ProcedureExtractionService.TryParseDate`'s fixed `dd/MM/yyyy HH:mm:ss` format couldn't parse, **rejecting every real fixture** the instant the pipeline ran behind an actual HTTP request (never caught before: no prior host ran this pipeline under ASP.NET Core's culture negotiation). Fixed by rendering `XLDataType.DateTime` cells with `CultureInfo.InvariantCulture` explicitly instead of trusting `GetString()`.
  - **K3**: `legacy/ExcelProcessingClientService.ProcessAsync` now takes `(Guid importProfileId, Guid exportProfileId, HttpPostedFileBase file)`, posts to `api/oxo/process` — direct one-shot contract switch, no feature flag. Scoped strictly to that legacy library; the real ASP.NET MVC 5 call site lives outside this repository and was not touched (confirmed via `docs/etat-des-lieux-technique.md`: `legacy/` "ne fait pas partie de la solution .NET 10").
  - **K4 (POC retirement, done)**: removed the entire old pipeline — `ExcelController`, `ProcessExcelFileService`, `ClosedXmlExtractionService`/`ClosedXmlGeneratorService`, `IExtractionConfigRepository`/`IExtractionHistoryRepository` + EF configurations, Domain entities `ExtractionConfig`/`SheetConfig`/`CellMapping`/`ExtractionHistory` (+ `CellDataType`/`ExtractionStatus` enums), and BlazorAdmin's `Mappings.razor`/`UploadTest.razor`/`Dashboard.razor`/`History.razor`/`AdminEndpointRouteBuilderExtensions.cs`/`ExcelProcessingClient`/`IExcelDownloadInterop`/`WebApiClientOptions`/`wwwroot/js/fileDownload.js`. **Scope extended beyond the original ticket** once code review showed `ExtractionHistory` (POC-only data, confirmed single-writer `ProcessExcelFileService`) was still read by `Dashboard.razor`/`History.razor`/the history-download endpoint — all three retired together rather than left pointing at a frozen, permanently-stale table. `ImportProfiles.razor` gained a second `@page "/"` route as the new landing page (verified via browser: hitting `/` now triggers the `[Authorize]` redirect to `/Account/Login` instead of a 404, and `NavMenu` shows only `Logs`/`Register`/`Login` for an unauthenticated visitor). New EF Core migration `20260721095640_RemoveExtractionConfigPoc` drops `ExtractionConfigs`/`SheetConfigs`/`CellMappings`/`ExtractionHistories` — applied and verified against the real LocalDB via the dev server's auto-migrate (Lot G4). `Microsoft.EntityFrameworkCore.Design` added directly to `ExcelETL.WebAPI.csproj` (PrivateAssets="all") since Infrastructure's own reference is private and doesn't flow to the startup project `dotnet ef` needs. `ApplicationErrorCode`/`DomainErrorCode` and their `.resx` entries pruned to POC-only members (`WorksheetNotFoundInWorkbookException`'s error code kept — reused by the OXO reader). Full suite green after removal: Domain 215, Application 94, Infrastructure 101, WebAPI 13, BlazorAdmin 77, Hosting 6, legacy 15.
  - **Lot K complete — this was the last lot on the extraction/generation pipeline's roadmap; `ExtractionConfig`/`SheetConfig`/`CellMapping`/`ExtractionHistory` and every file listed above no longer exist in this repo.**
- **Default-profile seeding (done — ticket doc `docs/tickets-tdd-seed-profils-defaut.md`; the doc's own header calls it "Lot M", but that letter was already used for the unrelated NavMenu username/Profile-link merge above — referred to here by ticket doc name to avoid confusion, not renumbered)**: `DefaultProfileSeeder` (`src/ExcelETL.Infrastructure/Seeding/DefaultProfileSeeder.cs`) bootstraps the standard OXO import/export profiles on every `ExcelETL.BlazorAdmin` startup, mirroring `IdentitySeeder`'s pattern exactly — scoped, idempotent, gated by a config switch (`ProfileSeeding:Enabled`, default `true`, checked in `Program.cs` right after the `IdentitySeeding:Enabled` block, not inside the seeder itself). Looked up by two stable hardcoded `Guid` constants (`DefaultProfileSeeder.ImportProfileId`/`ExportProfileId`), never by `Name` — an admin can rename a seeded profile, so `Name` is not a safe identity check. **Strict idempotence, client-confirmed**: once a profile exists under that Id, the seeder never touches it again regardless of admin edits since — same "an existing account is never reset" behavior as `IdentitySeeder`.
  - The default import profile's 6 `SheetExtractionRule`s are transcribed verbatim from the already-validated hardcoded profile in `ImportPipelineOrchestratorIntegrationTests`/`GenerationPipelineIntegrationTests` (same cell ranges, steps, `UnconditionalColonneNames`, `ConditionalPointRule`s) — the ticket's own pre-implementation checklist re-verified every literal against the 5 real extraction services with zero divergence found. `EquipementTypeElementNom = "MAD TRAVAUX"` is the one business value hardcoded in the seeder itself (the Domain model deliberately has no default for it). PROCEDURE's header cell ranges and its 2 unconditional Points (`"TRAVAUX COMPLET"`/`"TRAVAUX DETAIL"`) and DIVERS' `loc1` (`B6:E6`) are **not** expressed in the seeded `SheetExtractionRule`s — confirmed still hardcoded inside `ProcedureExtractionService`/`DiversExtractionService` themselves, out of any profile's reach.
  - The default export profile is **deliberately minimal** (client-confirmed): only the descriptive columns the import profile already produces (`Repère`/`Type Elément`/`Zone`/`Désignation` for `"Parents"`; `Numéro`/`Type`/`Zone`/`Désignation`/`Position à la pose` for `"Enfants"`, all via `PivotFieldRef`) plus every Point `Colonne` the import profile actually produces (2 for `"Parents"` — the hardcoded PROCEDURE Points; 17 deduplicated across ISOLEMENT/PLATINES/ORIFICES CAPACITES/AUTRES JOINTS TOUCHES/DIVERS for `"Enfants"`) — no `Source = null` placeholder columns anticipating extraction rules that don't exist yet, unlike `GenerationPipelineIntegrationTests`' own fixture (which does include a couple of `Source = null` columns as a deliberate approximation for that unrelated test). Point column `Header`s reuse the raw `Colonne` name verbatim — no localized-label catalogue exists for these yet.
  - Tests (`tests/ExcelETL.Infrastructure.Tests/Seeding/`, real EF Core InMemory via a colocated `TestExcelEtlDbContextFactory`, never mocked): `DefaultProfileSeederTests` covers empty-database creation, no-duplicate-on-repeat-run, "never overwrite an admin-modified profile" for both profile types, and content assertions for every sheet rule/column (including the cross-consistency check that every `Enfants` `PointColumnDefinition.ColonneNom` is actually produced by the import profile, and the DIVERS `"POINT FEU"` vs `"POINT DE FEU"` literal guard). `DefaultProfileSeederPipelineIntegrationTests` re-runs the same non-regression assertions as `ImportPipelineOrchestratorIntegrationTests`/`GenerationPipelineIntegrationTests` (isolement counts per fixture, `loc1` broadcast, D8570's `"VANNE"` warning, generated headers/values) but against the profile **as seeded and fetched back via `IImportProfileStore`/`IExportProfileStore`**, not rebuilt inline — proving the seeder's transcription is correct, not just a hand-built test double.
  - **No dedicated test for `ProfileSeeding:Enabled = false`**: unlike `IdentitySeeding:Enabled`, which is exercised indirectly by WebAPI's `WebApplicationFactory`-based tests, there is no `WebApplicationFactory<Program>` integration-test project for `ExcelETL.BlazorAdmin` in this repo at all (`bin`/`obj` aside, confirmed by search) — so this flag has the same test coverage as its `IdentitySeeding:Enabled` sibling had before this lot: none, verified by code reading only. Building a new host-level test harness just for this one flag was judged disproportionate and out of this ticket's scope; revisit if/when a BlazorAdmin host-level test project is ever added for other reasons.

### Web API surface
- `POST /api/oxo/process` — `OxoController` (`src/ExcelETL.WebAPI/Controllers/OxoController.cs`), the sole HTTP entry point since Lot K4 retired the old `ExtractionConfig` pipeline. `multipart/form-data`, `[FromForm] ProcessOxoFileRequest { Guid? ImportProfileId; Guid? ExportProfileId; IFormFile File }` — **`Guid?`, not `Guid`, since Lot 036.1** (a public-contract change): a field totally absent from the multipart body is now a `400` naming the missing parameter (checked first, in order `ImportProfileId` then `ExportProfileId` — when both are missing only the first is reported), distinct from the pre-existing `404` for a syntactically-valid `Guid` matching no profile (via `IImportProfileStore`/`IExportProfileStore` → `ImportProfileNotFoundException`/`ExportProfileNotFoundException`); a syntactically-invalid `Guid` value is still rejected earlier by ASP.NET Core's own `[ApiController]` model binding, regardless of nullability. A missing `File` field entirely is also caught by that same automatic model-state validation (still `400`, but with ASP.NET Core's own generic "field is required" message, never reaching the controller body) — confirmed by test rather than assumed; only a **present-but-zero-length** file reaches the controller's own explicit `request.File.Length == 0` check (localized `EmptyFileUploadRequired` message). **Since Lot 036.2**, a file whose content can't be opened as a valid Excel workbook (`XLWorkbook`'s constructor throws `System.IO.FileFormatException`, confirmed by direct investigation, not assumed) is now caught explicitly in `OxoController` and translated to `400` (`InvalidExcelFileFormat`) — never an unqualified `500`; this exception type is a bare BCL one, not `IHasDomainErrorCode`/`IHasApplicationErrorCode`, so `GlobalExceptionHandler`/`BusinessExceptionLocalizer` alone would never have caught it (translation happens at the controller level, the same level that already handled the empty-file case, not via `GlobalExceptionHandler.StatusCodeFor`). Runs `ImportPipelineOrchestrator` then `SheetGenerationEngine`/`ClosedXmlWorkbookWriter`; a whole-file rejection (`ImportResult.Equipement is null`, model doc §3.1) returns `422 Unprocessable Entity` with a `ProblemDetails.Extensions["errors"]` list instead of attempting generation. On success, returns `FileStreamResult` (`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`), archived via the same `IFileStorageService` as the old pipeline. Max 10 MB, 5-min server timeout policy (`UploadLimits.MaxExcelFileSizeBytes` / `ExcelProcessingTimeout` / `ExcelProcessingTimeoutPolicy`) — kept identical to BlazorAdmin's `ImportProfileTest.razor`/`ExportProfileTest.razor` client-side cap so those pages exercise the server's real limit. **`legacy/ExcelProcessingClientService` unaffected by the `Guid` → `Guid?` change**: confirmed it always sends both fields as real, non-null `Guid` values (never omits them), so the new `400` path is unreachable from that client — no signal needed to Simon per the ticket's own contingency.
- API key auth: header `X-Api-Key` (`ApiKeyAuthenticationDefaults.HeaderName`), constant-time compare in `ApiKeyAuthenticationHandler`, value at appsettings key `ApiKeyAuthentication:ApiKey` (only set in `appsettings.Development.json` today — prod value expected via user secrets/env var). Applied as the fallback auth policy for the whole WebAPI app.
- Kestrel (WebAPI `Program.cs`): `MaxRequestBodySize`=10 MB, `KeepAliveTimeout`=5 min, `RequestHeadersTimeout`=2 min.
- Logging: Serilog, not the bare `ILogger` console provider — `WriteTo.Console()` plus a SQL Server sink (`Serilog.Sinks.MSSqlServer`, table `SystemLogs`, `AutoCreateSqlTable = true`, schema owned by Serilog itself and intentionally outside the EF Core Code-First migrations used for the domain database). **As of Lot G3, this setup is defined exactly once**, in `ExcelETL.Hosting/SerilogHostLoggingExtensions.cs` (`AddOxoHostLogging(this IHostBuilder, applicationName, connectionString)`) — both `ExcelETL.WebAPI/Program.cs` and `ExcelETL.BlazorAdmin/Program.cs` call it with only their own host name and connection string differing (`"ExcelETL.WebAPI"` / `"ExcelETL.BlazorAdmin"`, enriched via `Enrich.WithProperty("Application", ...)`), rather than each carrying its own copy of the sink/enrichment setup as before Lot G3. Both write to the same `SystemLogs` table so BlazorAdmin's `Logs.razor` page can render a unified log view distinguishing which host emitted each entry — `SystemLogsDbContext` (BlazorAdmin) reads this table read-only via its own `IDbContextFactory`, no migrations of its own. The sink is gated by config key `Serilog:EnableMsSqlServerSink` (default `true`); both `WebApplicationFactory`-based WebAPI integration tests and BlazorAdmin bUnit tests set it `false` so host startup never opens a real SQL Server connection. **As of Lot K4, this mechanism covers only the OXO pipeline** (the old POC pipeline and its separate `ExtractionHistory` table no longer exist) — the OXO pipeline still has no dedicated log persistence of its own (no Serilog dependency in `ExcelETL.Application`/`ExcelETL.Infrastructure`), it only emits through `ILogger<T>`, relying on whichever host resolves its services to have wired `AddOxoHostLogging(...)` so those events actually reach `SystemLogs`.

### BlazorAdmin state
- Auth pattern for admin pages: `@attribute [Authorize(Roles = IdentitySeeder.AdminRoleName)]` (see `Components/Pages/Admin/Users.razor`).
- Rendermode is set **once, globally**: `App.razor` → `<Routes @rendermode="InteractiveServer" />`, `Program.cs` → `.AddInteractiveServerRenderMode()`. Individual pages must not declare their own `@rendermode`.
- i18n: `src/ExcelETL.BlazorAdmin/Resources/BlazorAdminMessages.resx`/`.fr.resx` exists and is used by Logs/Users/ImportProfiles/ImportProfileEditor/ImportProfileTest/ExportProfiles/ExportProfileEditor/ExportProfileTest/GeneratedFiles/ApiTest via `IStringLocalizer<BlazorAdminMessages>`. Key convention: `{PageName}_{Element}` (e.g. `Users_PageTitle`, `Logs_Loading`, `ImportProfiles_*`, `ImportProfileEditor_*`, `ImportProfileTest_*`, `ExportProfiles_*`, `ExportProfileEditor_*`, `ExportProfileTest_*`, `ApiTest_*`).
- **As of Lot K4**: `ExcelProcessingClient`/`IExcelDownloadInterop`/`WebApiClientOptions`/`wwwroot/js/fileDownload.js` no longer exist — they existed only to support `/upload-test` (`UploadTest.razor`), retired along with the rest of the old `ExtractionConfig` pipeline. `ImportProfileTest.razor`/`ExportProfileTest.razor` run the OXO pipeline in-process (see Lot F2/J3). **As of Lot 038, `ApiTest.razor` (`/api-test`) is the one deliberate, documented exception to "BlazorAdmin never talks to WebAPI over HTTP"** — a real `HttpClient` (`OxoApiTestClient`) calling the deployed Web API's `POST /api/oxo/process`, for manual post-deployment verification/demo/debug. Treat any *other* future page needing HTTP as a new decision, not an extension of this one.
- Nav-item icons (`NavMenu.razor`): every `<span class="bi bi-{name}-nav-menu" aria-hidden="true">` markup reference must have a matching CSS rule in `Components/Layout/NavMenu.razor.css` defining its `background-image` (inline Bootstrap Icons SVG data URI, `fill='white'`, matching the existing entries' format). The two files are edited as a pair — adding a `NavLink`/icon span in the `.razor` without its `.razor.css` counterpart (or vice versa) silently renders a blank icon with no build error or warning.
- **Lot L1 (`NavMenu.razor` admin-link visibility, done — 2026-07-21, ticket doc `docs/tickets-tdd-blazor-navmenu-visibilite-authentification.md`)**: the admin-links block was already wrapped in a single `<AuthorizeView Roles="@IdentitySeeder.AdminRoleName">` (pre-existing from Lot K4, no `<NotAuthorized>` branch) — so admin links were already absent from the DOM for a non-Admin visitor before this lot; no real security regression existed, purely a UI-completeness gap. This lot added: a stable `id` (`nav-{route}-link`) on each of the 5 admin `NavLink`s (`nav-users-link`/`nav-import-profiles-link`/`nav-import-profiles-test-link`/`nav-export-profiles-link`/`nav-export-profiles-test-link` — previously none of `NavMenu.razor`'s links had `id` attributes at all, unlike `Components/Pages/Admin/*.razor`'s `id="..."-button"` convention). L1 also added a `<NotAuthorized>` branch to that same `AuthorizeView` with its own `#nav-login-link` — **this created a duplicate login link, fixed by L2 below, do not reintroduce it.**
- **Lot L2 (correctif, done — 2026-07-21, same ticket doc, opened after manual browser verification of L1)**: two regressions caught by hand in the browser, both traceable to L1's exhaustive search only covering `Authorize(Roles = IdentitySeeder.AdminRoleName)` — too narrow for the rest of `NavMenu.razor`. **Fix 1 (duplicate login link)**: removed the `<NotAuthorized>` branch L1 had added to the Admin-role `AuthorizeView` (reverted to `<Authorized>`-only, 5 ids kept); the single canonical login link is now the **pre-existing** generic `<AuthorizeView>` block's `Account/Login` link (any authenticated user — Profile/Logout/Register/Login), which now carries `id="nav-login-link"` (moved there, not duplicated — exactly one occurrence anywhere in the file, test-verified via `FindAll(...).Should().HaveCount(1)`). **Fix 2 ("Journaux" visible unauthenticated)**: the `logs` link had never been wrapped in any `AuthorizeView` at all (sat above both blocks, always rendered) — an exhaustive re-audit of every `@page`/`@attribute` in `ExcelETL.BlazorAdmin` found `Logs.razor` was the **only** menu-linked page with no explicit `@attribute` at all, relying entirely on the app-wide `FallbackPolicy` (`Program.cs:100-105`, `RequireAuthenticatedUser()`, no role) for its real protection — same level as `Profile.razor`, which *does* declare `[Authorize]` explicitly. Fixed both for consistency: added `@attribute [Authorize]` to `Logs.razor` (no server-behavior change, `FallbackPolicy` already covered it — just makes the declared protection match the codebase's own convention of every protected page self-declaring it), and wrapped the `logs` `NavLink` (now `id="nav-logs-link"`) in its own `<AuthorizeView><Authorized>` (no `Roles=`, since its real requirement is "authenticated, any role", not Admin) at its original top-of-menu position. `ImportProfileEditor.razor`/`ExportProfileEditor.razor` confirmed to still have no menu entry at all (unchanged since F1.2/J1/J2, not a gap). Tests (`NavMenuTests.cs`): the 2 pre-existing EN/FR culture tests had a now-incorrect assertion removed (they asserted "Logs"/"Journaux" visible *while unauthenticated*, which directly contradicted fix 2 — this was the bug, not a false test); added `..._ShowsLoginLink_ExactlyOnce`, `..._HidesLogsLink` (unauthenticated), `..._WithoutAdminRole_ShowsLogsLink` (authenticated, no role — same `SetAuthorized`-without-`SetRoles` pattern the pre-existing Logout test already used, not a fabricated non-admin account). Full proof in `docs/etat-avancement-lot-l2-navmenu-correctif-2026-07-21.md`; L1's own proof doc is `docs/etat-avancement-lot-l-navmenu-visibilite-authentification-2026-07-21.md` (still accurate for the 5 admin links + ids, superseded only on the login-link placement).
- As of Milestone 11 (self-service `/profile` page, `Components/Pages/Admin/Profile.razor`): `IUserRepository` (`Application/Identity/`) was extended beyond its original list-only `GetAllAsync` with `GetByIdAsync` (plain EF projection via `IDbContextFactory<ApplicationIdentityDbContext>`, returns nullable `UserProfile`), `UpdateProfileAsync`, and `ChangePasswordAsync` (both backed internally by `UserManager<ApplicationUser>`, added as a second constructor dependency on `UserRepository`). This resolved a pre-existing architectural inconsistency (`Users.razor` went through `IUserRepository` while `Login.razor`/`Register.razor` injected `UserManager<ApplicationUser>` straight from Infrastructure into components) — `Profile.razor` only ever injects `IUserRepository`, never `UserManager` directly. Identity failures (wrong current password, duplicate email, etc.) are surfaced as a new framework-free `IdentityOperationResult(bool Succeeded, IReadOnlyList<string> Errors)` record — `UserRepository` maps `IdentityResult.Errors[].Description` (already localized via the existing `LocalizedIdentityErrorDescriber`) into it, so Application stays free of `Microsoft.AspNetCore.Identity` types. "User id not found" is treated as a developer invariant (`InvalidOperationException`, out of i18n scope), not a translatable `IdentityOperationResult` failure, since callers always pass the current authenticated user's own id.
- Post-password-change re-authentication: Interactive Server components cannot write the auth cookie mid-circuit (same constraint documented on `IdentityComponentsEndpointRouteBuilderExtensions` for Logout), so `Profile.razor` cannot silently refresh the sign-in after `ChangePasswordAsync` succeeds. Instead it swaps its password form for a success message plus a raw (non-`EditForm`) `<form action="Account/Logout" method="post">` + `<AntiforgeryToken />`, identical to `NavMenu.razor`'s existing logout form — reuses the existing `/Account/Logout` endpoint verbatim, no new backend code.
- `Profile.razor` reads the current user id via `[CascadingParameter] Task<AuthenticationState> AuthState` (first consumer of this cascading value in the app; `AddCascadingAuthenticationState()` was already registered) + `ClaimTypes.NameIdentifier`, not `UserManager.GetUserId(principal)`, to avoid injecting `UserManager` into the component.
- `NavMenu.razor`'s generic `<AuthorizeView><Authorized>` block (any signed-in user, not just admins) now includes a `href="profile"` link (`bi-person-circle-nav-menu` icon) alongside the username display and logout form. **As of Lot M (below), the username display and the Profile link are a single merged element, not two.**
- **Bug fixed while building Milestone 11, found only by manual browser verification, not bUnit**: `StatusMessage.razor`'s `[CascadingParameter] HttpContext HttpContext` threw `NullReferenceException` in `OnInitialized()` when the component is reached via in-circuit client-side navigation (e.g. clicking a `NavLink` to another page while already connected to an established Interactive Server circuit) — `HttpContext` only cascades during the HTTP request that (re)established the circuit, so it's `null` for any later same-circuit navigation. Every prior `<StatusMessage>` usage (`Login.razor`, `Register.razor`) happened to always be reached via a fresh full-page request, so this never surfaced until `Profile.razor` became the first page reached from an in-app `NavLink` that also renders it. Fixed by making the cascading parameter nullable (`HttpContext?`) with null-conditional access; regression-tested in `StatusMessageWithoutHttpContextTests` (a separate `BunitContext` class deliberately *without* the `CascadingValue<HttpContext>` wrapper the other `StatusMessageTests` add). Existing bUnit tests for Login/Register/Profile all explicitly add that wrapper, so none of them would have caught this — worth remembering when writing new tests for any page reachable via in-app navigation that renders `StatusMessage`.
- **Fixed 2026-07-19**: `LoginTests.cs` selected the stale `#Input\.Email` selector (field has been `Input.UserName` since commit `b67ddf5`) and `MainLayoutTests.cs` asserted markup contained "About"/"À propos" chrome that commit `b4caa3b` removed — both were test-side drift, not production bugs. Updated the 3 `LoginTests` selectors to `#Input\.UserName` and dropped the dead "About"/"À propos" assertion from the 2 `MainLayoutTests` (the "unhandled error"/"Reload" assertions in the same tests were already correct and are kept). `ExcelETL.BlazorAdmin.Tests` is now 112/112 green.
- **Lot M (`NavMenu.razor` username/Profile-link merge, done — 2026-07-21, ticket doc `docs/tickets-tdd-blazor-navmenu-fusion-lien-profil.md`)**: the generic `<AuthorizeView>` block's standalone `<span class="nav-link">` (icon + `@context.User.Identity?.Name`, not a link) and the adjacent `Mon Profil`/`My Profile` `NavLink` are now one element — a single `NavLink id="nav-profile-link" href="profile"` whose content is `@context.User.Identity?.Name - @Loc["NavMenu_Profile"]`, keeping the Profile link's existing `bi-person-circle-nav-menu` icon. The standalone span had no `id` and used the `bi-person-fill-nav-menu` icon; the Profile `NavLink` itself had no `id` either (the ticket's assumed `nav-profile-link` didn't actually exist yet — verified by reading the file before coding, per the ticket's own instruction) — `nav-profile-link` was added fresh here, matching Lot L's `nav-{route}-link` convention, not "reused" in the literal sense the ticket phrased it. `bi-person-fill-nav-menu`'s CSS rule in `NavMenu.razor.css` was removed as an orphan (last markup reference gone) — no rule references it anymore. No new `.resx` keys (reuses `NavMenu_Profile`). Tests (`NavMenuTests.cs`): added `..._ShowsSingleLinkWithUsernameAndProfileLabel` (resolves `NavMenu_Profile` via `IStringLocalizer<BlazorAdminMessages>` in the test rather than a hardcoded string) and `..._DoesNotRenderStandaloneUsernameElement` (asserts `span.nav-link` no longer exists); the 2 pre-existing tests asserting username/Profile link presence separately (`ShowsLogoutLink`, `ShowsProfileLink`) needed no changes — both assertions still hold true of the merged element. Verified live via the actual dev server (registered a throwaway non-admin user, `/logs` page) in addition to bUnit — accessibility tree showed exactly one link `"navmenu-test@example.com - My Profile"` → `href="profile"`, no separate username node.
- **`ImportProfileEditor.razor` labels + inline sheet-rule editing, done — 2026-07-21, ticket doc `docs/ticket-profil-import-edition.md`**: the 3 root-field inputs (`profile-name-input`/`profile-repere-prefix-input`/`profile-equipement-type-element-nom-input`) gained `<label class="form-label" for="...">` elements (new `.resx` keys `ImportProfileEditor_NameLabel`/`ReperePrefixLabel`/`EquipementTypeElementNomLabel`, following the `Login.razor`/`Profile.razor` label convention — placeholders were left as-is, separate from the new labels). Existing sheet rules, previously plain read-only text with no way to edit/delete them, now get a "Modify"/"Delete" button each (`modify-sheet-rule-button-{index}`/`delete-sheet-rule-button-{index}`); Modify swaps that one `<li>` into an edit form pre-filled from the rule (`_editingIndex` tracks at most one rule in edit mode at a time), Delete removes it from the in-memory `_sheetRules` list immediately (no confirmation step, matching this page's existing no-confirmation pattern for other actions).
  - **New shared component, `SheetRuleForm.razor`** (colocated next to `ImportProfileEditor.razor`, since nothing else uses it): the sheet-rule sub-form (locator fields, block-field/unconditional-colonne/point-rule add-lists) used to live only inline in the "Add a sheet rule" card; it's now a single component reused for both the always-present "Add" card (`IdPrefix=""`, so its element ids are unchanged from before this ticket — `sheet-rule-name-input` etc. — no existing test needed updating) and each rule's edit-mode instance (`IdPrefix="edit-{index}-"`, e.g. `edit-0-sheet-rule-name-input`, freshly mounted per `@if (_editingIndex == index)` block so "Cancel" needs no explicit revert logic — the component is simply unmounted, discarding its in-progress edits, and a fresh instance re-reads the untouched original rule if "Modify" is clicked again). Submitting (`SubmitButtonId`/`SubmitLabel` differ per usage: `add-sheet-rule-button`/"Add sheet rule" vs. `save-sheet-rule-button-{index}`/"Save changes") builds a `SheetExtractionRule` via the Domain constructors (same validation/error-display as before) and invokes `EventCallback<SheetExtractionRule> OnSubmit` — the parent either appends it (`HandleAddSheetRule`) or replaces the existing list entry in place (`HandleSaveSheetRule(index, updated)`, also closes edit mode), so "Enregistrer le profil" (unchanged) picks up whichever `_sheetRules` state resulted.
  - **Real bug caught by the ticket's own tests, not a rewrite-only refactor**: `SheetExtractionRule.UnconditionalColonneNames` stores its constructor argument **by reference** (unlike `PointRules`, which the constructor copies via `[.. pointRules]` — see the type's own Domain source comment). `SheetRuleForm`'s `Submit()` originally passed its own mutable `_unconditionalColonneNames` list straight into the constructor, then called `ResetForm()` (which `.Clear()`s that same list) right after — silently wiping the just-submitted rule's colonne names for both the Add and Edit paths. Caught by `AddSheetRule_WithValidInput_DisplaysSheetSummaryAndResetsForm` (pre-existing test, unmodified) failing after the initial extraction. Fixed by passing a snapshot (`[.. _unconditionalColonneNames]`) into the `SheetExtractionRule` constructor instead of the live list.
  - Tests (`ImportProfileEditorTests.cs`, +12 vs. before this ticket): label-association, modify-button presence, edit-mode-affects-only-that-rule (2 sheet rules), full-field prefill (locator + block field + unconditional colonne + conditional point rule), save-updates-in-place-without-duplicating, cancel-restores-original-values, delete-removes-from-list, and a genuine reload-based persistence test (`SaveProfile_PersistsEditedSheetRule_VisibleAfterReload`, renders a **second**, fresh `ImportProfileEditor` instance against the same profile Id rather than just re-querying the store, per the ticket's "recharge les données" wording). All pre-existing tests for this page pass unmodified. **Not done, per the ticket's own "(si pertinent)" hedge**: no per-item removal of an individual block field / unconditional colonne / conditional point rule while building up a sheet rule (add-only, matching the pre-existing "Add a sheet rule" card's own long-standing limitation — never had removal either).
  - **Follow-up, same day**: the "(si pertinent)" block-field removal hedge above was resolved once the user asked for it directly, live in the browser (screenshot of the PROCEDURE rule's edit form with an unlabeled locator-field row and a plain read-only "Champs du bloc" list). Two additions, both extending the same reuse pattern rather than inventing a new one: (1) the `SheetRuleForm`'s 4 root locator inputs (`sheet-rule-name-input`/`-first-block-start-row-input`/`-step-input`/`-stop-field-name-input`, and their `edit-{index}-`-prefixed edit-mode counterparts) gained `<label>`s too, via 4 new `.resx` keys (`ImportProfileEditor_SheetNameLabel`/`FirstBlockStartRowLabel`/`StepLabel`/`StopFieldNameLabel` — deliberately separate from the pre-existing same-text placeholder keys, matching how the 3 root-profile-field labels were kept separate from their placeholders above). (2) A second new shared component, **`BlockFieldForm.razor`** (same file location, same add-vs-edit reuse shape as `SheetRuleForm.razor` itself — `IdPrefix`/`InitialField`/`SubmitButtonId`/`SubmitLabel`/`ShowCancel`/`CancelButtonId`/`OnCancel`/`OnSubmit<BlockFieldDefinition>`), lets each already-added `BlockFieldDefinition` inside a `SheetRuleForm` (whether that `SheetRuleForm` itself is in Add or Edit mode) get its own Modify/Delete/Save/Cancel — a third, nested level of the same "list of editable items" pattern used for sheet rules. Id scheme composes rather than repeats: the add-new-field row keeps its pre-existing id (`{outerPrefix}block-field-name-input`, via `BlockFieldForm IdPrefix="{outerPrefix}block-field-"`), while editing already-added field `j` uses `{outerPrefix}block-field-{j}-` as `BlockFieldForm`'s `IdPrefix` (e.g. top-level Add form editing field 0 → `block-field-0-name-input`; inside sheet-rule edit-mode 0 editing its field 2 → `edit-0-block-field-2-name-input`) — reuses `ImportProfileEditor_ModifyButton`/`DeleteButton`/`SaveChangesButton`/`CancelButton` verbatim, no new resx keys needed for those. `SheetRuleForm.AddBlockField` simplified from doing its own Domain-constructor try/catch to a one-line `_fields.Add(field)`, since `BlockFieldForm` now owns that validation/error-display itself (mirrors how `SheetRuleForm.Submit` already owns the sheet-rule-level validation). **Razor gotcha hit while building this**: putting a bare `@field.Name (...)` expression directly inside an `@if`/`else` block with sibling `<button>` elements (no wrapping tag of its own) produced nonsensical parser errors (`CS8802`/`CS9348`, "top-level statements", markup read as C#) — Razor needs an explicit element boundary around loose interpolated text sitting next to markup siblings; fixed by wrapping it in `<span>...</span>`. Tests (`ImportProfileEditorTests.cs`, +8): labels on both add- and edit-mode locator inputs, per-field modify/delete-button presence, prefill-on-modify, save-updates-field-in-place-without-duplicating, cancel-discards, delete-removes, and one true nested-edit-then-persist test (edits a field inside an already-in-edit-mode sheet rule, saves the field, saves the rule, saves the profile, reads back from the store). Full `ExcelETL.BlazorAdmin.Tests` suite: 102/102 green (was 94 before the first round of this ticket, 102 after both rounds).
- **Lot N (`ImportProfileEditor.razor` block-field ranges shown/entered as absolute Excel coordinates, done — 2026-07-22, ticket doc `docs/tickets-tdd-blazor-profil-import-lisibilite-plages-excel.md`)**: client feedback on a real ISOLEMENT screenshot — `TypeElement (B:E, 3-4)`'s raw `RowOffsetStart`/`RowOffsetEnd` (relative to the rule's `FirstBlockStartRow`) meant nothing to someone reading a cell reference in the real source file. Domain's `BlockFieldDefinition` is unchanged (still relative offsets — `Step` needs that to project onto every following block); only the Blazor presentation layer translates, both ways, both in the read-only list and the add/edit form (one representation everywhere, per the ticket's own explicit decision against showing offsets in one place and absolute ranges in another).
  - **New pure utility, `src/ExcelETL.BlazorAdmin/Formatting/BlockFieldRangeFormatter.cs`** (+ `BlockFieldRangeParseResult.cs`, same folder) — deliberately BlazorAdmin-layer, not Domain: this is a presentation concept `BlockFieldDefinition` has no business knowing about, consistent with the project's "architecture by relevance, not mimicry" precedent already noted elsewhere. `ToAbsoluteRange(firstBlockStartRow, columnRange, rowOffsetStart, rowOffsetEnd)` is a plain static string builder (single-column + equal offsets → single-cell notation `"B9"`, everything else → `"B19:E20"`). `FromAbsoluteRange(absoluteRange, firstBlockStartRow)` returns a `BlockFieldRangeParseResult` (a Result type, not a thrown exception — the ticket's own specified signature; this is a third, ticket-mandated exception to the "no generic Result pattern" rule, but scoped to this one BlazorAdmin-only presentation type, not a precedent for Domain/Application) with two independent validation levels: level 1 (blocking) rejects anything outside a real workbook's technical bounds (column `A`-`XFD`/1-16384, row `1`-`1 048 576`) or with the end before the start (row or column); level 2 (non-blocking) sets `IsBeyondPracticalRange` when column exceeds `AZ`(52) or row exceeds `1000` — arbitrary, client-confirmed thresholds based on the real fixtures observed to date (rightmost column `U`, highest start row `19`), kept as separate named constants from the level-1 Excel-format constants specifically so a future adjustment to the threshold is never confused with an actual workbook limit. Column-letter comparison goes through a base-26 `ColumnToNumber` conversion (not string comparison — `"Z" < "AA"` numerically but not lexicographically).
  - Tests (`tests/ExcelETL.BlazorAdmin.Tests/Formatting/BlockFieldRangeFormatterTests.cs`, pure xUnit, no bUnit needed): the ticket's own table cases (`Identification`→`B19:E20`, `TypeElement`→`B22:E23`, negative-offset `Designation`→`H18:U19`, single-cell), plus the most important regression guard of this lot — a round-trip test (`ToAbsoluteRange` then `FromAbsoluteRange` must reproduce the exact original `ColumnRange`/`RowOffsetStart`/`RowOffsetEnd`) run against **every block field of all 6 sheets of the real seeded default import profile** (`DefaultProfileSeeder`, run against the real EF InMemory provider via the project's existing `TestDbContextFactory`, not hand-built) — proves the conversion never silently drifts a coordinate already validated in production. Plus blocking-error cases (invalid format, reversed row/column order, column beyond `XFD`, row beyond `1 048 576`, row `0`) and the exact `AZ`/`BA`/`1000`/`1001` plausibility-threshold boundary cases from the ticket.
  - **`ImportProfileEditor.razor`/`SheetRuleForm.razor` (N2)**: both the top-level read-only sheet-rule display and `SheetRuleForm`'s own already-added-fields list (visible while a sheet rule — Add or Edit — is open) now render `BlockFieldRangeFormatter.ToAbsoluteRange(...)` instead of `{ColumnRange}, {RowOffsetStart}-{RowOffsetEnd}`; no HTML id changes, pure text-output change. Verified live against the real seeded profile in the dev server: `ISOLEMENT`'s `Identification`/`TypeElement`/`Designation` render exactly `B19:E20`/`B22:E23`/`H18:U19` (the negative-offset case), matching the ticket's own worked example verbatim.
  - **`BlockFieldForm.razor` (N3)**: the 3 inputs (`{prefix}column-range-input`/`{prefix}row-offset-start-input`/`{prefix}row-offset-end-input`) collapsed into one text field, `{prefix}absolute-range-input` (new `<label>`, `.resx` key `ImportProfileEditor_AbsoluteRangeLabel`/placeholder `ImportProfileEditor_AbsoluteRangePlaceholder`), plus a new required `[Parameter] public int FirstBlockStartRow` (supplied by the parent `SheetRuleForm`'s own `_firstBlockStartRow`, live-bound so it reflects whatever the user has currently typed, not just the value at mount time). Edit-mode prefill now calls `ToAbsoluteRange` in `OnInitialized` instead of copying the raw offset properties. `Submit()` calls `FromAbsoluteRange` first: failure → new `.resx` key `ImportProfileEditor_InvalidExcelRangeError` (one shared blocking-error message covering every rejection reason — invalid format, reversed range, out-of-Excel-bounds — per the ticket's own singular-key wording), no `BlockFieldDefinition` constructed, no `OnSubmit` invoked; success → build `BlockFieldDefinition` from the parsed `ColumnRange`/offsets (still inside the pre-existing `DomainValidationException` catch, for the `Name`-empty case), and if `IsBeyondPracticalRange` show a **separate, non-blocking** `.resx`-keyed warning (`ImportProfileEditor_ExcelRangeBeyondPracticalRangeWarning`) alongside still submitting/saving the field. Verified live in the dev server: opening ISOLEMENT's `TypeElement` field in edit mode prefills the text input with exactly `B22:E23` (confirmed via the rendered `edit-1-block-field-3-absolute-range-input` element's value), matching N3's worked example.
  - **Hors périmètre, explicitly deferred by the ticket**: `ExportProfileEditor.razor` (Lot J) has the likely-same readability defect but uses an unrelated model (`ColumnDefinition`/`PivotFieldRef`, not `BlockFieldDefinition`) — left untouched pending client validation of this lot on the import side first.
  - Tests (`ImportProfileEditorTests.cs`): every existing block-field test rewritten for the single-input UI (id changes only, same assertions in spirit); added the N2 seeded-ISOLEMENT absolute-range display test, the N3 `B19:E20`→offsets(0,1) round-trip-through-the-UI test, the TypeElement `B22:E23` prefill test, invalid-format/reversed-range rejection, out-of-Excel-bounds rejection, and the `BA1` beyond-practical-threshold warning-but-still-saved case. Full `ExcelETL.BlazorAdmin.Tests` suite: 127/127 green (was 102 before this lot; +25 across the new `BlockFieldRangeFormatterTests` file and the rewritten/added `ImportProfileEditorTests`).
- **Lot O (`ImportProfileEditor.razor` visual polish — block-field icon buttons + summary section labels, done — 2026-07-22, ticket doc `docs/tickets-tdd-blazor-profil-import-boutons-champs-bloc.md`)**: pure presentation, no behavior change, no new `.resx` keys (both O1 and O2 reuse existing `ImportProfileEditor_ModifyButton`/`DeleteButton`/`UnconditionalColonnesHeading`/`PointRulesHeading`).
  - **O1 — `SheetRuleForm.razor`'s block-field list** (the only place per-field Modify/Delete buttons exist — confirmed by reading the file first, per the ticket's own instruction; the read-only summary list in `ImportProfileEditor.razor` has no per-field buttons at all, only a single Modify/Delete per whole sheet rule): each `<li>` restructured from one `<span>Name (Range)</span>` + two text buttons into `<div class="block-field-info">` (name in a bold `.block-field-name` span, range on its own line in a `.block-field-range` span carrying Bootstrap's built-in `text-muted font-monospace` utility classes — no new CSS custom properties or font stack introduced, `--bs-font-monospace` already ships with the project's vendored Bootstrap) + `.block-field-actions` (two square 34px icon-only buttons, `btn-outline-secondary`/`btn-outline-danger`, inline SVGs matching Bootstrap Icons' `bi-pencil-square`/`bi-trash` shapes with `fill="currentColor"` so they tint with the button's own state color — no bootstrap-icons font/CSS ever referenced anywhere in this project, so this follows the same "inline SVG shape, not a font dependency" convention `NavMenu.razor.css` already established). New scoped `SheetRuleForm.razor.css` adds the flex layout, the `border-bottom` row separator, and the 34px icon-button sizing. **Ids unchanged** (`{prefix}modify-block-field-button-{index}`/`{prefix}delete-block-field-button-{index}`, confirmed by reading the file before touching it), `aria-label` added to both buttons since their text content is gone. 3 pre-existing tests (`BlockField_SaveChanges_UpdatesFieldInPlace_AndClosesEditMode`, `BlockField_Cancel_DiscardsChanges`, `BlockField_Delete_RemovesFieldFromList`) had asserted on the old concatenated `"Name (Range)"` markup string — rewritten to assert on `.block-field-name`/`.block-field-range` separately, per the ticket's own "two separate selectors, not one concatenated string" test guidance. Added `BlockField_DisplaysNameAndRangeAsSeparateElements_WithMonospaceRangeClass` (against the existing `BuildProfileWithIsolementSheetRule` fixture, ticket's own N0 example) and `BlockField_IconButtons_HaveAriaLabels`.
  - **O2 — `ImportProfileEditor.razor`'s read-only sheet-rule summary**: unconditional-colonne names, previously one comma-joined `<p>`, are now a `<ul><li>` (same shape as the point-rules list right below it) preceded by the `UnconditionalColonnesHeading` label reused verbatim from the edit form; the point-rules list gains the same treatment with the `PointRulesHeading` label. **Both label+list pairs are conditional on their own collection being non-empty** — verified live against the real seeded default profile: PLATINES/ORIFICES CAPACITES (no `ConditionalPointRule`s) show only the unconditional-colonnes label, DIVERS (no unconditional colonnes, all-conditional sheet) shows only the point-rules label, PROCEDURE (neither) shows neither. Added 3 tests scoped to the summary `<li class="list-group-item">` specifically via `QuerySelectorAll` (not `cut.Markup.Should().Contain(...)`) — the always-visible "Add a sheet rule" card at the bottom of the page renders a `SheetRuleForm` whose own two headings are unconditional, so a page-wide markup search would have falsely passed even before O2's conditional-rendering fix; this was caught during implementation, not anticipated up front.
  - Verified live against the real dev server (real seeded profile, real SQL Server LocalDB, not just bUnit): O1's icon buttons confirmed via computed styles (`font-family` resolves to the Bootstrap monospace stack, name `font-weight: 600`, delete button computed `color`/`borderColor` both the danger red, 34×34px, `aria-label`/ids intact) since the Browser pane's screenshot capture was unavailable this session (consistent timeouts unrelated to this change; `read_page`/`get_page_text`/`javascript_tool` all functioned normally and were used instead). O2's conditional labels confirmed against the full 6-sheet real profile via `read_page`'s accessibility tree.
  - Full `ExcelETL.BlazorAdmin.Tests` suite: 132/132 green (was 127 before this lot; +5).
  - **Hors périmètre, per the ticket**: `ExportProfileEditor.razor`'s likely-identical styling gap, deferred pending client validation on the import side (same deferral pattern as Lot N).
  - **Follow-up, same day (client feedback on a live screenshot, not a new ticket doc)**: two visual gaps O1 didn't close. (1) The read-only sheet-rule summary's field ranges (`ImportProfileEditor.razor`) still used the old plain `Name (Range)` inline text — visually inconsistent side by side with the edit form's new two-level/monospace styling right above it in the same screenshot. Fixed by rendering the summary's field list with the exact same `.block-field-item`/`.block-field-name`/`.block-field-range` markup as `SheetRuleForm.razor`'s own list (minus the per-field buttons, which the summary never had). (2) `SheetRuleForm`'s Save/Cancel (and Add) buttons were left-aligned, inconsistent with the right-aligned per-field icon buttons above them. **Discovered while fixing (1)**: Blazor CSS isolation scopes a `.razor.css` file's rules to only that component's own render output (a distinct `b-xxxxx` attribute per component) — reusing `.block-field-*` class names in `ImportProfileEditor.razor`'s markup would not have picked up `SheetRuleForm.razor.css`'s scoped rules at all. Fixed by moving every `.block-field-*` rule (plus a new right-aligned-actions helper class for the button fix — **renamed to `.right-aligned-actions` by Lot P below**, originally `.sheet-rule-form-actions`) from the now-deleted `SheetRuleForm.razor.css` into global `wwwroot/app.css`, since the same visual language is now genuinely shared by two different components — this is the only such shared-across-components CSS in `BlazorAdmin` today, every other page/component still keeps its own scoped `.razor.css` (`Logs.razor.css`, `NavMenu.razor.css`, etc.), so don't default to "put it in app.css" for anything that stays single-component. Both Save/Cancel and the standalone Add button (in the always-present "Add a sheet rule" card) are now wrapped in the right-aligned actions div. Verified live in the dev server (real seeded profile): the summary's `Action`/`C9:L9` now computes the same `font-family`/muted color as edit mode's fields, and both the Add-card button and the edit-mode Save/Cancel pair sit inside a `justify-content: flex-end` container. Added 3 tests to `ImportProfileEditorTests.cs` (`Summary_DisplaysFieldNameAndRangeAsSeparateElements_WithMonospaceRangeClass_LikeEditMode`, `SheetRuleForm_AddModeActionButton_IsInRightAlignedContainer`, `SheetRuleForm_EditModeActionButtons_AreInRightAlignedContainer`); full suite 135/135 green (was 132).
- **Lot P (per-sheet-rule cards + right-aligned action buttons, done — 2026-07-22, ticket doc `docs/tickets-tdd-blazor-profil-import-cartes-regles-feuille.md`, plus `docs/convention-ui-blazor-alignement-boutons.md`)**: closes a client-reported gap in the summary view left by N/O — sheet rules (`PROCEDURE`, `ISOLEMENT`, `PLATINES`, …) were separated only by the same thin rule used *inside* a rule between fields, with no visual card boundary and no typographic hierarchy between the sheet name and its metadata.
  - **New convention doc, `docs/convention-ui-blazor-alignement-boutons.md`** (referenced by this ticket, not written by it — read first per the user's instruction): formalizes "every action-button row is right-aligned within its enclosing container" as a standing rule for all of `ExcelETL.BlazorAdmin`, not just this screen. Backed by a single global CSS class, **`.right-aligned-actions`** (`wwwroot/app.css`, `display: flex; justify-content: flex-end; gap: 0.5rem;`) — **renamed from Lot O's follow-up's `.sheet-rule-form-actions`**, since that name was already too narrowly scoped to "sheet rule form" the moment a second, unrelated context (the P1 card's Modify/Delete, P2's root `save-profile-button`) needed the identical treatment. All 3 usages (`SheetRuleForm.razor`'s Save/Cancel/Add row, the new per-card Modify/Delete row, `save-profile-button`'s wrapper) share this one class.
  - **P1 — sheet-rule-card**: each non-editing rule in the summary (`ImportProfileEditor.razor`) is now `<li class="sheet-rule-card">` (bordered, rounded, padded) inside `<ul class="sheet-rule-list">` (`display:flex; flex-direction:column; gap:1.5rem` — a real gap between cards, 3× the 0.5rem internal padding used between block fields inside one card, so the eye reads card boundaries unambiguously per the ticket's own requirement). A `<div class="sheet-rule-card-header">` holds the sheet name in a bold `<h4 class="sheet-rule-card-title">` (page heading hierarchy is now h1 → h3 "Sheet rules" → h4 sheet name → h5 "Unconditional colonnes"/"Conditional point rules" from O2 — consistent, no level skipped) next to a `<span class="sheet-rule-card-meta text-muted">` for the row/step/stop-field metadata. **Resolves the ticket's own open question by reading the code first, as instructed**: a rule being edited is *replaced in place* by `SheetRuleForm` inside the same `<li>` (now `class="sheet-rule-editing-item"`, no card border) — it was never duplicated into the summary while its edit panel is open, confirmed by a new regression test (`EditingRule_IsNotRenderedAsACardOrDuplicatedInTheSummary`) rather than left as a hypothetical.
  - **Split resource key**: `ImportProfileEditor_SheetSummary` (`"{0} -- start row {1}, step {2}, stop field '{3}'"`, one string mixing the sheet name into the same template as its metadata) is **replaced**, not kept alongside, by `ImportProfileEditor_SheetMetadata` (`"start row {0}, step {1}, stop field '{2}'"`, no name placeholder) — the sheet name itself is rendered directly from `rule.SheetName` (a raw field value, not a template), consistent with O1's precedent of not localizing plain data. `ImportProfileEditor_SheetSummary` had exactly one call site (this one), so removed outright rather than left as an orphaned key, matching Lot K4's precedent of pruning unused resource entries rather than accumulating them.
  - **P2 — `save-profile-button`**: wrapped in its own `.right-aligned-actions` div, no id/behavior change. Explicitly not a "sheet-rule-card" concern (it belongs to the root profile form), but governed by the same convention doc.
  - Tests (`ImportProfileEditorTests.cs`): the 3 pre-existing O2 tests that selected `li.list-group-item` were updated to `li.sheet-rule-card` (Bootstrap's `list-group`/`list-group-item` classes are gone from this view entirely, replaced by the card treatment) — same test bodies otherwise, per the ticket's "reuse, don't duplicate" instruction. Added `Summary_WithMultipleSheetRules_WrapsEachInADistinctCard` (card count == rule count), `Summary_SheetNameAndMetadata_AreSeparateElements`, `EditingRule_IsNotRenderedAsACardOrDuplicatedInTheSummary`, `SaveProfileButton_IsInRightAlignedContainer`. Full `ExcelETL.BlazorAdmin.Tests` suite: 139/139 green (was 135 before this lot; +4). Verified live against the real dev server (real seeded profile, after a few flaky Browser-pane preview starts unrelated to this change): all 6 seeded sheet rules render as `.sheet-rule-card` (bordered, `border-radius: 8px`), the first card's title element is a real `<h4>PROCEDURE</h4>` separate from its `.sheet-rule-card-meta` text, `.sheet-rule-list` computes `display: flex`/`gap: 24px` between cards, and both `save-profile-button` and the card's `modify-sheet-rule-button-0` sit in a `.right-aligned-actions` container computing `justify-content: flex-end`.
  - **Hors périmètre, per the ticket**: `ExportProfileEditor.razor`'s identical treatment, deferred pending client validation on the import side (same deferral pattern as N/O).
- **Follow-up, same day (client feedback on a live screenshot, not a new ticket doc): delete confirmation for `delete-sheet-rule-button-{index}`**. `ImportProfileEditor.razor` no longer removes a sheet rule on the first click — clicking `Delete` sets `_pendingDeleteIndex = index`, swapping the card's action row for a warning message (`ImportProfileEditor_ConfirmDeleteSheetRuleMessage`) plus `confirm-delete-sheet-rule-button-{index}`/`cancel-delete-sheet-rule-button-{index}` (both new ids; `Modify`/`Delete`'s own ids are unchanged). Only `confirm-delete-sheet-rule-button-{index}` actually calls `DeleteSheetRule(index)`. **Researched first, per this project's "no existing pattern → don't invent one blindly" habit**: the codebase has zero prior confirmation-dialog precedent (no Bootstrap modal, no `window.confirm`, `ReconnectModal.razor`'s `<dialog>` is framework-owned reconnect UI, not reusable) and is on record (Lot K4 notes) as deliberately avoiding JS interop beyond `Logs.razor`'s one clipboard call — so this uses the same **inline two-step state toggle** idiom already established throughout this exact file (`_editingIndex`, `_pendingDeleteIndex` now sibling fields), not a new interop dependency. Scoped to the sheet-rule level only (a whole rule's nested fields/columns/point-rules is a meaningfully bigger loss than one block field), block-field-level delete is untouched. 4 tests updated/added in `ImportProfileEditorTests.cs` (first click shows confirmation without removing, confirm removes, cancel restores the original buttons and keeps the rule); full suite 141/141 green (was 139).
  - **Incident during this fix's own live verification, remediated same session**: while manually clicking through the confirmation flow against the real dev server, the Browser pane's preview infrastructure was unusually unstable this session (multiple `preview_start` processes exiting within seconds for reasons unrelated to the code change, and at least one tab losing its page state mid-interaction — `location.href` reporting `non-http`/blank). One such click sequence against the **real seeded default profile** (`DefaultProfileSeeder.ImportProfileId`, `a2d81110-6ed6-4b56-ac38-59e543c79f22`) resulted in `PROCEDURE` and `ISOLEMENT` being genuinely deleted and persisted to the local `ExcelEtl` LocalDB — confirmed directly via `sqlcmd` (bypassing the unreliable browser/UI layer entirely) that `ImportProfileSheetRules` for that Id dropped from 6 rows to 4. The exact causal mechanism (stale server binary vs. a replayed/duplicated click during a circuit reconnect) was not conclusively identified, and wasn't worth over-investigating on a pre-prod-only database. **Fix, user-approved before executing** (a destructive action, not something to silently self-authorize even when self-caused): `DELETE FROM ImportProfiles WHERE Id = '...'` via `sqlcmd` (FK cascade removed the remaining 4 rows' children automatically, per the existing cascade-delete mapping), then one fresh app startup let `DefaultProfileSeeder` recreate the profile from scratch — verified restored to all 6 rows again via direct `sqlcmd`, not through the same unreliable UI that surfaced the problem. **Takeaway for future sessions**: when this Browser pane preview infrastructure is visibly flaky (servers dying within seconds, a tab's URL going blank mid-click), prefer verifying structural/behavioral correctness through the bUnit suite over live-clicking against the real seeded dev profile — and if live verification is still needed, use a disposable throwaway profile (created via `/import-profiles/new`, never saved) rather than the shared seeded one, exactly as this session ultimately did once the risk became clear.
- **Follow-up, same day: visible labels on every remaining "Add a sheet rule" input.** Client feedback on a screenshot showed `BlockFieldForm`'s field-name input and 5 `SheetRuleForm` inputs (unconditional-colonne name, and the 4 conditional-point-rule inputs: colonne name, source field name, operator, comparison value) still relying on placeholder-only text, unlike the root locator fields and the absolute-range input labeled earlier this same day. Added 5 new `.resx` label keys (`ImportProfileEditor_FieldNameLabel`/`ColonneNameLabel`/`SourceFieldNameLabel`/`OperatorLabel`/`ComparisonValueLabel`, EN+FR) — kept **separate from** the pre-existing `*Placeholder` keys even though the English text happens to match, consistent with how `AbsoluteRangeLabel`/`AbsoluteRangePlaceholder` were already split; `ColonneNameLabel` is reused for both the unconditional-colonne input and the point-rule colonne-name input, mirroring how `ColonneNamePlaceholder` was already shared between the two. One new test, `SheetRuleForm_RemainingInputs_HaveVisibleLabels`. Full `ExcelETL.BlazorAdmin.Tests` suite: 142/142 green (was 141). **Not live-verified in the browser this time** — the Browser pane's preview processes kept exiting immediately again (`navigation ... denied or failed` on every attempt), and per the caution note directly above, a purely-additive/no-new-interaction change like this doesn't justify pushing further against flaky preview infra; confidence rests on the bUnit suite alone.
- **`ExportProfileEditor.razor` visual parity with the import side, Q1/Q2/Q3/Q6 done — 2026-07-22, ticket doc `docs/tickets-tdd-blazor-profil-export-parite-visuelle.md`**: closes the deferral flagged repeatedly above ("Hors périmètre... deferred pending client validation on the import side") — the client explicitly reopened it once the import side's N/O/P treatment was in place, and asked for uniformity rather than a second, independently-styled screen. **Deliberately split into two tickets, this one first**: visual/structural parity (this bullet) now, functional parity (Modify/Delete an already-added `SheetGenerationRule`/`ColumnDefinition`/`PointColumnDefinition`, which **do not exist at all today** on the export side — only "Add", no undo before `SaveProfileAsync`) deferred to a second ticket, `docs/tickets-tdd-blazor-profil-export-parite-fonctionnelle.md`, so the bigger/riskier component-extraction work doesn't ship in the same pass as a zero-risk markup/CSS change.
  - Every input (`export-profile-name-input`, `sheet-generation-rule-name-input`/`-pivot-source-select`, `column-header-input`/`-source-select`, `point-column-nom-input`/`-header-input`/`-mark-value-input`) gained a `<label>`, 8 new `.resx` keys (`ExportProfileEditor_NameLabel`/`SheetNameLabel`/`PivotSourceLabel`/`HeaderLabel`/`SourceLabel`/`ColonneNomLabel`/`MarkValueLabel`, EN+FR) kept separate from the pre-existing `*Placeholder` keys even where the English text coincides — same convention as the import side's `AbsoluteRangeLabel`/`AbsoluteRangePlaceholder` split. `PivotSourceLabel`/`SourceLabel` have no placeholder counterpart (the `<select>` elements never had one).
  - The summary list of already-added `SheetGenerationRule`s (and the "Add a sheet rule" card's own in-progress lists) moved from `<ul class="list-group">`/`<li class="list-group-item">` + one concatenated text run per column/point-column to `<ul class="sheet-rule-list">`/`<li class="sheet-rule-card">` with a `<h4 class="sheet-rule-card-title">`/`<span class="sheet-rule-card-meta">` header and `.block-field-list`/`.block-field-item`/`.block-field-name`/`.block-field-range` rows per column/point-column — **reusing every one of these classes verbatim from `wwwroot/app.css`**, zero new CSS added, exactly the "don't duplicate styles, it's the same layout" instruction the client gave when asking for this ticket. New resx key `ExportProfileEditor_SheetMetadata` (`"Source: {0}"`/`"Source : {0}"`) is the export-side counterpart of `ImportProfileEditor_SheetMetadata`, with only one placeholder (`PivotSource`) since `SheetGenerationRule` has no locator/row/step metadata to show. A `ColumnDefinition`'s secondary line is its `Source` (or `ExportProfileEditor_SourceNotMapped` if `null`); a `PointColumnDefinition`'s is `"{ColonneNom} · {MarkValue}"` — both in `.block-field-range` (muted + monospace), an explicit, documented reuse of a class name inherited from the import-side `BlockFieldDefinition` domain concept even though it no longer names what it's applied to on the export side (flagged as a deliberate low-risk choice in the ticket, not revisited here).
  - `add-sheet-generation-rule-button` and `save-export-profile-button` (the two bottom-of-card/section buttons) wrapped in `.right-aligned-actions` (also already global). `add-column-definition-button`/`add-point-column-definition-button` deliberately **not** wrapped — same carve-out as the import side's convention doc: they're already at the end of their input row by construction.
  - Tests (`ExportProfileEditorTests.cs`, +7): label-association for all 8 inputs, multiple-rules-render-as-distinct-cards, sheet name/metadata as separate elements, column/point-column name+value as separate elements (including the no-source-mapped case), and the two right-aligned-container checks — same assertion style (`label[for=...]`, `.sheet-rule-card`, `.block-field-name`/`.block-field-range`, `.ParentElement!.GetAttribute("class")`) as the equivalent `ImportProfileEditorTests.cs` tests, reused rather than invented fresh. Full `ExcelETL.BlazorAdmin.Tests` suite: 149/149 green (was 142).
  - **Not live-verified in the browser this session** — the Browser pane's preview infrastructure was down again (`navigation ... denied or failed` on every attempt, empty tab origin), same class of instability as the two incidents noted above; per that precedent, a purely-additive markup/CSS change with a green bUnit suite and a green full-solution build doesn't justify forcing it further. Live verification (and the functional-parity ticket) remain for a future session once the preview is stable again — use a disposable throwaway profile (`/export-profiles/new`, never saved) if the shared seeded export profile exists by then, not the shared one directly.
- **`ExportProfileEditor.razor` functional parity with the import side, Q4/Q5 done — 2026-07-22, ticket doc `docs/tickets-tdd-blazor-profil-export-parite-fonctionnelle.md`**: closes the gap the visual-parity lot above deliberately deferred — an already-added `SheetGenerationRule`/`ColumnDefinition`/`PointColumnDefinition` can now be modified or deleted, not just added, mirroring `ImportProfileEditor.razor`'s `SheetRuleForm`/`BlockFieldForm` edit-in-place architecture exactly.
  - **3 new components** (`Components/Pages/Admin/`, same folder as the page, matching `SheetRuleForm.razor`/`BlockFieldForm.razor`'s precedent): `SheetGenerationRuleForm.razor` (root-level add/edit, `IdPrefix`/`InitialRule`/`SubmitButtonId`/`SubmitLabel`/`ShowCancel`/`CancelButtonId`/`OnCancel`/`OnSubmit<SheetGenerationRule>` — identical parameter shape to `SheetRuleForm`), `ColumnDefinitionForm.razor` and `PointColumnDefinitionForm.razor` (nested add/edit, same shape as `BlockFieldForm`). `ColumnDefinitionForm` takes a live-bound `PivotSource` parameter (mirrors `BlockFieldForm.FirstBlockStartRow`) so its source `<select>` options stay filtered as the parent's pivot-source selection changes. Id composition mirrors the import side's nesting scheme verbatim: root "Add" instances keep the pre-existing unprefixed ids (`sheet-generation-rule-name-input`, `column-header-input`, `point-column-nom-input`, etc. — zero test churn for the add-only flows), an already-added column/point-column `j` being edited gets `{outerPrefix}column-{j}-`/`{outerPrefix}point-column-{j}-`, and a root-level sheet rule `i` being edited gets `edit-{i}-`, composing to e.g. `edit-0-column-2-header-input` for editing column 2 inside sheet rule 0's edit mode.
  - **No by-reference aliasing bug to guard against here** (unlike the import side's `SheetExtractionRule.UnconditionalColonneNames`, which stores its constructor argument by reference and required a defensive snapshot in `SheetRuleForm.Submit()`): `SheetGenerationRule`'s constructor already copies both `ColumnDefinitions`/`PointColumnDefinitions` via `[.. ...]` internally, so `SheetGenerationRuleForm.Submit()` can call `ResetForm()`'s `.Clear()` right after `OnSubmit.InvokeAsync(rule)` with no snapshot needed — verified by reading `SheetGenerationRule.cs` before writing the component, not assumed from the import-side precedent.
  - `ExportProfileEditor.razor` gained `_editingIndex`/`_pendingDeleteIndex` fields and `HandleAddSheetRule`/`HandleSaveSheetRule`/`ConfirmDeleteSheetRule`/`DeleteSheetRule` methods, identical in role to `ImportProfileEditor.razor`'s. Delete confirmation is the same inline two-step toggle idiom (no JS interop, no modal) — 3 new `.resx` keys (`ExportProfileEditor_ConfirmDeleteSheetRuleMessage`/`ConfirmDeleteButton`/`CancelButton`, EN+FR) plus `ModifyButton`/`DeleteButton`/`SaveChangesButton` reused as icon `aria-label`s and edit-mode button labels — page-scoped keys, not a duplication of the import side's identically-worded ones, consistent with this project's per-page resx-key convention. **Scoped to the sheet-rule level only, same as import**: deleting a column or point-column is immediate, no confirmation — a column/point-column loss is smaller than a whole rule's nested state, matching the precedent already established for `BlockFieldDefinition` deletes.
  - Tests (`ExportProfileEditorTests.cs`, +21): sheet-rule-level modify/prefill/save-in-place/cancel/delete-confirm-first-click/delete-confirm/delete-cancel/persist-after-reload/add-still-works-after-edit (9 tests, directly mirroring the corresponding `ImportProfileEditorTests.cs` tests), plus column-level and point-column-level modify/prefill/save/cancel/delete (5 tests each) including one nested "edit column within an already-in-edit-mode sheet rule, then save the rule, then save the profile" persistence test per list — same nesting-depth coverage as the import side's block-field tests. All 38 tests in the file passed on the first run after implementation (no red-phase debugging needed) — the component split matched the design cleanly. Full `ExcelETL.BlazorAdmin.Tests` suite: 170/170 green (was 149); full solution (`ExcelETL.Domain.Tests`/`.Application.Tests`/`.Infrastructure.Tests`/`.WebAPI.Tests`/`.Hosting.Tests`/`legacy`) also green, 215+94+113+13+6+9 = unaffected outside BlazorAdmin.
  - **Not live-verified in the browser this session** — the Browser pane itself failed to composite frames at all (`screenshot failed: ... the Browser pane is not displayed`), a more fundamental failure than the navigation-only flakiness noted in the two prior incidents above. Confidence rests entirely on the bUnit suite and the full-solution build; live verification remains for a future session once the pane itself is displaying.
  - **Lot Q (visual + functional parity) complete** — `ExportProfileEditor.razor` now matches `ImportProfileEditor.razor`'s UI/UX conventions in full (labels, cards, monospace split rows, right-aligned actions, edit-in-place, delete confirmation). `ExportProfileTest.razor`/`ImportProfileTest.razor` remain untouched (out of scope, per both tickets).
- **Follow-up, same day: "Back to list" button on both profile editors.** `ImportProfileEditor.razor`/`ExportProfileEditor.razor` (both `/…/new` and `/…/{Id}/edit` routes) had no way back to `import-profiles`/`export-profiles` other than the browser's own back button — a plain `btn btn-secondary` (`back-to-import-profiles-button`/`back-to-export-profiles-button`) was added right after each page's `<h1>`, **outside** the `@if (_notFound)`/else split so it's visible in every state (normal form, the not-found alert, both new and edit). Two new page-scoped `.resx` keys (`ImportProfileEditor_BackToListButton`/`ExportProfileEditor_BackToListButton`, EN "Back to list"/FR "Retour à la liste") — same per-page-key convention as everywhere else in this file, not shared despite identical wording. Not governed by `convention-ui-blazor-alignement-boutons.md` (that convention is about action buttons operating on content — modify/delete/save/cancel — not top-of-page navigation), so left in the natural document flow rather than wrapped in `.right-aligned-actions`. 4 new tests (2 per editor: click navigates to the list route; button still renders on the not-found path) added to `ImportProfileEditorTests.cs`/`ExportProfileEditorTests.cs`. Full `ExcelETL.BlazorAdmin.Tests` suite: 174/174 green (was 170).
- **Lot R (densification de l'affichage des profils, done — 2026-07-23, ticket doc `docs/tickets-tdd-blazor-profils-densification-affichage.md`)**: client feedback on a 27" screen — the sheet-rule-card summary was too sparse (one card per row, ~2 of 6 sheets visible without scrolling). Pure presentation, no Domain/Application change, applied to `ImportProfileEditor.razor`/`ExportProfileEditor.razor` simultaneously per the ticket's own explicit parity requirement — confirmed first (per the ticket's own "check before choosing an approach" instruction) that `.sheet-rule-card`/`.block-field-list`/`.right-aligned-actions` all already live in the shared global `wwwroot/app.css` (Lot O/P's own doing), not duplicated per component, so R1/R2's CSS changes are each a single edit that applies to both editors identically with no risk of drift.
  - **R1**: `<ul class="sheet-rule-list">` gained a second class, `sheet-rule-grid` (`display: grid; grid-template-columns: repeat(auto-fill, minmax(480px, 1fr)); gap: 1.5rem;`, `app.css`) — kept as a class distinct from `.sheet-rule-list` itself (which stays just the list-reset rules) specifically so a dedicated selector exists for the ticket's own "assert on the class, not a visual render" test requirement. `.sheet-rule-editing-item` (the in-place edit form swapped in for a card) gained `grid-column: 1 / -1` so an open edit form isn't squeezed into a single ~480px column.
  - **R2**: the read-only summary's field/column list — `Locator.Fields` on the import side, the combined `ColumnDefinitions`+`PointColumnDefinitions` list on the export side — gained a second class, `block-field-grid` (`display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));`, border-bottom removed per item). **Deliberately scoped to only the read-only `.sheet-rule-card` summary**, per the ticket's own "à l'intérieur d'une .sheet-rule-card" wording — `SheetRuleForm.razor`/`SheetGenerationRuleForm.razor`'s own always-editable add/edit lists (which reuse the same `.block-field-list` class) were **not** given the grid class and keep their pre-existing one-per-row layout; those forms' own tests needed no changes.
  - **R3**: `UnconditionalColonneNames`+`PointRules` (import) / `ColumnDefinitions`+`PointColumnDefinitions` (export) are now collapsed behind a `<details class="sheet-rule-sublist-details"><summary>` per card, always closed by default, guarded by `if (either collection non-empty)`. **The full list markup is genuinely absent from the DOM while collapsed** (a component-state `HashSet<int> _expandedSheetRuleDetails` + `@if` around the content — not `<details>`'s own native open/closed CSS, which would leave the children in the DOM either way) — same "FindAll empty, not a display:none check" rule as L2/NavMenu, per the ticket's explicit test requirement. The `<summary>` binds `@onclick`+`@onclick:preventDefault="true"` to a `ToggleSheetRuleDetails(index)` toggle rather than letting the browser's native `<details>` open/close state drive anything, avoiding a fight between Blazor's diffing and the browser's own toggle. New resx keys `ImportProfileEditor_SheetRuleDetailsSummary` (`"{0} unconditional colonnes, {1} conditional rules"`) / `ExportProfileEditor_SheetRuleDetailsSummary` (`"{0} columns, {1} point columns"`) — deliberately different wording (there's no "unconditional/conditional" concept on the export side) but the same two-count `"{0} X, {1} Y"` shape, satisfying the ticket's "même structure de résumé" parity requirement without forcing identical vocabulary onto a different domain model. Both counts are read live off the rule's own collections on every render (no cached/first-render value), so the summary text updates immediately after an in-place edit adds/removes an item, before the profile itself is saved.
  - **~10 pre-existing tests updated** (not just added) across both files: every test that asserted unconditional-colonne/point-rule (import) or column/point-column (export) content was visible in the read-only summary immediately after adding/reloading a rule now first clicks `#sheet-rule-details-toggle-{index}` before asserting — this is an intentional behavior change (collapsed-by-default), not a regression being patched around. Tests scoped to the always-visible add/edit forms (`SheetRuleForm`/`SheetGenerationRuleForm`/`BlockFieldForm`/`ColumnDefinitionForm`/`PointColumnDefinitionForm`, and any test that clicks "Modify" to enter edit mode) needed no changes at all — R3 only touches the non-editing read-only summary.
  - **New dedicated cross-component parity test file, `tests/ExcelETL.BlazorAdmin.Tests/Pages/Admin/ProfileEditorParityTests.cs`** (not just parallel assertions duplicated in each editor's own test file) — per the ticket's explicit "test dédié... comparaison de chaîne" requirement: renders `ImportProfileEditor`+`ExportProfileEditor` side by side against persisted profiles with the same shape (2 sheet rules each) and asserts the two components' `ul.sheet-rule-list`/`ul.block-field-list` `class` attribute strings are `Should().Be(...)` each other exactly (not just "both non-empty"), plus a shared-behavior test that both editors' `.sheet-rule-sublist-details` start collapsed and expand identically on click. A second `BunitContext`-derived class was needed (rather than reusing either editor's own test class) since it registers **both** `IImportProfileStore`/`IExportProfileStore` against one shared `IDbContextFactory<ExcelEtlDbContext>` to render both editor types in the same test method.
  - Full `ExcelETL.BlazorAdmin.Tests` suite: 187/187 green (was 174) — 13 new tests (5 import-editor R1/R2/R3, 5 export-editor R1/R2/R3, 3 cross-component parity), 0 removed, ~10 rewritten in place. Verified live against the real dev server too (throwaway, never-saved profiles on both `/import-profiles/new` and `/export-profiles/new`, per the browser-preview-caution precedent — the shared seeded default profile was never touched): confirmed via `getComputedStyle`/DOM inspection (the Browser pane's screenshot capture failed outright this session, same class of infra flakiness noted in Lot Q) that both editors compute `display: grid` on `.sheet-rule-grid`, both apply `.block-field-grid` only inside the read-only summary, and both editors' R3 disclosure starts collapsed (`0` matching content divs) and reveals identical real content (`PROLOCK VANNES` / `Repère`+`EquipementRepere`) after a click round-trip completes.
- **Lot S (`NavMenu.razor` reorganization, profile-test links moved, brand rename, done — 2026-07-23, ticket doc `docs/tickets-tdd-blazor-navmenu-reorganisation-tests-marque-lot-s.md`)**: client feedback after a visual review of `/export-profiles`.
  - **S1**: `#nav-import-profiles-test-link`/`#nav-export-profiles-test-link` removed entirely from `NavMenu.razor` (routes `/import-profiles/test`/`/export-profiles/test` unchanged) — a per-sheet-test action doesn't belong in the top-level sidebar, it belongs on that profile type's own list page. `ImportProfiles.razor`/`ExportProfiles.razor` each gained a `#test-import-profile-button`/`#test-export-profile-button` next to their existing `create`-button, both wrapped in `.right-aligned-actions` (the existing shared global class from Lot P/Q's convention doc — `create-profile-button`/`create-export-profile-button` lost their standalone `mb-3` div and joined this wrapper). **Reused the pre-existing `NavMenu_ImportProfileTest`/`NavMenu_ExportProfileTest` resx keys verbatim** rather than adding new ones, per the ticket's own explicit instruction to check first — those two keys now serve double duty (still exist only once each, just referenced from a different Razor file).
  - **S2**: the admin block (`AuthorizeView Roles="Admin"`) reordered to Import → Export → Users (previously Users → Import → [Import-test] → Export → [Export-test]), and the generic Journaux `AuthorizeView` **moved from before to after** the admin block — deliberately kept as two separate `AuthorizeView`s, not merged, since Journaux is visible to any authenticated user (not just Admin) and merging would silently change who sees it. Final top-to-bottom order: Import profiles → Export profiles → Users → Logs → (username)/Profile → Logout, `<NotAuthorized>` branch unchanged (Register/Login).
  - **S3**: the hardcoded `navbar-brand` link text changed from `ExcelETL.BlazorAdmin` to `Alpha - MAD / REL OXO` — no resx key existed for it (confirmed by search before editing), so this stays a plain literal per the ticket's own fallback instruction.
  - **Orphaned icon CSS removed** (`NavMenu.razor.css`): `.bi-play-circle-nav-menu`/`.bi-download-nav-menu` had no remaining markup reference once S1 removed the two test links — dropped, same "remove the orphan, don't leave dead CSS" precedent as Lot M's `.bi-person-fill-nav-menu` removal. `.bi-file-earmark-spreadsheet-nav-menu` (still used by `#nav-export-profiles-link`) was kept.
  - Tests (`NavMenuTests.cs`): `AdminLinkIds` trimmed to the 3 remaining admin links (fixes the 2 pre-existing tests that iterated it without further changes, per the ticket's own efficiency note); added `NavMenu_WhenAuthorizedAsAdmin_DoesNotRenderProfileTestLinks` (markup absence, not just hidden), `NavMenu_RendersAdminAndLogsLinks_InExpectedOrder` (asserts relative `id="..."` string-index ordering across all 6 links, not text-based selection), and `NavMenu_RendersRenamedBrand`. `ImportProfilesTests.cs`/`ExportProfilesTests.cs` each gained a `TestProfileButton_NavigatesToTestRoute` test mirroring the existing `CreateButton_NavigatesToNewProfileRoute` pattern. Full `ExcelETL.BlazorAdmin.Tests` suite: 192/192 green (was 187); full solution (Domain 215/Application 94/Infrastructure 113/WebAPI 13/Hosting 6/legacy 9) unaffected outside BlazorAdmin.
  - **Not live-verified in the browser this session** — per this file's own "Utilisation du navigateur" guidance above, NavMenu link reordering and a plain text rename are exactly the kind of change to prefer build+bUnit over the browser for; confidence rests on the full green suite.
- **Ticket R3 (correctif): `sheet-rule-sublist-details` accordion reported "does nothing" when clicked in a real (Visual Studio-launched) browser, done — 2026-07-23, ticket doc `docs/ticket-r3-accordeon-sous-listes.md`**. Investigated by driving the real dev server (`dotnet run`, HTTPS Kestrel, real EF/LocalDB) through the Browser pane and dispatching genuine mouse clicks (not bUnit's direct-handler-invocation `.Click()`, which bypasses the browser's own default-action/event-dispatch pipeline entirely) against a throwaway duplicate of the seeded default import profile (`0ee23ac0-79e1-462c-9ae6-ccab7146914d`, named "Profil OXO standard (Copy)" — **left in the local LocalDB, not deleted**, since SQL `DELETE` was denied by the auto-mode classifier as a destructive action outside this session's write authority; harmless clutter, safe to remove by hand via SSMS/`sqlcmd` if desired). **Root cause was not a click-wiring defect**: the ISOLEMENT card's toggle opened and closed correctly on repeated real clicks, proving the `@onclick`/`@onclick:preventDefault` + `HashSet<int> _expandedSheetRuleDetails` mechanism (Lot R3) works as designed in an actual browser, not just under bUnit. The **actual, reproducible gap** was PROCEDURE-shaped sheet rules (`UnconditionalColonneNames.Count == 0 && PointRules.Count == 0`, true for the seeded profile's PROCEDURE rule): the outer `@if` guard omitted the whole `<details>` block for these, so there was **no element at all** to click — indistinguishable from "clicking does nothing" to a user who doesn't know that specific card has zero sub-items. This matched the ticket's own Étape 3 edge case ("une sous-liste vide... affiche un état vide cohérent") almost exactly, and is the most likely real trigger for the report.
  - **Fix, both `ImportProfileEditor.razor` and `ExportProfileEditor.razor` (kept in parity, per this project's established convention even though the ticket only named the import page)**: removed the outer `@if (collections non-empty)` guard around the `<details>` — it's now **always rendered**. When both collections are empty, the expanded content shows a new localized message instead of the unconditional-colonnes/point-rules lists (`ImportProfileEditor_NoSheetRuleSublistItems` / `ExportProfileEditor_NoSheetRuleSublistItems`, EN/FR). The pre-existing `Summary_WithNeitherCollectionPopulated_ShowsNeitherLabel` test (Lot O2) needed no change — it only asserts zero `<h5>` headings in the empty case, still true (the new empty-state text is a plain `<p>`, no heading).
  - **Accessibility fix, Refactor step's explicit ask**: the `<details>` element's `open` attribute is now bound to `_expandedSheetRuleDetails.Contains(index)` (`open="@_expandedSheetRuleDetails.Contains(index)"`, Blazor's compile-time-inferred boolean-attribute rendering — present when true, omitted when false). Previously `open` was never set by anything (the native toggle that would set it is blocked by `@onclick:preventDefault`, and the C# state only drove the separate `@if` around the content div) — sighted users saw correct behavior via the conditional content, but assistive technology relying on `<details>`'s native open/closed semantics had no signal at all.
  - **New tests** (`ImportProfileEditorTests.cs`/`ExportProfileEditorTests.cs`, mirrored): bidirectional toggle (click twice collapses again — previously untested, though the `HashSet.Add`/`Remove` toggle logic already made this trivially correct), two-card independence (expanding one card's disclosure leaves a second card's collapsed — previously untested, `HashSet<int>` membership already made this correct by construction), empty-sublist still renders a working toggle + shows the coherent empty-state message, and the `open` attribute reflects expanded/collapsed state. Full `ExcelETL.BlazorAdmin.Tests` suite: 202/202 green (was 192; +10, 5 per editor), 0 removed.
  - **`ProfileEditorParityTests.cs` needed no changes** — its R3 fixtures (`BuildImportProfileWithTwoSheetRules`/`BuildExportProfileWithTwoSheetRules`) both use non-empty sublists, so the always-render change doesn't affect their assertions.
- **Bug fix: `/import-profiles/test` and `/export-profiles/test` threw "Synchronous reads are not supported." on every real upload, done — 2026-07-23**. Root cause: `IBrowserFile.OpenReadStream()` returns a stream backed by the live Interactive Server circuit — it only supports the async `ReadAsync` overloads, since the upload streams in over SignalR rather than being a real seekable file. `new ClosedXmlWorkbookReader(stream)` hands that stream straight to `XLWorkbook`'s constructor, which opens the `.xlsx` as a zip package via synchronous `Stream.Read()` calls — throwing the instant it touches a real browser upload. **Never caught by bUnit**: `InputFileContent`'s test double is backed by a plain byte array and supports synchronous reads fine, so all existing `ImportProfileTestTests`/`ExportProfileTestTests` passed both before and after this fix — this class of bug is only reproducible against a real browser (or a hand-built fake stream that actually rejects sync reads, which is what the regression test below does).
  - **Fix**: new `src/ExcelETL.BlazorAdmin/Excel/BrowserFileStreamBuffering.cs` — `BufferToSeekableStreamAsync(Stream)` copies the browser stream into a `MemoryStream` via `CopyToAsync` (the one operation the browser stream does support) before it's ever handed to `ClosedXmlWorkbookReader`. Both `ImportProfileTest.razor` and `ExportProfileTest.razor`'s `OnInputFileChangeAsync` now buffer before constructing the reader. WebAPI's `OxoController` needed no change — `IFormFile.OpenReadStream()` is already fully buffered to memory/disk by multipart model binding before the controller runs, so it never hit this.
  - **Regression test**: `tests/ExcelETL.BlazorAdmin.Tests/Excel/BrowserFileStreamBufferingTests.cs` — a hand-rolled `AsyncOnlyReadStream` whose synchronous `Read`/`ReadByte` overloads throw `NotSupportedException("Synchronous reads are not supported.")` (mirroring the real browser stream), proving `BufferToSeekableStreamAsync` never touches them. Deliberately a unit test on the extracted helper, not a bUnit reproduction through the two pages — bUnit's `InputFile` test double can't be made to reject sync reads without bypassing bUnit's own upload helpers entirely, and the helper is where the actual fix lives. Full `ExcelETL.BlazorAdmin.Tests` suite: 203/203 green (was 202; +1).
- **Back-to-list button on `ImportProfileTest.razor`/`ExportProfileTest.razor`, done — 2026-07-23**: same pattern as the editors' own `back-to-import-profiles-button`/`back-to-export-profiles-button` (see the "Back to list" bullet above) — `#back-to-import-profiles-button` → `import-profiles`, `#back-to-export-profiles-button` → `export-profiles`, both via a newly-injected `NavigationManager` and a new page-scoped resx key each (`ImportProfileTest_BackToListButton`/`ExportProfileTest_BackToListButton`, EN "Back to list"/FR "Retour à la liste" — kept separate per-page even though the wording matches the editors', consistent with this file's established per-page-key convention). Reusing the same element id across the editor and test page for the same profile type is safe — they're different routes, never rendered together. Tests: `BackToListButton_NavigatesToImportProfileList`/`BackToListButton_NavigatesToExportProfileList` in `ImportProfileTestTests.cs`/`ExportProfileTestTests.cs` (the latter file gained a synchronous `WithCulture` helper alongside its pre-existing `WithCultureAsync`, mirroring the import side's file). Full `ExcelETL.BlazorAdmin.Tests` suite: 205/205 green (was 203; +2).
- **Lot T (`PivotSource.TacheMultiple`, dynamic per-code target-workbook sheets, done — 2026-07-23, ticket doc `docs/tickets-tdd-export-taches-multiples.md`)**: extends the export/generation side (Lot I/J) to cover `TacheMultiplePivot`, explicitly out of scope when Lot I/J were built. Implemented T1→T8 in the ticket's own recommended order, one commit per ticket (T8 added post-delivery, see below).
  - **T1/T2 (Domain)**: `PivotSource` gained `TacheMultiple`; `PivotFieldRef` gained 5 members (`TacheMultipleOrdre`/`Action`/`Acteur`/`Risques`/`DateValidation` — no `TypeTacheMultipleCode` member, since that field drives sheet *grouping*, not a column, and no `EstFactice` member, never displayed). `PivotFieldResolver` gained a third `Resolve(TacheMultiplePivot, PivotFieldRef)` overload (`Ordre`/`DateValidation` render via `CultureInfo.InvariantCulture`, `DateValidation` as `dd/MM/yyyy` matching `ProcedureExtractionService`'s own convention; both render `""` when null). `SheetGenerationRule`'s existing cross-validation (`GetPivotSource(column.Source) != pivotSource`) already rejected `Equipement`/`Isolement` columns under `PivotSource.TacheMultiple` with zero code changes — the check was already fully generic. Added one genuinely new invariant: any `PointColumnDefinition` under `PivotSource.TacheMultiple` throws a dedicated `DomainRuleViolationException` (`SheetGenerationRule_PointColumnDefinitionsNotAllowedForTacheMultiple`) — a TacheMultiple has no associated Point, structurally distinct from Equipement/Isolement. `SheetName` is documented (comment only, no behavior change) as reinterpreted to an admin-facing internal label for this `PivotSource` — the generated workbook never uses it as an actual sheet name here (see T3).
  - **T3/T4 (Application, `SheetGenerationEngine`/new `ExcelSheetNameSanitizer`)**: `Generate` changed from `.Select(rule => GenerateSheet(...))` to `.SelectMany(rule => GenerateSheets(...))` — `PivotSource.TacheMultiple` groups `ImportResult.TachesMultiples` by `TypeTacheMultipleCode` (strict string equality, no normalization — codes are clean internal constants), sorted alphabetically (`StringComparer.Ordinal`, deterministic, not `GroupBy`'s encounter order) into one physical `GeneratedSheet` per distinct code; rows keep `TachesMultiples`' own order within a group (LINQ `GroupBy` preserves source order per group), so an `EstFactice` row stays at its original position, never resorted by `Ordre` (which is null for factice rows). Zero `TachesMultiples` ⇒ zero TacheMultiple sheets (no empty-sheet placeholder, unlike the Equipement-null case). Equipement/Isolement rules are otherwise unaffected (`GenerateSheets` returns a 1-item list for them, delegating to the pre-existing `GenerateSheet`). New `ExcelSheetNameSanitizer.Sanitize` (`src/ExcelETL.Application/Generation/`) replaces Excel-forbidden characters (`\ / ? * [ ] :`) with `_` and truncates to 31 chars — applied to the raw `TypeTacheMultipleCode` before it becomes a `GeneratedSheet.Name`; defensive only, today's real codes (`TM_PROC_MAD`/`TM_PROC_REL`) need no sanitization, but a future client-added code isn't guaranteed Excel-safe.
  - **T5 (`DefaultProfileSeeder`)**: the seeded default export profile gained a third `SheetGenerationRule` ("Tâches multiples", `PivotSource.TacheMultiple`, 5 columns, no Point columns), now factored into a shared `BuildTacheMultipleSheetRule()` helper (T8 reuses it) — needs no Guid of its own (`SheetGenerationRule` has no identity property). **Verified against all 3 real fixtures which sheets actually produce `TacheMultiplePivot`s before writing T5's own test** (per the ticket's own efficiency note): all 3 do, every one via PROCEDURE's `TacheMultiple` block — C7401 98 total (`TM_PROC_MAD`=59/`TM_PROC_REL`=39), D8570 88 (`TM_PROC_MAD`=88 only, no REL), G6306B 24 (`TM_PROC_MAD`=11/`TM_PROC_REL`=13). `DefaultProfileSeederPipelineIntegrationTests`' pre-existing C7401 sheet-name assertion updated to expect `"Parents", "Enfants", "TM_PROC_MAD", "TM_PROC_REL"`; a new dedicated test asserts each dynamic sheet's row count matches `ImportResult.TachesMultiples`' own per-code group count.
  - **T6 (`ExportProfileEditor.razor`/`SheetGenerationRuleForm.razor`)**: pivot-source select gained a third `TacheMultiple` option (new resx keys `ExportProfileEditor_PivotSource_TacheMultiple`/`SheetNameHelp`/`SheetNameHelpTacheMultiple`). The column-source select's existing filter (`PivotFieldResolver.GetPivotSource(fieldRef) == PivotSource`) needed no change — it's already fully generic over `PivotFieldRef`. The `PointColumnDefinition` add/edit subform is hidden entirely (`@if (_pivotSource != PivotSource.TacheMultiple)`), not just disabled, matching T2's Domain invariant; switching to `TacheMultiple` also clears any already-added point columns via a new `@bind:after="OnPivotSourceChanged"` handler (avoids a hidden, confusing submit-time `DomainRuleViolationException` for state the user can no longer see) — switching back only restores the *ability* to add new ones, it does not resurrect cleared ones. `SheetName`'s label gained contextual help text explaining its reinterpreted role for this `PivotSource`.
  - **T7 (`ExportProfileTest.razor`)**: needed **zero** code changes — the preview already renders one HTML `<table>` per entry in `_generatedWorkbook.Sheets` via a plain `@foreach`, and T3 already made that collection dynamically include one sheet per distinct `TypeTacheMultipleCode`. New bUnit tests in `ExportProfileTestTests.cs` prove this end-to-end against the real C7401 fixture: a profile with a `TacheMultiple` rule renders both `#generated-sheet-TM_PROC_MAD-table` (59 body rows) and `#generated-sheet-TM_PROC_REL-table` (39 rows) with the expected headers; the same fixture through a profile *without* that rule renders no `table[id^='generated-sheet-TM_']` at all (non-regression, proving the UI only reflects what the engine actually generated, not what the source data contains).
  - **T8 (`DefaultProfileSeeder`, post-delivery correctif — real-environment gap discovered after T1-T7 shipped)**: `DefaultProfileSeeder` never modifies an already-existing profile (by design, so an admin customization is never overwritten) — which meant an export profile seeded *before* T5 landed (i.e. from Lot M, before the "Tâches multiples" rule existed) would never receive it, even after the code shipped: the nominal seeding path (`SeedExportProfileAsync`) is a no-op the moment a profile exists under `ExportProfileId`. Confirmed in the real environment: the rule was genuinely missing from the already-seeded default profile, and `/export-profiles`/`/export-profiles/test` showed no `TM_PROC_*` sheets despite T1-T7 being deployed and green. **Fix**: a new, narrow, additive-only migration, `MigrateTacheMultipleSheetRuleIfMissingAsync` — runs only when the export profile already exists *and* has no `SheetGenerationRule` with `PivotSource.TacheMultiple`; appends exactly `BuildTacheMultipleSheetRule()` (the same T5 definition, now shared) to the existing `SheetRules`, leaving `Parents`/`Enfants` — and any admin customization already made to them — completely untouched. **Deliberately not a general reseed**: the check is the simplest possible ("rule absent ⇒ add"), with no dedicated migration marker — an admin who later deliberately removes the rule will see it reappear on the next restart under this rule, a known limitation flagged in the ticket itself, to be revisited with the user only if it proves to be a real problem in practice (not pre-emptively solved here). Tests (`DefaultProfileSeederTests`, +4): migration adds the rule once to a profile lacking it; `Parents`/`Enfants` (including an admin rename and a trimmed Point-column set) survive structurally unchanged; a profile that already has the rule is left completely alone (byte-for-byte `BeEquivalentTo` before/after a second `SeedAsync` call); repeated `SeedAsync` calls after migration never produce more than one `TacheMultiple` rule. Note: owned-collection (`SheetRules`) order isn't guaranteed by the EF Core InMemory provider across a delete+reinsert round trip (no explicit ordering column configured) — these new tests assert rule *membership*, not sequence, unlike T5's own single-fresh-insert test which happens to get a stable order for free.
  - Full suite after Lot T: Domain 234 (was 215), Application 109 (was 94), Infrastructure 118 (was 113), WebAPI 13 (unaffected), BlazorAdmin 217 (was 205), Hosting 6 (unaffected) — all green.
  - **Hors périmètre, per the ticket**: no exposure of `DataTacheMultiple`/`ItemTacheMultiple` (legacy `AvancementRecette` fields, not part of the pivot model at all); no configurable sort/pagination of generated sheets; the dynamic per-code grouping mechanic stays deliberately specific to `PivotSource.TacheMultiple`, not generalized into a reusable primitive for `Equipement`/`Isolement`; no dedicated migration marker for T8 (see above).
- **`ExportProfileTest.razor` collapsible sheet tables + readable wide-column wrapping, done — 2026-07-23**: client feedback after Lot T shipped dynamic `TM_PROC_*` tables.
  - **Collapsible sections**: each entry in `_generatedWorkbook.Sheets` (`Parents`/`Enfants`/every dynamic `TM_PROC_*`) is now wrapped in a `<details class="test-table-details">`/`<summary id="generated-sheet-{sheet.Name}-details-toggle">`, mirroring `ImportProfileTest.razor`'s own `equipement-details-toggle`/`test-table-scroll` mechanic exactly (content genuinely removed from the DOM while collapsed, not just hidden, via a `HashSet<string> _collapsedSections` keyed by sheet name, same idiom as the import page's `_collapsedSections`). CSS added to `ExportProfileTest.razor.css` (component-scoped, so duplicated verbatim from `ImportProfileTest.razor.css` rather than shared — Blazor CSS isolation means a shared class name across two components needs the rule in each component's own `.razor.css`, or moved to global `app.css` as Lot O/P did for other shared classes; not done here since only these two pages need it and they're not otherwise CSS-coupled). 4 new tests in `ExportProfileTestTests.cs` mirror the import page's own toggle tests.
  - **Page-width containment bug fixed**: `MainLayout.razor.css`'s `main { flex: 1; }` had no `min-width: 0` — a flex item's default `min-width: auto` means it never shrinks below its content's intrinsic width, so a wide nowrap table was inflating the whole page past the viewport instead of staying inside `.generated-sheet-scroll`'s own `overflow: auto`. Fixed once, globally, in `MainLayout.razor.css` (`main { min-width: 0; }`) — applies to every page, not just this one; not duplicated per-page.
  - **Readable wide-column wrapping**: `.generated-sheet-table th/td` changed from `white-space: nowrap` to `white-space: normal` + `max-width: 320px` + `overflow-wrap: break-word` + `vertical-align: top` — a long-text column (e.g. `TM_PROC_MAD`'s "Action") now wraps within a capped width instead of forcing single-line horizontal overflow, while short columns (`Ordre`, `Date de validation`) stay naturally compact under `table-layout: auto`'s default column-sizing (deliberately not `table-layout: fixed`, which would force equal-width columns regardless of content — worse reading for a profile-driven, variable column count). Applies uniformly to every sheet rendered by this page (`Parents`/`Enfants`/`TM_PROC_*`), since they all share the one `.generated-sheet-table` class/markup template.
  - **New living convention doc, `docs/convention-ui-blazor-tableaux-generes-lisibilite.md`** (same "define once, reference from future tickets" pattern as `convention-ui-blazor-alignement-boutons.md`, Lot P): documents the content-cell rule (wrap + capped `max-width` + `table-layout: auto`, not `fixed`) and the page-container rule (`main { min-width: 0; }`, fixed once in `MainLayout.razor.css`, never per-page) for any future BlazorAdmin table whose column count/content depends on a profile or an imported file, not a fixed known schema. Notes `ImportProfileTest.razor`'s `.test-table` never needed this treatment — it never set `nowrap` in the first place, so the browser's default wrapping already applied; not an oversight, just a problem that never arose there.
  - Purely CSS/markup-toggle change, no new resx keys, no Domain/Application impact. Not independently live-browser-verified by this session (the user's own dev server holds port 7013, no admin credentials available to this session) — confirmed working directly by the user in their own browser session for both the collapsible toggles and the column wrapping.
- **`ExportProfileTest.razor` sheet-summary item counts, done — 2026-07-23**: each collapsible sheet summary now shows an item count next to the sheet name (`Parents (1)`, `Enfants (23)`, `TM_PROC_MAD (59)`, ...), new resx key `ExportProfileTest_SheetNameWithCount` (`"{0} ({1})"`, identical EN/FR — punctuation only, no translatable prose).
  - **`GeneratedSheet` carries no pivot-source metadata** (just `Name`/`Headers`/`Rows`) — resolved instead via a new `_generatedExportProfile` field (the `ExportProfile` actually used for the current generation, set alongside `_generatedWorkbook`, reset to `null` everywhere `_generatedWorkbook` is reset). `GetGeneratedSheetItemCount(GeneratedSheet)` looks up the `SheetGenerationRule` whose `SheetName` matches (`PivotSource.Equipement`/`Isolement` rules always produce exactly one physically-named sheet, per Lot T3/T4) — Equipement present ⇒ 1, else `_importResult.Isolements.Count`. Any sheet with no matching rule is, by elimination, a `PivotSource.TacheMultiple` dynamic sheet (Lot T), matched back to its `TypeTacheMultipleCode` group via the exact same `ExcelSheetNameSanitizer.Sanitize` call the engine used to name it.
  - **Excludes "ligne de mise en page" (factice) rows from the TacheMultiple count**, per explicit client instruction: only `TacheMultiplePivot`s with a non-null `Ordre` are counted (`t.Ordre.HasValue`) — a header/placeholder row within a TacheMultiple block has a blank `Ordre` by definition (model doc §1.2, `TacheMultiplePivot.EstFactice`'s underlying signal) but still physically occupies a row in the generated sheet, so `sheet.Rows.Count` alone would over-count.
  - Test (`ExportProfileTestTests.cs`, `GeneratedSheetSummaries_DisplayItemCountsNextToSheetName_ExcludingFacticeTacheMultipleRows`): computes expected counts independently by running the real `IImportPipelineOrchestrator` against the real C7401 fixture inside the test itself, rather than hardcoding fixture-specific numbers — includes an explicit sanity assertion that C7401 genuinely has at least one blank-`Ordre` row, so the exclusion is actually exercised and not vacuously true. Full `ExcelETL.BlazorAdmin.Tests` suite: 223/223 green (was 222).
- **Lot U (Tableaux/Applications pivot fields + export profile enrichment, done — 2026-07-24, ticket doc `docs/tickets-tdd-pivot-tableaux-applications-export.md`)**: implemented U1→U6 sequentially, one commit per ticket.
  - **U1**: `ImportProfile` gains `DefaultTableaux`/`DefaultApplicationNames` (`IReadOnlyList<string>`, required non-null, empty allowed, no magic default — same anti-hardcoding rule as `EquipementTypeElementNom`). Added as new required constructor parameters (not an `init`-property-with-default) to stay consistent with this codebase's established no-default-for-collections convention — every `new ImportProfile(...)` call site across Domain/Application/Infrastructure/BlazorAdmin/tests updated mechanically. `ImportProfileEditor.razor` gained minimal add-only list UI for both fields (`default-tableau-name-input`/`add-default-tableau-button`, `default-application-name-input`/`add-default-application-name-button`), mirroring `SheetRuleForm`'s existing `UnconditionalColonneNames` add-list pattern.
  - **U2**: `EquipementPivot` gains `Tableaux`/`Applications`; `IsolementPivot` gains `Tableaux`/`Applications`/`RepereParent` — all `init` properties defaulting to empty (`""`/`[]`), broadcast later via `with` expressions, **exactly the same mechanism as `Localisation`/loc1** (not positional constructor params, despite the ticket's own illustrative pseudocode showing them that way — matching established codebase convention preserves every pre-existing 3-arg/5-arg call site). Both records gained explicit `Equals`/`GetHashCode` overrides (`SequenceEqual` on the new list properties) since default record equality on `IReadOnlyList<string>` is reference equality, same reason as `RepeatingBlockLocator`/`Concat`.
  - **U3**: `ProcedureExtractionService.Extract` gained a `defaultTableaux` parameter — the 2 unconditional PROCEDURE Points are now built from that list (`defaultTableaux.Select(name => new PointPivot(name, repere))`) instead of the 2 retired `private const string` fields (`TravauxCompletColonneName`/`TravauxDetailColonneName`), same guard-rail test pattern as the pre-existing `EquipementTypeElementNom` one. `ImportPipelineOrchestrator` broadcasts `profile.DefaultTableaux`/`profile.DefaultApplicationNames` onto the Equipement and every Isolement via `with` (same call site/principle as the existing loc1 broadcast), and additionally copies `EquipementPivot.Repere` onto every `IsolementPivot.RepereParent`. Non-regression verified against all 3 real fixtures with `DefaultTableaux = ["TRAVAUX COMPLET", "TRAVAUX DETAIL"]` reproducing prior behavior exactly, plus `RepereParent` asserted equal to the Equipement's own `Repere` on every extracted Isolement.
  - **U4**: new `ApplicationColumnDefinition` record (`ApplicationNom`/`Header`/`MarkValue`, `DefaultMarkValue = "X"`), modeled on `PointColumnDefinition` but structurally distinct (an Application isn't a `Point`/`Colonne`). `SheetGenerationRule` gains `ApplicationColumnDefinitions`, cross-validated at construction time exactly like `PointColumnDefinitions`: a **new, separate** `DomainErrorCode` (`SheetGenerationRule_ApplicationColumnDefinitionsNotAllowedForTacheMultiple`, not a reuse of the Point one — kept precise/independently localizable, consistent with this project's one-code-per-distinct-failure convention) rejects any `ApplicationColumnDefinition` under `PivotSource.TacheMultiple`; a second new code (`SheetGenerationRule_DuplicateApplicationNom`) rejects a repeated `ApplicationNom` within one rule; the existing duplicate-`Header` check now also considers Application column headers. **Also lands, ahead of U5's own scope, since it's pure Domain plumbing that U5 needs**: `PivotFieldRef` gains `EquipementTableaux`/`IsolementTableaux`/`IsolementRepereParent`, and `PivotFieldResolver.Resolve` renders Tableaux as `string.Join(", ", pivot.Tableaux)` — reusing the existing `ColumnDefinition`/`PivotFieldResolver` rendering path entirely, so the "Tableaux" column needed **zero** `SheetGenerationEngine` changes (only the Application columns did, see U5). EF: new `OwnsMany` mapping for `ApplicationColumnDefinitions` (table `ExportProfileSheetRuleApplicationColumnDefinitions`, same shadow-`int Id` convention as `PointColumnDefinitions`); one combined migration `20260724005133_AddTableauxApplicationsToProfiles` carries both this table and U1's 2 new `ImportProfiles` columns (not yet applied to any real SQL Server database as of this lot). Blazor: new `ApplicationColumnDefinitionForm.razor` (mirrors `PointColumnDefinitionForm.razor`, Blazor-only default `MarkValue = "O"` — distinct from the Domain type's own `"X"` default, matching the one real seeded Application) wired into `SheetGenerationRuleForm.razor`'s always-present "Add a sheet rule" card, hidden for `PivotSource.TacheMultiple` (same treatment as the Point subform, including clearing already-added Application columns on switch-to-TacheMultiple).
  - **U5**: `SheetGenerationEngine`'s header/cell order becomes descriptive columns → **Application columns** (new) → Point columns. A cell is `MarkValue` when the row's pivot `Applications` list contains `ApplicationNom` (trimmed, case-insensitive — same §7 convention as `TypeElement`/`Colonne.Nom` comparisons elsewhere in the pipeline), else empty; applies identically to `GenerateEquipementRows`/`GenerateIsolementRows`. `GenerateTacheMultipleSheets` untouched (Domain already guarantees `ApplicationColumnDefinitions` is always empty there).
  - **U6**: seeded default import profile: `DefaultTableaux = ["TRAVAUX COMPLET", "TRAVAUX DETAIL"]`, `DefaultApplicationNames = ["PROGRESS"]` (`ProgressApplicationName` const) — reproduces the retired PROCEDURE hardcode exactly, non-regression guaranteed by U3's own tests. Seeded default export profile, both sheets: **"Parents"** gains a "Tableaux" `ColumnDefinition` (`EquipementTableaux`, right after "Désignation") and a "PROGRESS" `ApplicationColumnDefinition` (right after "Tableaux", `MarkValue = "O"`), both before the unchanged Point columns. **"Enfants"**: its pre-existing "Type" column is **renamed** to "Type Elément" (same source field, `IsolementTypeElementNom` — decision #5 confirmed no new pivot field was needed, so this is a header-text change only, not a duplicate column), gains a new "ELEMENT PARENT" column (`IsolementRepereParent`, between "Zone" and "Désignation") plus the same "Tableaux"/"PROGRESS" pair as Parents (positioned after "Position à la pose", before the unchanged 17 Point columns). Tests: `DefaultProfileSeederTests` content assertions updated for the new column lists (`ColumnDefinitions.Select(c => c.Source)` per sheet, `ApplicationColumnDefinitions` presence/values), plus a new cross-profile consistency test (`SeedAsync_ImportAndExportProfiles_ShareConsistentDefaultTableauxAndApplicationNames`) and a rewritten `SeedAsync_ExportParentsPointColumns_MatchImportProfilesDefaultTableaux` (now a genuine cross-profile comparison against `importProfile.DefaultTableaux`, not a literal-vs-literal one, since U3 removed the hardcode this test used to pin). `DefaultProfileSeederPipelineIntegrationTests` gained `Generate_C7401Fixture_WithSeededProfiles_ProducesExpectedHeaderOrderAndNewColumnValues` — full header-order assertion for both sheets against the real seeded-and-refetched profile, plus a per-row check that every one of C7401's 23 Enfants rows shares the same broadcast "ELEMENT PARENT"/"Tableaux"/"PROGRESS" values (`"38-C7401"`/`"TRAVAUX COMPLET, TRAVAUX DETAIL"`/`"O"`). Full suite after Lot U: Domain 264 (was 240), Application 121 (was 109), Infrastructure 121 (was 119), BlazorAdmin 226 (was 223) — all green.
  - **Hors périmètre, per the ticket**: ETIQUETTE/Isolement TexteLibre column (parked, needs a real-need clarification first); migration of an already-seeded environment predating this lot (`DefaultProfileSeeder` never touches an existing profile — a T8-style narrow migration would be needed only if such an environment is confirmed to exist, not pre-emptively written here); any `ExportProfileEditor.razor`/`ImportProfileEditor.razor` ergonomics beyond the minimal fields needed to enter the new data (reordering, visual grouping) — explicitly deferred by the ticket.
- **Lot V (mobile-first Blazor pass, done — 2026-07-24, ticket doc `docs/tickets-tdd-blazor-mobile-first-lot-v.md`)**: a merge of two parallel "Lot U"-named documents (collision with the real Lot U above, renumbered V1–V13), following a client mobile screenshot review. Implemented in the doc's own recommended order, one commit per ticket. Every ticket touches only `ExcelETL.BlazorAdmin` (+ its tests) — no Domain/Application/Infrastructure change anywhere in this lot.
  - **V1 (bug fix)**: the sheet-rules-count `<th>` column header on `ImportProfiles.razor`/`ExportProfiles.razor` reused the templated `*_SheetCount` resx value (`"{0} sheet rule(s)"`) with no count argument, literally rendering `"{0} sheet rule(s)"` as the header text — the data cell itself was already interpolating correctly. Fixed via a new plain-text `*_SheetCountHeader` key per list.
  - **V2/V4 (table→card + button stacking)**: `ImportProfiles.razor`/`ExportProfiles.razor`/`Users.razor` (new `UsersTests.cs`, none existed before) render both a `<table class="d-none d-md-table">` and a parallel `<div class="d-md-none">` card-per-row container from the same already-fetched data — no duplicated `GetAllAsync()` call. `Logs.razor` deliberately keeps its plain table (explicit client decision, guarded by a regression test). The two profile-list header buttons (Test/Create) share a `d-grid gap-2 d-md-flex` wrapper (standard Bootstrap stacked-to-inline idiom — CSS Grid's default item-stretch gives full width below `md` with zero extra width utility, flex returns natural width from `md` up).
  - **V3 (icon-only row actions)**: Modify/Duplicate buttons (both table and card) become icon-only inline SVGs (`.block-field-icon-btn` reused, pencil-square/copy shapes — no `bi bi-*` font classes anywhere in this project, see `NavMenu.razor.css`) with explicit `aria-label`/`title` per the new `docs/convention-ui-blazor-icones-boutons.md`.
  - **V5 (`Logs.razor`)**: Message column truncates to 50 chars + `…` with a `<details>/<summary>` disclosure (`#view-log-details-button-{id}`, same collapsed-by-default idiom as `ImportProfileEditor.razor`'s R3) revealing the full text in `<pre><code>`; untruncated messages render in full with no link. Filters gain `form-select-lg`/`form-control-lg`.
  - **V6 (`Profile.razor`)**: both submit buttons gain `w-100 btn-lg`; the Security section's row container gains `mt-5`.
  - **V7 (investigation, Partie B)**: confirmed actual back-button ids (`back-to-{import|export}-profiles-button`, unrelated to the editors' own same-named-pattern buttons), `<InputFile>` structure, Bootstrap 5.3.3 (so `bg-success-subtle` etc. are available), and that both intro strings are one sentence each (so V9's bullet-list fallback never applies).
  - **V10 (upload styling)**: `<InputFile>` gains `form-control-lg` and sits inside an `input-group` with a file-upload icon — never hidden/overlaid, per the ticket's own explicit rejection of a custom dropzone. The critical regression check (`FindComponent<InputFile>()` + `UploadFiles` still drive the pipeline) is every pre-existing upload test passing unmodified.
  - **V8 (back link)**: moved into a new global `.page-banner`/`.page-banner-back-link` (`app.css`, shared by both pages) as an icon-only `bi-arrow-left` button (label visible only `d-md-inline`) — same id/navigation as before. Icon-only is deliberate despite `convention-ui-blazor-icones-boutons.md`'s "no icon for secondary actions" rule: that rule targets content-operating buttons (Cancel/Close), not page-to-page navigation, a distinct category.
  - **V9**: `text-muted small` on both intro paragraphs, text unchanged (single-sentence, no bullet-list conversion needed per V7's finding).
  - **V11**: `form-select-lg` on every profile `<select>`; `btn-lg` on `generate-workbook-button`/`download-generated-workbook-link`.
  - **V12**: `generate-workbook-button` (the one genuine primary action button in this lot — `ImportProfileTest.razor` has none, upload triggers processing automatically) gains `w-100 w-md-auto`. Bootstrap ships no responsive width utility, so `.w-md-auto` is one small custom rule in `app.css` at the same 768px breakpoint V2 established.
  - **V13**: the success-result container on both pages (`#test-status` on import; the generated-workbook block on export) becomes `card shadow-sm bg-success-subtle` + `card-body` instead of a plain `alert-success` — transient Processing/Error alerts are untouched, only the actual result/summary area changes.
  - Full `ExcelETL.BlazorAdmin.Tests` suite after Lot V: 258/258 green (was 223 before this lot).
- **Lot W (`ImportProfileEditor`/`SheetRuleForm.razor`, edit/delete of `UnconditionalColonneNames`/`ConditionalPointRule`, done — 2026-07-24, ticket doc `docs/tickets-tdd-blazor-profil-import-edition-listes-conditionnelles.md`)**: closes the last add-only gap in the sheet-rule sub-form — both sub-lists were, until this lot, append-only with no way to fix or remove an already-added entry short of recreating the whole profile. Investigation (Étape 0) confirmed the gap lived entirely in `SheetRuleForm.razor` (not the read-only `ImportProfileEditor.razor` summary, which R3's accordion already covers and which this lot doesn't touch) and that `BlockFieldDefinition`'s existing indexed-edit-state pattern (Lot O1/`ticket-profil-import-edition.md`) was the model to reuse verbatim.
  - **W1/W2 (`UnconditionalColonneNames`)**: each entry renders as a `.block-field-item` (name only, no `.block-field-range` line — a plain string has nothing else to show) with `#edit-unconditional-colonne-button-{prefix}{index}`/`#delete-unconditional-colonne-button-{prefix}{index}` icon buttons (pencil/trash, same inline-SVG shapes as `BlockFieldDefinition`'s). Edit mode swaps the row for a single text input (`#unconditional-colonne-edit-input-{index}`) plus `#save-unconditional-colonne-button-{index}`/`#cancel-unconditional-colonne-edit-button-{index}` (new check/x inline SVGs — the first icon-only Save/Cancel pair in the codebase; every prior Save/Cancel was a labeled button, per `convention-ui-blazor-icones-boutons.md`'s "CRUD action → icon" rule extended to this compact inline-row context). Empty-after-edit is rejected with a new localized message (`ImportProfileEditor_EmptyColonneNameError`) — unlike `ConditionalPointRule`, a bare `string` has no Domain constructor to throw for this, so the check is plain component-level validation, mirroring `AddUnconditionalColonneName`'s existing silent-no-op guard but now surfaced as a visible error since edit-mode must stay open on failure. State is `_editingUnconditionalColonneIndex`/`_editingUnconditionalColonneValue`/`_unconditionalColonneEditError`, indexed per component instance (same isolation guarantee as `_editingFieldIndex` — each sheet-rule card's `SheetRuleForm` is its own component instance).
  - **W3/W4 (`ConditionalPointRule`)**: identical shape, 4 fields (`ColonneName`/`SourceFieldName`/`Operator` select/`ComparisonValue`) via `#conditional-point-rule-edit-{colonne-name|source-field|comparison-value}-input-{index}`/`#conditional-point-rule-edit-operator-select-{index}`, `#save-conditional-point-rule-button-{index}`/`#cancel-conditional-point-rule-edit-button-{index}`. Save reconstructs a fresh `ConditionalPointRule` via its real Domain constructor inside a `try`/`catch (DomainValidationException)` — the constructor already rejects all 3 non-enum-empty cases (`SourceFieldName`/`ComparisonValue`/`ColonneName`), so no new validation logic or resx keys were needed here, only the existing `BusinessExceptionLocalizer` path already used by `AddPointRule`.
  - Both deletes are immediate, no confirmation modal — consistent with the project's already-accepted no-confirmation-on-delete pattern for sheet rules/block fields (see the incident note above this bullet in the changelog).
  - Tests (`ImportProfileEditorTests.cs`, +26): prefill-on-modify, save-updates-in-place, empty-value/empty-field rejection with edit mode staying open, cancel-restores-original, per-item state isolation within the same list, delete-removes-without-index-shift, delete-last-item-leaves-empty-list, and 4 full persistence round-trips (edit/delete for both sub-lists, entering edit mode via `#modify-sheet-rule-button-0` → `#edit-0-...` ids, then `#save-sheet-rule-button-0` → `#save-profile-button` → reload from `IImportProfileStore`). All pre-existing Add-flow tests for both sub-lists pass unmodified. Full `ExcelETL.BlazorAdmin.Tests` suite: 278/278 green (was 258).
  - **Hors périmètre, per the ticket**: the accordion mechanism itself (R3, untouched); delete confirmation (deliberately not introduced here alone); `ExportProfileEditor.razor` (no `UnconditionalColonneNames`/`ConditionalPointRule` equivalent on the export side); any Domain model change (`ConditionalPointRule`/`SheetExtractionRule` reconstructed as-is via their existing constructors).
- **Lot Y (post-mobile-review corrections, done — 2026-07-24, ticket doc `docs/tickets-tdd-blazor-polish-ux-lot-y.md`)**: 4 fixes from a second round of client mobile screenshots, following the mandatory Y0 investigation (both X6 and X11 were confirmed already merged before touching anything).
  - **Y1 (`NavMenu.razor`/`NavMenu.razor.css`)**: on mobile, the brand text "Alpha - MAD / REL OXO" visually collided with the hamburger toggler. Y0 found the toggler is a **sibling of `.top-row`, not a flex child of it** — it stays absolutely positioned (`top:0.5rem; right:1rem; width:3.5rem`) specifically so `.navbar-toggler:checked ~ .nav-scrollable`'s CSS-only sibling selector keeps working; moving it into the flex row would break that mechanism. Fix: `.container-fluid` gained `d-flex align-items-center` plus a scoped `padding-right: 4.5rem` (reserving the toggler's overlay space so `.navbar-brand`'s new `text-truncate flex-grow-1` classes stop short of it instead of running underneath); `.navbar-brand` gained `min-width: 0` in the scoped CSS (required for `text-truncate` to take effect inside a flex child at all — a flex item's default `min-width:auto` otherwise blocks shrinking). `PageBackNavLink.razor`'s button and the toggler `<input>` both gained the Bootstrap `flex-shrink-0` utility class directly in markup (not a CSS property — matches the ticket's literal ask and gives bUnit something to assert on). No brand-text shortening (explicitly out of scope, an editorial decision).
  - **Y2 (`ColonneNom`/other export-side `form-floating` fields)**: Y0 found every field already respects `form-floating`'s two hard requirements (input before label in the DOM, non-empty `placeholder`) — X4 was already fully and correctly merged, confirmed field-by-field across `ColumnDefinitionForm`/`PointColumnDefinitionForm`/`ApplicationColumnDefinitionForm`/`SheetGenerationRuleForm`/the root name input. **No production change** — added regression-guard tests only (`ExportProfileEditorTests.cs`: DOM-order + non-empty-placeholder for `point-column-nom-input` plus 9 sibling fields via `[Theory]`, and a binding non-regression test), so a future change reintroducing the label-before-input/empty-placeholder violation the client screenshot showed would be caught.
  - **Y3 (`#save-export-profile-button`)**: gained `w-100 w-md-auto btn-lg mt-4 mb-4` — reused V12's exact `w-md-auto` custom breakpoint class (`app.css`, 768px) rather than inventing a new one; X5's outline-button treatment (intermediate Add buttons) is untouched, this button stays `btn-primary`, never `btn-outline-*`.
  - **Y4 (`app.css`, `.sheet-rule-grid`)**: the real cause of the reported left/right text truncation on `/export-profiles/{id}/edit` mobile was **not** a missing container (X6 was already merged) but Lot R's `.sheet-rule-grid { grid-template-columns: repeat(auto-fill, minmax(480px, 1fr)); }` — CSS Grid never shrinks a track below its minmax floor, so on any mobile viewport (< 480px) the grid forced page-wide horizontal overflow, and the browser rendered the already-scrolled-right page, truncating text on both edges ("odifier"/"arents"/"Suppr..."). Fixed with one `@media (max-width: 767.98px) { .sheet-rule-grid { grid-template-columns: 1fr; } }` rule at the same breakpoint Y3/V2 already use. Since `.sheet-rule-grid` is shared global CSS (Lot R), `ImportProfileEditor.razor` benefits too as an incidental correct side effect of fixing a genuinely broken shared rule — not a scope-creep feature port, so it doesn't conflict with this lot's "don't extend to import without validation" exclusion (that exclusion targets new features/polish, not shared-CSS bug fixes).
  - Tests: `NavMenuTests.cs` (+4: brand truncation/flex-grow classes, toggler flex-shrink-0, real top-row structure guard-rail — back-link+brand inside `.container-fluid`, toggler outside `.top-row` — and a `SectionContent`-active variant proving the back-link also gets `flex-shrink-0` when rendered), `ExportProfileEditorTests.cs` (+14: Y2 structure/binding, Y3 classes + non-regression save/navigate, Y4 grid class + long-name-stays-in-DOM non-regression). Full `ExcelETL.BlazorAdmin.Tests` suite: 312/312 green (was 278); full solution build green.
  - **Hors périmètre, per the ticket**: `ImportProfileEditor.razor` (beyond the incidental shared-CSS fix above); reopening X10/X11's `SectionOutlet`/`SectionContent` mechanism itself; brand text shortening; any pipeline/business-logic change.
  - **Follow-up, same day (client request): Y3 extended to `ImportProfileEditor.razor`**. `#save-profile-button` (same component/id on both `/import-profiles/new` and `/import-profiles/edit` routes) gained the identical `w-100 w-md-auto btn-lg mt-4 mb-4` classes. Tests added in `ImportProfileEditorTests.cs`: `SaveProfileButton_IsFullWidthLargeCta_WithVerticalMargins_OnNewRoute`/`..._OnEditRoute` (the latter rendering with `Id` set via `BuildProfileWithOneSheetRule()`, mirroring the file's existing edit-route test pattern). Full `ExcelETL.BlazorAdmin.Tests` suite: 314/314 green (was 312).
- **Lot 027 (unicité du nom des profils d'import/export, done — 2026-07-24, ticket doc `docs/tickets-tdd-lot-027-unicite-nom-profils.md`)**: triggered by a client screenshot showing 3 profiles named identically `"Profil OXO standard (Copie)"` from the Duplicate button's un-guarded suffixing (Lot J1/F1). Implemented 27.0→27.4 in the ticket's own recommended order, one commit per ticket.
  - **27.0 (Domain, both `ImportProfile`/`ExportProfile`)**: `MaxNameLength = 60` constant on each type; the one reconstructing constructor that actually validates (`ImportProfile`'s `Guid`-taking overload, `ExportProfile`'s only constructor) now also checks `name.Trim().Length <= 60`, throwing `DomainValidationException` with 2 new `DomainErrorCode` members (`ImportProfile_NameTooLong`/`ExportProfile_NameTooLong`) — same mechanism as the pre-existing empty-name check, `MaxNameLength` passed as `Args[0]` for the `{0}`-templated resx message. `ImportProfileConfiguration`/`ExportProfileConfiguration`'s `Name` column changed from a hardcoded `HasMaxLength(200)` to `HasMaxLength(ImportProfile.MaxNameLength)`/`HasMaxLength(ExportProfile.MaxNameLength)`. Blazor: `maxlength="@ImportProfile.MaxNameLength"`/`maxlength="@ExportProfile.MaxNameLength"` added to `#profile-name-input`/`#export-profile-name-input` (defense-in-depth only — the Domain constructor stays the real source of truth, proven by a dedicated bUnit test that bypasses the HTML attribute via `.Change()` with a 61-character string).
  - **27.1 (`ProfileNameAlreadyExistsException`)**: lives in `ExcelETL.Application.Exceptions` (not a `Extraction/Exceptions` subfolder as the ticket's own example literally suggested) — deliberately a **single shared class** reused by both `EfImportProfileStore`/`EfExportProfileStore`, not two mirrored per-type exceptions like `ImportProfileNotFoundException`/`ExportProfileNotFoundException`: the failure mode and message shape ("a profile named 'X' already exists") are identical either way, so a second near-duplicate class would add nothing. New `ApplicationErrorCode.ProfileNameAlreadyExists` member + `ApplicationMessages.resx`/`.fr.resx` entries — picked up automatically by the existing `BusinessExceptionLocalizer` dispatch (`IHasApplicationErrorCode` branch), no new localizer mechanism needed.
  - **27.2 (`EfImportProfileStore`/`EfExportProfileStore.SaveAsync`)**: before any write, loads just the `Name` column of every *other* profile (`Where(p => p.Id != profile.Id)`, own Id excluded so a no-op rename during edit never self-collides) and compares in-memory via `Trim()` + `OrdinalIgnoreCase` — deliberately not translated into a server-side EF query, since the InMemory provider's LINQ translation of `.Trim().ToUpper()` is unreliable and the profile count is small. Throws `ProfileNameAlreadyExistsException` before the existing delete-then-insert upsert sequence runs. `HasIndex(p => p.Name).IsUnique()` added to both `IEntityTypeConfiguration`s as the defense-in-depth DB-level safety net (relies on the target SQL Server's default case-insensitive collation, e.g. `SQL_Latin1_General_CP1_CI_AS` — no explicit `.UseCollation(...)` set, documented inline rather than forced, since every environment this project targets uses that default). **One combined migration**, `20260724115715_AddProfileNameUniqueIndexAndMaxLength` (per the ticket's explicit "one migration, not two" instruction) — carries both the `Name` column's `nvarchar(200)` → `nvarchar(60)` `AlterColumn` (EF's own scaffolder flagged this as a potential-data-loss operation, expected and accepted per the ticket's "test data only, DB gets dropped and recreated" note) and the two `CreateIndex(... unique: true)` calls.
  - **27.3 (`ImportProfileEditor.razor`/`ExportProfileEditor.razor`)**: `SaveProfileAsync` gained a second `catch (ProfileNameAlreadyExistsException ex)` block alongside the pre-existing `catch (DomainValidationException ex)`, both calling `BusinessExceptionLocalizer.TryLocalize(ex) ?? ex.Message` identically — no navigation on failure, form stays filled as typed (matches the pre-existing Domain-error catch behavior exactly, just a second exception type routed through the same one display mechanism). **Deviated from the ticket's literal ask for 2 new page-scoped resx keys** (`ImportProfileEditor_DuplicateName`/`ExportProfileEditor_DuplicateName`): since `ProfileNameAlreadyExistsException` already implements `IHasApplicationErrorCode` and is fully localized via 27.1's `ApplicationMessages` resx entry, any such page-level key would never actually be read by the code path the ticket itself describes — added would just be dead resources, inconsistent with this project's own precedent of pruning (not accumulating) unused resx entries (see Lot K4). Flagged here rather than silently added.
  - **27.4 (`ImportProfiles.razor`/`ExportProfiles.razor`, Duplicate button)**: `BuildAvailableDuplicateName(profileName)` first strips an already-present `"(Copy[ N])"`-shaped suffix off the *clicked* profile's own name via one `Regex` (built from the localized `*_DuplicateSuffix` resx value, e.g. `"(Copy)"` → inner word `"Copy"`) — this is what makes clicking Duplicate on either the original **or** an existing copy converge on the same next-available `"{BaseName} (Copy N)"`, per the ticket's own worked example, rather than ever nesting `"(Copy) (Copy)"`. Then increments `attempt` from 1 (`"(Copy)"`) upward (`"(Copy 2)"`, `"(Copy 3)"`, ...) checking each candidate against the **already-loaded** `_profiles` list (`Trim()` + `OrdinalIgnoreCase`, same normalization as the Store) — no extra `GetAllAsync()` round trip, per the ticket's explicit efficiency note. Makes 27.2/27.3 unobservable in the normal duplicate flow (the submitted name is always already free) without bypassing the Store's own check, which stays the real safety net for the Create/Modify flows.
  - Full solution after Lot 027: Domain 270 (was 264), Application 123 (was 121), Infrastructure 131 (was 121), BlazorAdmin 328 (was 314), WebAPI 13/Hosting 6/legacy 9+15 (unaffected) — all green.
  - **Hors périmètre, per the ticket**: no Domain-constructor-level uniqueness check (depends on Store state, not a construction invariant); no auto-rename-suggestion on Create/Modify collisions (user picks a new name manually after the error, only Duplicate gets auto-increment); no data migration to deduplicate/truncate any already-existing test-only profiles (the real dev database gets dropped and recreated from migrations + `DefaultProfileSeeder` instead); `DefaultProfileSeeder`'s own seeded names are untouched (unique by construction, idempotent by `Guid` not by name).
- **Lot 028 (suppression de profils d'import/export avec confirmation, done — 2026-07-24, ticket doc `docs/tickets-tdd-lot-028-suppression-profils-import-export.md`)**: triggered by a client screenshot of `ExportProfiles.razor` showing no way to remove a profile, then explicitly extended by the client to `ImportProfiles.razor` for parity. Implemented 28.0→28.4 in the ticket's own recommended order.
  - **28.0/28.1**: investigation confirmed `EfImportProfileStore.DeleteAsync`/`EfExportProfileStore.DeleteAsync` already existed (Lot E2/I6 generation) and were already tested (existing-profile removal + idempotent unknown-id) — no new store method needed, exactly as the ticket predicted. The one real gap: neither store's existing delete test asserted `GetAllAsync` also stops containing the deleted profile (only `GetByIdAsync` was checked) — added that one assertion to both `EfImportProfileStoreTests`/`EfExportProfileStoreTests`' existing `DeleteAsync_WithExistingProfile_RemovesItAndItsNestedRows` tests, per the ticket's own "fill the gap, don't duplicate a test" instruction.
  - **28.2/28.3 (`ImportProfiles.razor`/`ExportProfiles.razor`, symmetric)**: a new icon-only `#delete-profile-button-{id}`/`#delete-export-profile-button-{id}` (+ `-card-{id}` mobile variants) sits alongside the existing Edit/Duplicate buttons in both the table row and mobile card. **Deviated from the ticket's literal test-markup example** (`<span class="bi bi-trash" aria-hidden="true">`): this project has never loaded the Bootstrap Icons font (see `NavMenu.razor.css`'s own comment) — every row icon on these two pages (`PencilIconMarkup`/`CopyIconMarkup`) is already an inline `<svg>` constant, so the new `TrashIconMarkup` follows that established pattern instead of the ticket's font-class example, consistent with 28.0's own instruction to reuse the pencil/copy pattern "à l'identique". Clicking it does not call `DeleteAsync` directly — it sets `_profileIdPendingDeletion`/`_profileNamePendingDeletion` (component state, no JS interop, matching Lot V Partie B's established principle), which conditionally swaps that row's Edit/Duplicate/Delete buttons for an inline confirmation (`#delete-profile-confirm-{id}` / `#delete-export-profile-confirm-{id}`, each with `-card-{id}` and non-card variants so the always-both-rendered table+card templates never produce a duplicate id) showing the target profile's name plus `btn-danger` Confirm (`#confirm-delete-profile-button-{id}`) and plain `btn-secondary` Cancel (`#cancel-delete-profile-button-{id}`, no icon per `convention-ui-blazor-icones-boutons.md`'s secondary-action rule). Confirm calls `DeleteAsync` then reloads via `LoadProfilesAsync()`; Cancel just clears the pending state. A single nullable `Guid?` field naturally enforces "only one confirmation open at a time" — opening a second row's confirmation overwrites the field, closing the first with no delete ever attempted for it.
  - **28.4 (resx)**: per-page keys (`ImportProfiles_Delete`/`ConfirmDeleteMessage`/`ConfirmDeleteButton`/`CancelDeleteButton`, mirrored as `ExportProfiles_*`), EN/FR. **Deviated from the ticket's suggestion to consider a shared generic Confirm/Cancel key pair between the two pages**: checked first — no such generic (non-page-prefixed) key exists anywhere in `BlazorAdminMessages.resx` today; every prior Confirm/Cancel pair (`ImportProfileEditor_ConfirmDeleteButton`/`CancelButton`, `ExportProfileEditor_*`) is page-scoped, explicitly kept separate even when the English wording coincides (documented repeatedly, e.g. Lot Q/S1) — introducing a new cross-page "Common_*" prefix here would be a bigger, unrequested naming-convention change for two extra resx entries. Followed the established per-page-key convention instead. `ImportProfiles_Delete`/`ExportProfiles_Delete` are templated (`"Delete profile {0}"`) rather than plain like `_Edit`/`_Duplicate` — a deliberate enhancement over the existing Edit/Duplicate aria-labels, per the ticket's own worked example, since a destructive action benefits from naming its target in the label.
  - **28.5**: no code — decision already acted with the client (documented in the ticket itself) that a seeded default profile deleted via these buttons re-appears on the next `BlazorAdmin` restart (`DefaultProfileSeeder` only checks its own stable `Guid` constants), accepted as-is, not to be reopened without a new explicit request.
  - Tests: 5 new bUnit facts per page (button opens confirmation without calling `DeleteAsync` — `Mock<IImportProfileStore>`/`Mock<IExportProfileStore>` `Times.Never`; confirmation shows the target name; Cancel closes it with `Times.Never` and leaves Edit/Duplicate working; Confirm calls `DeleteAsync` with the exact id and the reloaded list drops the profile; opening a second row's confirmation silently closes the first without deleting it), plus `delete-profile-button`/`delete-export-profile-button` folded into the pre-existing icon-only-row-actions parity test. Full `ExcelETL.BlazorAdmin.Tests` suite: 338/338 green (was 328); `ExcelETL.Infrastructure.Tests`: 131/131 green (unchanged count, existing tests extended in place).
  - **Hors périmètre, per the ticket**: bulk delete/select-all; protection of the seeded default profiles (28.5, decision already acted, not to reopen); undo/soft-delete (deletion stays permanent, consistent with the rest of the project).
- **Lot 029 (renommage de la base de données SQL Server par défaut, done — 2026-07-24, ticket doc `docs/tickets-tdd-lot-029-renommage-base-donnees.md`)**: pure configuration change, no schema/Domain/migration impact — the default database catalog name becomes `AM-OXO-ETL-MAD-REL` (was `ExcelEtl`).
  - **029.0 investigation**: one repo-wide search for `ExcelEtl` found 4 `appsettings*.json` connection strings (`ExcelETL.WebAPI`/`ExcelETL.BlazorAdmin`, both `appsettings.json` and `appsettings.Development.json`) plus **2 design-time factories with their own independently hardcoded connection string**, distinct from any `appsettings.json` — `ExcelEtlDbContextFactory`/`ApplicationIdentityDbContextFactory` (`src/ExcelETL.Infrastructure/Persistence/`, `.../Identity/`), both `IDesignTimeDbContextFactory<T>` implementations used only by `dotnet ef migrations add`. These are exactly the trap the ticket's own 029.0 called out — missing them would mean future migrations get scaffolded against a `ExcelEtl` database that no host actually points at anymore. Everything else matching `ExcelEtl` was either a class name (`ExcelEtlDbContext`, never renamed — a bigger, unrequested rename out of this ticket's scope), an InMemory-provider test factory (`TestDbContextFactory`/`TestExcelEtlDbContextFactory`, no real catalog name involved), an unrelated literal (`FileStorage:ArchiveDirectory`, the legacy client's `ExcelEtlApi:*` config-key prefix), or historical narrative in this file's own past-lot bullets and dated `docs/etat-avancement-*.md` snapshots (left untouched, frozen-in-time records per this file's own living-vs-frozen convention). No SQL scripts reference it.
  - **029.1**: the migrations-history table names (`__EFMigrationsHistory_ExcelEtl`/`__EFMigrationsHistory_Identity`) are explicitly **not** renamed, per the ticket's own out-of-scope note — they're derived from the `DbContext` class name, not the physical database name, and renaming them wasn't confirmed. The retired `ExcelEtl` LocalDB database itself is abandoned, not migrated/dropped by this ticket (Lot G4's auto-migrate-on-startup creates `AM-OXO-ETL-MAD-REL` fresh on the next real host startup) — consistent with this being a dev-environment-only change.
  - Tests: `ConnectionStringConfigurationTests` (new, one per host — `tests/ExcelETL.WebAPI.Tests/Configuration/`, `tests/ExcelETL.BlazorAdmin.Tests/Configuration/`) load each host's own `appsettings.json`/`appsettings.Development.json` via a real `ConfigurationBuilder.AddJsonFile(...)` (located via a repository-root walk-up to `ExcelETL.slnx`, same walk-up-to-a-marker idiom as the existing `Fixtures`-directory helpers) and assert `ConnectionStrings:DefaultConnection` contains the new name and no longer contains the old one — scoped to the connection string value specifically, not a whole-file text search, so `FileStorage:ArchiveDirectory`'s unrelated `ExcelEtl`-containing value never produces a false failure. `ExcelEtlDbContextFactoryTests`/`ApplicationIdentityDbContextFactoryTests` (new, `tests/ExcelETL.Infrastructure.Tests/Persistence/`, `.../Identity/`) call `CreateDbContext([])` and assert the resulting context's `Database.GetConnectionString()` the same way. Full solution after Lot 029: Domain 270/Application 123/Infrastructure 133 (was 131, +2)/WebAPI 15 (was 13, +2)/BlazorAdmin 340 (was 338, +2)/Hosting 6/legacy 9+15 — all green; no existing test needed changes (every prior integration test already runs against EF Core InMemory, never the real SQL Server catalog name).
- **Lot 030 (parité visuelle Import/Export, correctif `form-floating` généralisé, boutons globaux sur une ligne, done — 2026-07-24, ticket doc `docs/tickets-tdd-lot-030-parite-import-export-form-floating-boutons.md`)**: triggered by a client screenshot comparison rating `/export-profiles/{id}/edit` correct and `/import-profiles/{id}/edit` poor — explicitly lifts the X3-X5/Y2/Y3 "import stays out of scope until client validates parity" exclusion recorded in Lots X/Y above.
  - **Part A (`ImportProfileEditor.razor`/`SheetRuleForm.razor`/`BlockFieldForm.razor`)**: root fields (Name/ReperePrefix/EquipementTypeElementNom), the Tableaux/Applications add-fields, `SheetRuleForm`'s locator fields (SheetName/FirstBlockStartRow/Step/StopFieldName), and `BlockFieldForm`'s two fields all converted from a `row g-2`/label-then-input grid to stacked `form-floating` divs (`input`/`select` before `<label>`), matching `ExportProfileEditor.razor`'s *actual* markup read directly in 30.0 rather than the ticket's own "col-12" wording (Export never uses `col-12` — it's a plain `<div class="mb-3">`, no `row`/`col-*` at all, since a block-level div outside a `.row` is already full-width). Tableaux/Applications, and `SheetRuleForm`'s three sub-lists (Champs du bloc/Unconditional Colonnes/Point Rules — previously not card-wrapped at all), each now sit inside their own `<div class="card bg-light mb-3">` (X4 pattern). Every intermediate "Add" button (`add-default-tableau-button`/`add-default-application-name-button`/`add-block-field-button`/`add-unconditional-colonne-button`/`add-point-rule-button`/`add-sheet-rule-button`, plus `SheetRuleForm`'s own Submit/Cancel which double as "Save changes"/"Cancel" in edit mode) became `btn btn-outline-secondary w-100 mt-3` (X5's exact export-side color, not a new choice) — `save-profile-button` (already `btn-primary w-100 w-md-auto btn-lg mt-4 mb-4` since a same-day Lot Y follow-up) needed no change. The Unconditional-Colonne/Point-Rule *inline edit-in-place* rows (Lot W's compact single-row UI, no equivalent on the export side) were deliberately left untouched — out of this ticket's literal field list and a different, already-accepted UX pattern.
  - **Part A test additions** (`ImportProfileEditorTests.cs`, +18): parameterized structural checks (`form-floating` wrapper, container is a bare `mb-3` with no `row`/`col-*`) for every converted root/sub-form field, `bg-light` card presence for all 3 `SheetRuleForm` sub-lists plus Tableaux/Applications, and outline/full-width class checks for every intermediate Add button vs. the still-`btn-primary` save button. **30.5 explicit cross-editor parity** (`ProfileEditorParityTests.cs`, +4): string-equality comparison (not "both non-empty") of the root-field-container class, the `card bg-light` subform-container class, an intermediate Add button's class, and the final Save button's class between `ImportProfileEditor`/`ExportProfileEditor` — all four are character-for-character identical (`"mb-3"` / `"card bg-light mb-3"` / `"btn btn-outline-secondary w-100 mt-3"` / `"btn btn-primary w-100 w-md-auto btn-lg mt-4 mb-4"`).
  - **Part B (30.6, `wwwroot/app.css`) — actual root cause found, not Y2's DOM-order/placeholder fix blindly reapplied**: the reported "label overlapping already-typed text/placeholder" on `/export-profiles`' "Nom de la colonne" field survived a check against the ticket's own literal fault example (DOM order and non-empty placeholder were *already* both correct there) — so 30.0's mandated CSS inspection went further and found `app.css` (present since the project's initial scaffold, boilerplate from the default Blazor Identity template) overriding Bootstrap's own `.form-floating > .form-control::placeholder { color: transparent; }` with `color: var(--bs-secondary-color)` (visible) + `text-align: end`/`start` on focus. Bootstrap's design deliberately keeps the placeholder invisible — the label *is* the only visible text at rest, so making the placeholder visible only matters when its text differs from the label's; every `*Placeholder` resx key in this project (including `Login_UsernamePlaceholder`/`Account_PasswordPlaceholder`, differing from their `*Label` only by casing) was already established to hold the *same* text as its label (documented repeatedly across past lots), so the override was providing zero value anywhere while creating a literal doubled-text render for every empty `form-floating` field with same-case label/placeholder text (like "Nom de la colonne"). **Fixed by deleting the two override rules outright** (not patched per-field) — restores Bootstrap's default transparent-placeholder behavior everywhere at once.
  - **Widened audit found and fixed a second, independent gap**: `Profile.razor`'s 6 `form-floating` fields (FirstName/LastName/Email/CurrentPassword/NewPassword/ConfirmNewPassword) had **no `placeholder` attribute at all** (a `:not(:placeholder-shown)` selector never matches without one, so the label was always rendered pre-floated regardless of value — a lesser, purely cosmetic deviation from the ideal float animation, not the reported overlap bug, but still a violation of form-floating's stated technical requirement). Fixed by adding `placeholder` attributes (`Account_EmailPlaceholder` reused for Email; 5 new resx keys — `Profile_FirstNamePlaceholder`/`LastNamePlaceholder`/`CurrentPasswordPlaceholder`/`NewPasswordPlaceholder`/`ConfirmNewPasswordPlaceholder`, EN+FR, each holding the same text as its `*Label` counterpart per the established convention).
  - **Part B tests** (`tests/ExcelETL.BlazorAdmin.Tests/Pages/Admin/FormFloatingStructureAuditTests.cs`, new file, +7 across 5 test classes): a shared `FormFloatingStructureAssertions.AssertAllFormFloatingFieldsAreStructurallyValid<TComponent>(IRenderedComponent<TComponent>)` helper generically enumerates every `div.form-floating` on a rendered page and asserts (a) an `input`/`select` precedes its `label` in the DOM, (b) an `input` (not `select`, which is always floated and has no `:placeholder-shown` mechanics) has a non-empty `placeholder` — a parameterized/generic guard-rail per the ticket's own preference over one hand-picked field, so a future field added anywhere in the audited pages can't silently reintroduce the violation. Applied to `ImportProfileEditor`/`ExportProfileEditor` (both the `/new`-equivalent initial state and, separately, after entering sheet-rule + nested-field/column edit mode, to cover the `edit-{index}-`-prefixed markup too), `Profile.razor`, `Login.razor`, `Register.razor` — the full `grep form-floating` scope identified in 30.0.
  - **Part C (30.7, `ImportProfiles.razor`/`ExportProfiles.razor`) — explicit reopening of V4/X2's mobile-stacking decision, not a bug fix**: the header "Tester un profil"/"Créer" buttons' shared wrapper changed from `d-grid gap-2 d-md-flex` (full-width-stacked below `md`) to a plain `d-flex gap-2` (single row at every breakpoint, including mobile); each button dropped its individual `w-100`/`mb-2` in favor of `flex-fill` so the two share the row's width equally rather than reverting to tiny natural-width buttons. A button's label wrapping to two lines *inside* the button at narrow widths is accepted as different from the original bug (two buttons on two lines). Symmetric on both pages. `ImportProfilesTests.cs`/`ExportProfilesTests.cs`: the 2 pre-existing V4/X2 tests per page (`HeaderActionButtons_AreIndividuallyFullWidth`, `*_HeaderButtons_ShareResponsiveStackingWrapper`) were rewritten in place (not left alongside outdated duplicates) to assert the new classes/absence of the old ones — this is a deliberate, client-directed behavior change, not a regression being patched around. New `ProfileListPageParityTests.cs` (+2): string-equality comparison of the wrapper's and each button's class between the two list pages.
  - Full `ExcelETL.BlazorAdmin.Tests` suite after Lot 030: 371/371 green (was 340) — +18 (`ImportProfileEditorTests`), +7 (`FormFloatingStructureAuditTests`, new file), +4 (`ProfileEditorParityTests`), +2 (`ProfileListPageParityTests`); `ImportProfilesTests`/`ExportProfilesTests` had 2 tests each rewritten in place (no net count change from those). Full solution build green. Verified live in the dev server (Login page, no admin credentials available to this session — see the pre-existing "Browser Preview Caution" precedent): `getComputedStyle(input, '::placeholder').color` now resolves to fully transparent (`rgba(0, 0, 0, 0)`), confirming the app.css fix actually took effect at runtime, not just in bUnit's DOM-only checks.
  - **Hors périmètre, per the ticket**: `SectionOutlet`/`SectionContent` banner mechanism (X10/X11, untouched); `.sheet-rule-card` desktop grid densification (Lot R, untouched); the custom-upload-component request (X11/V10, reopened-and-reconfirmed-deferred yet again); target-workbook format / `TacheMultiples` decisions (unrelated); any Domain/Application business-logic change (this lot is strictly BlazorAdmin CSS/Razor structure).
- **Lot 031 (element counters on `ImportProfileTest.razor`'s collapsible section titles, done — 2026-07-24, ticket doc `docs/tickets-tdd-lot-031-compteurs-import-profile-test.md`)**: closes a UI/UX parity gap with `ExportProfileTest.razor`, which already shows a count (e.g. `Parents (1)`, `TM_PROC_MAD (51)`) next to each collapsible sheet's title (delivered 2026-07-23, commit `aa8fdb6`) — the import side's equivalent sections (Equipement/Isolements/Points/Taches multiples/Non-blocking warnings) had no such count.
  - **31.0 investigation confirmed the export-side mechanism is *not* a shared component/method** — `ExportProfileTest.razor`'s `GetGeneratedSheetItemCount(GeneratedSheet)` is a private page-local method (needed there only because a dynamic `GeneratedSheet` doesn't carry its own pivot-source metadata, so the count has to be resolved by matching the sheet back to its `ExportProfile.SheetRules`/`TypeTacheMultipleCode`), and the format itself lives in resx key `ExportProfileTest_SheetNameWithCount` = `"{0} ({1})"` (identical EN/FR, punctuation only), interpolating a **raw sheet name** (data) as `{0}`. Import's 5 section titles are, by contrast, already-localized **static labels** (`ImportProfileTest_EquipementHeading`="Equipement", etc.) with their collections directly at hand (`_result.Isolements`/`.Points`/`.TachesMultiples`/`.Errors`, no cross-referencing needed) — so this lot doesn't reuse or introduce any shared component; each of the 5 existing resx heading values gained its own `{0}` count placeholder instead (e.g. `"Isolements ({0})"`/`"Isolements ({0})"` FR), interpolated at the call site as `@Loc["ImportProfileTest_IsolementsHeading", _result.Isolements.Count]` — same visual format (`"Name (N)"`) as the export side, achieved by a different, simpler mechanism appropriate to static vs. dynamic titles. No non-regression test on `ExportProfileTest.razor` was needed as a result (nothing there was touched or reused).
  - **`Equipement`**: `ImportResult.Equipement` is a single nullable `EquipementPivot`, not a collection — no `.Count` exists. Investigation confirmed this section only ever renders inside the `_result.Equipement is not null` branch (the null/whole-file-rejection case swaps in the "File rejected" alert instead, per model doc §3.1) — so in practice the count is always `1`; implemented as `_result.Equipement is null ? 0 : 1` (documented inline) rather than hardcoding `1`, per the ticket's own instruction not to silently assume.
  - **`Non-blocking warnings`**: counts `_result.Errors.Count`, consistent with the section's pre-existing `Errors.Count > 0` display guard — no structural divergence from the other 4 sections was found, so it got the same treatment (no ad hoc rewrite needed, per 31.0's own contingency).
  - No section added/removed; no Domain/Application/`ExportProfileTest.razor` change at all — purely the 5 resx values + their 5 call-site interpolations in `ImportProfileTest.razor`.
  - Tests (`ImportProfileTestTests.cs`, +2, one pre-existing test's assertions extended in place per the ticket's explicit instruction not to duplicate an existing D8570 test): a new `RunOrchestratorDirectly(profile, fixtureFileName)` helper runs the real `IImportPipelineOrchestrator` directly against a fixture inside the test (not a hardcoded magic number) to compute the expected count, then asserts the rendered title's text contains `"SectionName (N)"` for that same N — `Run_C7401Fixture_SectionTitles_ShowActualElementCounts_FromTheRealPipelineRun` (Equipement/Points/Taches multiples/Isolements) and `Run_D8570Fixture_WarningsSectionTitle_ShowsActualNonBlockingErrorCount` (Warnings); `Run_D8570Fixture_ShowsVanneAsNonBlockingWarning_NotAsFileRejection` gained one additional assertion (`Isolements (67)`) alongside its existing unmodified assertions. Full `ExcelETL.BlazorAdmin.Tests` suite: 373/373 green (was 371); full solution build green.
- **Lot 032 (détection d'incohérence de TYPE dans les tâches multiples PROCEDURE, done — 2026-07-24, ticket doc `docs/tickets-tdd-lot-032-detection-incoherence-type-procedure.md`)**: a data-entry-quality guard-rail, not a business rule or a functional evolution — triggered by a real anomaly Simon spotted in `Dossier_de_MaD_IDL__C7401.xlsx` (PROCEDURE, factice section "10-MISE EN SERVICE DU COMPRESSEUR", tasks 49-88): 3 consecutive TYPE runs (REL 49-72, MAD 73-78, REL 79-88) where the middle run's `Action` wording template matches its REL neighbor, strongly suggesting a copy-paste data-entry error on the client's source file rather than real business intent.
  - **32.0 investigation found no existing section-grouping mechanism** — `ProcedureExtractionService.ReadTachesMultiples` produces a flat `List<TacheMultiplePivot>` with no notion of "section"; a factice (`EstFactice`) row's `Action` is the section title, and the real (non-factice) tasks that follow it up to the next factice row are that section's members — confirmed against all 3 real fixtures via a throwaway ClosedXML dump before writing any code, including the edge case (G6306B) where the sheet's very first task has no preceding factice header at all (section title `""`). Also found `ExtractionErrorLogging.Log`'s Warning/Error routing was a 2-way ternary (`UnrecognizedTypeElement` → Warning, everything else → Error) that would have silently mis-routed the new code to `Error` (blocking-looking) had it not been made explicit — fixed to a 3-way check.
  - **New pure, ClosedXML-free files** in `src/ExcelETL.Application/Extraction/Oxo/Procedure/` (per the ticket's own "note d'efficacité" #1, most coverage lives here, not in fixture-based tests): `TacheMultipleSectionGrouper.GroupBySection` (the discrete grouping step above, `TacheMultipleSection(Title, Tasks)`); `TacheMultipleTypeCoherenceAnalyzer.Analyze` — computes contiguous TYPE runs (`ComputeRuns`, Trim+case-insensitive comparison per spec §7), determines the majority type by *total task count* (not longest individual run, decision 5), and returns either `MinorityRunAnomalies` (each tagged `Sandwich`/`DebutDeSection`/`FinDeSection` by its position among *all* runs, not just minority ones — decision 1, both bord cases carry the same weight as sandwich, decision 7) when a clear majority exists, or `AmbiguousGroups` (one per tied type, decision 6 — no minority/majority decomposition when 2+ types share the top count) when it doesn't. No length threshold (decision 2: every run break is reported).
  - **New `ExtractionErrorCode.TypeIncoherenceDansTacheMultiple` member** (decision 4) — non-blocking (decision 8: `TacheMultiplePivot`s are always extracted normally regardless), logged at `Warning` alongside `UnrecognizedTypeElement`.
  - **Cabled directly in `ProcedureExtractionService`** (decision 3, not an `ImportProfile`-configurable rule, consistent with the pre-existing Ordre/ligne-factice rule on this same sheet): `Extract` now also calls `DetectTypeIncoherences`, which groups `tachesMultiples` into sections and runs the analyzer per section, building one `ExtractionError` per minority-run anomaly (`BlockIdentifier` = `"{titre section} (tâches {N1}-{N2})"`) or exactly one `ExtractionError` for an ambiguous section (`BlockIdentifier` = the section title alone, no task range — decision 6/7) via a French-only message (no i18n — this pipeline's `ExtractionError.Message` values have never gone through `.resx`, consistent with the pre-existing `UnrecognizedTypeElement`/`RequiredFieldMissing` messages in the other 4 sheet services). The résultat is appended to `ImportResult.Errors`, same channel as every other non-blocking warning.
  - **Égalité-stricte message wording confirmed live with Simon during 32.0** (the one open item the ticket flagged as blocking) — used verbatim as proposed in the ticket.
  - Tests: `TacheMultipleSectionGrouperTests`/`TacheMultipleTypeCoherenceAnalyzerTests` (new files, Application.Tests, +19) cover every 32.1/32.2 scenario purely (single run, C7401's exact 24/6/10 shape, minority run at start/end, two distinct minority runs, strict tie incl. a 3-way tie beyond the ticket's own 2-way example, trim/case-insensitive comparison, sandwich/début/fin classification, plus `ComputeRuns` directly). `ProcedureExtractionServiceTests` (+5, `Mock<IWorkbookReader>`, minimal synthetic sections) cover the 32.3 wiring/message-construction end-to-end for sandwich/début/fin/égalité-stricte/homogeneous-no-warning. `ProcedureExtractionServiceIntegrationTests` (Infrastructure.Tests, +3): C7401 asserts the exact single `ExtractionError` (code, `BlockIdentifier`, full `Message` text) plus non-regression that tasks 73-78 are still extracted normally; D8570/G6306B (`[Theory]`) assert **zero** `TypeIncoherenceDansTacheMultiple` errors — the ticket's own explicit false-positive guard-rail, since these 2 fixtures' sections are all homogeneous. Full suite after Lot 032: Domain 270 (unchanged)/Application 147 (was 123, +24)/Infrastructure 136 (was 133, +3)/BlazorAdmin 373 (unchanged)/WebAPI 15/Hosting 6/legacy 9+15 (unaffected) — all green.
  - **Hors périmètre, per the ticket**: no textual-similarity heuristic on `Action` wording (structural TYPE-column detection only); no `ImportProfile` configurability; no blocking of import/export; no auto-correction of the detected TYPE; no other sheet (`TM_PROC_MAD`/`TM_PROC_REL` only exist on PROCEDURE); no new persistence channel (reuses `ImportResult.Errors` exclusively).
- **Lot 033 (multi-file upload on `ImportProfileTest.razor`/`ExportProfileTest.razor`, done — 2026-07-25, ticket doc `docs/tickets-tdd-lot-033-upload-multiple-fichiers-pages-test.md`)**: client is about to supply ~10 more real source files to validate against; testing them one at a time was tedious. Both pages' `<InputFile>` gained `multiple`, processing the whole selection sequentially (`foreach`, no parallelization, per the ticket's own YAGNI/thread-safety rationale) through the unchanged pipeline.
  - **33.0 investigation confirmed the ticket's own flagged trap was already handled**: both pages already pass `MaxFileSizeBytes` explicitly to `IBrowserFile.OpenReadStream(...)` (fixed during the earlier `BrowserFileStreamBuffering` bug fix), so the Blazor default ~500 KB cap was never a live risk here — no code change needed for that specific piège, just confirmation before writing tests, per the ticket's own instruction.
  - **New shared types, `src/ExcelETL.BlazorAdmin/Excel/`**: `BatchUploadLimits` (`MaxFilesPerBatch = 20`, `MaxFileSizeBytes = 10 MB`, `MaxTotalBatchSizeBytes` **derived** as `MaxFilesPerBatch * MaxFileSizeBytes`, per the ticket's explicit "no independently-maintained total" requirement) — the two base constants exist nowhere else in the repo (WebAPI's `UploadLimits.MaxExcelFileSizeBytes`, already referenced by both pages' pre-existing single-file cap comment, is a different, unrelated 10 MB constant for the HTTP endpoint, not reused here since it has no batch/count concept). `BatchFileStatus` enum (`Ok`/`Warning`/`Rejected`/`TechnicalError`) — the 4th status is new this lot: an unhandled exception while reading/processing one file (corrupt/non-Excel content) is now isolated to that file (caught, not rethrown) rather than aborting the whole batch or being indistinguishable from a business `Rejected` (`Equipement is null`). `BatchFileValidator` — pure, no localization (callers build the actual message via their own `IStringLocalizer`), split into `ValidateCount(int)` and `ValidateSizes(IReadOnlyList<IBrowserFile>)` specifically because `InputFileChangeEventArgs.GetMultipleFiles(maxAllowedFiles)` itself throws if the real selection exceeds `maxAllowedFiles` — the count has to be checked against `e.FileCount` *before* a page can safely call `GetMultipleFiles(BatchUploadLimits.MaxFilesPerBatch)` to even obtain the list `ValidateSizes` needs. `FormatMegabytes(long)` (invariant-culture, up to 2 decimals) rounds out the shared helper.
  - **33.1 (batch validation, both pages identically)**: order is count → individual size → total size → process, exactly per the ticket. All 3 rejection messages are separate `.resx` keys (`*_BatchTooManyFilesError`/`*_BatchFileTooLargeError`/`*_BatchTotalSizeTooLargeError`, EN/FR, page-prefixed per this project's established per-page-key convention even though the English text is identical between the two pages) — on any rejection, `_batchResults` is cleared and **no file is processed**, mirroring the existing single-file-rejection "nothing processed" precedent. **The total-size check is provably unreachable through real UI interaction at today's constant values**: 20 files × 10 MB = exactly 200 MB (boundary, accepted), so exceeding 200 MB requires either >20 files or a >10 MB file, both already caught by the other two checks first — confirmed in `BatchFileValidatorTests` by calling `BatchFileValidator.ValidateSizes` directly with 21 fake (`IBrowserFile.Size` set without any real byte allocation) 10 MB files, bypassing the count check on purpose, exactly the workaround the ticket's own test-plan anticipated. Limits are inclusive at both boundaries (20 files exactly / 10 MB exactly / 200 MB exactly all accepted) — asserted explicitly.
  - **33.2 (`ImportProfileTest.razor`)**: `_batchResults` (`List<BatchImportProcessing.Result>`, see 33.4) replaces the old single `_result`. A **file-level** `<details class="test-table-details batch-file-details">` accordion (reusing the Lot V8/R3 pattern verbatim, one level above the pre-existing per-result subsections) wraps each file, titled by its real filename with a Bootstrap `badge` (`bg-success`/`bg-warning`/`bg-danger`/`bg-dark`) showing its status label. **Collapsed by default when the batch has more than 1 file, expanded by default for exactly 1** (so the pre-Lot-033 single-file UX loses no clicks) — content genuinely absent from the DOM while collapsed, same "no `display:none`, no children at all" rule as every other accordion in this codebase (L2/R3/Lot V8 precedent). A `<p id="batch-summary">` above the accordions lists only the non-zero statuses (`"{0} {status}"` per category, comma-joined after `"{N} file(s) processed:"`) — generalizes the ticket's own instruction to omit a zero "Technical error" count to every status, not just that one.
    - **Legacy id preservation, the load-bearing constraint of this lot**: every pre-existing per-result element id (`equipement-table`, `equipement-details-toggle`, `isolements-table`, `warnings-table`, etc.) stays **exactly unsuffixed** when the batch has exactly 1 file (`var suffix = _batchResults.Count == 1 ? "" : $"-{index}";`) — required so every pre-Lot-033 bUnit test (`Run_C7401Fixture_...`, `ResultTables_AreOpenByDefault`, `ClickingSummary_CollapsesTable_...`, etc.) passes **completely unmodified**, per the ticket's own explicit non-regression requirement ("batch d'1 fichier = cas particulier du batch de N"). Multi-file batches get `-{index}` suffixed ids instead (`equipement-table-0`, `equipement-table-1`, ...) to stay valid, non-colliding HTML. Internal `_collapsedSections` keys are always `"{index}:{section}"` regardless of suffix (a C# `HashSet<string>` key, invisible in the DOM, so no legacy constraint applies there).
    - `@onclick='() => ToggleSection(index, "warnings")'` (and the other 4 subsections) needed **single-quoted** attribute delimiters, not the codebase's usual double quotes — a nested C# string-literal argument inside an implicit `@onclick="..."` Razor attribute expression conflicts with the outer double-quote delimiter; caught by a build/render check, not by the type system. Contrast with `open="@(!_collapsedSections.Contains("warnings"))"` elsewhere in the same file, which stays double-quoted because it's an *explicit* `@(...)` expression, which Razor's C#-aware parser handles differently — the pre-existing code already relied on this distinction, this lot just needed to reproduce it correctly for the two new attribute forms it introduced.
  - **33.3 (`ExportProfileTest.razor`)**: mirrors 33.2's validation/summary/accordion mechanics exactly, but **one Export profile applies to the whole batch** — a single Export-profile `<select>` + one "Generate workbook" button (shown once, only if at least one file in the batch is `Ok`/`Warning`) fans out generation across every non-`Rejected`/non-`TechnicalError` file in the batch when clicked (`GenerateWorkbooksForBatch`, still one `foreach`, no parallelization). A per-file generation exception (rare — the import step already succeeded) is caught and isolated to that file's own `GenerationErrorMessage`, never aborting the rest of the batch, same isolation principle as the import step itself. Each generated file gets its **own** `.card.shadow-sm.bg-success-subtle` result block (download link + per-sheet tables), nested inside that file's own accordion — preserves the pre-existing `GeneratedWorkbookResultBlock_IsACardWithShadowAndSuccessTint_NotAPlainAlert` test's exact DOM shape (download link's parent = card-body, grandparent = card) per file, not just once globally.
    - **Download filename convention changed for every case, not just batch**: `{nom-fichier-source-sans-extension}_export.xlsx` (e.g. `Dossier.de.MaD.IDL.-.C7401_export.xlsx` — `Path.GetFileNameWithoutExtension` only strips the *last* extension, so the fixture's own embedded dots survive) **replaces** the old `TargetWorkbookFileNameBuilder.Build(repere, timestamp)` convention outright, including for a batch of 1 — confirmed safe because no pre-existing test ever asserted the `download` attribute's actual value (only that the `href` started with the right `data:` URI prefix), so this is a real, deliberate behavior change with zero risk of silently breaking an unrelated assertion, not a batch-only special case needing a fork.
    - `#export-test-status` (Processing/Error alert id) is **not** reused for the batch-results container — re-reading the pre-Lot-033 source showed that id was already scoped *only* to the Processing/Error/Rejected-alert states, never to the successful-generation branch (which had no wrapping id of its own at all) — so introducing a new `#export-batch-results` wrapper for the batch content required no compromise on either side of that pre-existing asymmetry.
  - **33.4 (factorization, done — not skipped)**: the import step (open stream → buffer → `ClosedXmlWorkbookReader` → `ImportPipelineOrchestrator.Run` → classify `Ok`/`Warning`/`Rejected` → catch-into-`TechnicalError`) was byte-for-byte identical between the two pages' first drafts — extracted into `BatchImportProcessing.ProcessAsync(...)` (`src/ExcelETL.BlazorAdmin/Excel/`), returning a shared `BatchImportProcessing.Result`. `ImportProfileTest.razor` uses that record directly as its `_batchResults` element type (its own former `BatchFileResult` record deleted outright, not kept as a thin duplicate). `ExportProfileTest.razor` keeps its own richer `ExportBatchFileResult` class (needs mutable post-construction generation fields the import page has no use for) but builds it via a `ExportBatchFileResult.From(BatchImportProcessing.Result)` factory instead of duplicating the try/catch/classify logic a second time. **Deliberately left duplicated, documented rather than forced together**: the summary-building/badge-label/toggle methods (tied to each page's own page-prefixed `.resx` keys, `ImportProfileTest_Status*` vs `ExportProfileTest_Status*` — this project's established per-page-key convention, not something to break for this lot) and the per-file accordion *content* (Import's 5 subsections vs. Export's generation/download block) diverge enough that a shared rendering abstraction would have been more complex than the ~15-20 duplicated lines it would replace on each side — matches the ticket's own "architecture by fitness, not mimicry" instruction.
  - Tests: `BatchFileValidatorTests` (new, `tests/ExcelETL.BlazorAdmin.Tests/Excel/`, pure xUnit — no bUnit rendering needed, a hand-rolled `FakeBrowserFile : IBrowserFile` lets `Size` be set directly without allocating real (possibly huge) byte arrays) covers every boundary from 33.1's ticket table. `ImportProfileTestTests`/`ExportProfileTestTests` each gained ~8-10 new bUnit facts (count/size rejection messages, 3-real-fixture batch summary + per-file accordions, a synthetic-`Equipement`-null file mixed with real fixtures showing `Rejected` without affecting the others, a synthetic corrupted (non-Excel-bytes) file mixed with real fixtures showing `TechnicalError` without affecting the others or crashing the render, default open/collapsed file-accordion state for 1-file vs N-file batches) — **discovered while writing the 3-real-fixture test that all 3 fixtures currently carry their own non-blocking warning** (C7401: the Lot 032 TYPE-incoherence anomaly; D8570: the long-standing `"VANNE"` `UnrecognizedTypeElement`; G6306B: the `"POINT DE FEU"`/`"POINT FEU"` DIVERS spelling mismatch) — so the batch-of-3 test asserts "3 non-blocking warning(s), 0 OK" rather than the initially-assumed "2 OK, 1 warning", a real, useful discovery about current fixture state rather than a test-authoring mistake. Every pre-Lot-033 test in both files (31 in `ImportProfileTestTests`, 28 minus the new ones in `ExportProfileTestTests`) passes with **zero assertion changes**, confirmed by running them before writing any new test. Full `ExcelETL.BlazorAdmin.Tests` suite: 402/402 green (was 373 before this lot; +11 `BatchFileValidatorTests`, +10 `ImportProfileTestTests`, +8 `ExportProfileTestTests`).
  - **Hors périmètre, per the ticket**: no batch-file parallelization; no aggregated export zip download; no persistence/history of batch results (still fully in-memory, no HTTP round trip); no per-file progress bar (the existing global spinner suffices at this file-count/size scale); the X11 custom-upload-button request stays explicitly deferred (this lot only added `multiple` to the existing V10-styled `<InputFile>`); no batch-level dedup/same-name detection (the orchestrator never sees the source filename, only content — a duplicate name in one batch is not an error).
- **Lot 034 (archivage des fichiers source/cible générés via `POST /api/oxo/process`, done — 2026-07-25, ticket doc `docs/tickets-tdd-lot-034-archivage-fichiers-generes-api.md`)**: closes the client's own reported gap — nothing kept a trace of a file processed via the API channel, unlike the Blazor test pages. **Périmètre API uniquement** (`ImportProfileTest.razor`/`ExportProfileTest.razor` untouched, explicit ticket exclusion).
  - **34.0 investigation found `ProcessOxoFileService` was already archiving the target file** via `IFileStorageService` (Lot K), but only on success (never on rejection), flat (no `yyyy/MM`), with no metadata, and — critically — **not best-effort**: a `SaveAsync` failure there propagates and fails the whole request. **Deliberately left completely untouched, not folded into the new mechanism**: an existing `OxoProcessEndpointTests` assertion (`Directory.GetFiles(_archiveDirectory, "*.xlsx").Should().ContainSingle()`) already pins its exact single-flat-file behavior, and the ticket's own non-regression requirement forbids modifying it. The two archiving mechanisms coexist side by side, documented inline in `ProcessOxoFileService` as a deliberate decision, not an oversight.
  - **34.1 (`src/ExcelETL.Domain/Archiving/GeneratedFileRecord.cs`, `src/ExcelETL.Application/Archiving/`)**: `GeneratedFileRecord` (plain `sealed class`, not owned by any aggregate) + `GeneratedFileArchiveStatus` enum (`Success`/`NonBlockingWarning`/`Rejected`, same semantics as Lot 033's batch badges: `Equipement is null` ⇒ `Rejected`; non-null + `HasErrors` ⇒ `NonBlockingWarning`; else `Success`). **Deliberately permissive** — `EquipementRepere`/`TargetFileName`/`TargetFilePath` are all legitimately `null` on rejection (the client's own "proof the source data was corrupt" use case) — only `SourceFileName`/`SourceFilePath` are guarded, via plain `ArgumentException.ThrowIfNullOrWhiteSpace` (a developer invariant, not a `DomainValidationException`/`DomainErrorCode` — this record is only ever built internally by `ProcessOxoFileService`, never from a user-facing form, so it stays out of the i18n scope described earlier in this file, same reasoning as `ImportResult`'s own constructor). `IGeneratedFileArchiveStore` (`SaveAsync`/`SearchAsync`/`GetByIdAsync`, append-only — no Update/Delete, no purge policy this lot) and `IGeneratedFileWriter` (`WriteSourceAsync`/`WriteTargetAsync`, both in `Application/Archiving/`) round out the interfaces 34.2/34.3 implement.
  - **34.2 (`EfGeneratedFileArchiveStore`, `GeneratedFileRecordConfiguration`, migration `20260725010636_AddGeneratedFileRecord`)**: same `IDbContextFactory`-per-method pattern as `EfImportProfileStore`, simpler (no upsert, just `Add`+`SaveChangesAsync`). `SearchAsync`'s `EquipementRepere` `Contains`/`OrdinalIgnoreCase` filter runs **in-memory** after `ToListAsync()`, not translated to SQL — same "InMemory provider's string-comparison LINQ translation is unreliable" reasoning already applied to `EfImportProfileStore.SaveAsync`'s own name-uniqueness check, and the record volume here is small by design. **Real EF Core gotcha hit and fixed**: `ImportProfileId`/`ExportProfileId` (plain denormalized `Guid`/`Guid?`, no relationship intended) initially broke `GeneratedFileRecord`'s constructor-binding materialization (`"Cannot bind 'importProfileId', 'exportProfileId' ... Navigations to related entities ... cannot be bound"`) — EF Core's foreign-key-by-convention discovery matches ANY property named `<PrincipalEntityTypeName>Id` against an entity type already registered on the same `DbContext` (here, `ImportProfile`/`ExportProfile`, both already `DbSet`s on `ExcelEtlDbContext`) and silently turns it into a shadow relationship, even with zero navigation properties on either side. Fixed by explicitly configuring both as plain scalar properties (`builder.Property(r => r.ImportProfileId)`/`.Property(r => r.ExportProfileId)`) in `GeneratedFileRecordConfiguration` — worth remembering for any future entity that stores another aggregate's `Id` by value without wanting a real EF relationship.
  - **34.3 (`FileSystemGeneratedFileWriter`, Infrastructure)**: `{RootPath}\{yyyy}\{MM}\{yyyyMMdd-HHmmss-fff}_{source|target}_{sanitized original file name}` — `timestampUtc` is a caller-supplied parameter (not read from a clock inside the writer), so `ProcessOxoFileService` captures **one** timestamp per request and reuses it for both the source and target write plus `GeneratedFileRecord.GeneratedAtUtc`, keeping a request's pair of archived files correlated by name. `GeneratedFileNameSanitizer` (new, Infrastructure/Archiving — deliberately **not** a reuse of `ExcelSheetNameSanitizer`, different forbidden-character set: Windows filename chars `\ / : * ? " < > |`, no length cap unlike Excel's 31-char sheet-name limit) replaces forbidden characters with `_`. No collision handling beyond millisecond precision (`FileMode.Create` on an exact-timestamp-and-name collision simply overwrites) — an accepted, tested (not just assumed) characterization of real behavior, per the ticket's own explicit instruction. Tested against a real temp directory (never a filesystem mock), same convention as `LocalFileStorageServiceTests`.
  - **34.4/34.6 (`ProcessOxoFileService`/`OxoController` wiring + DI)**: `ProcessOxoFileCommand` gained `byte[] SourceFileContent` (the raw uploaded bytes) alongside the already-constructed `IWorkbookReader` — the reader only ever exposes cell values, never the original bytes, so `OxoController.Process` now buffers `IFormFile.OpenReadStream()` into a byte array **once**, builds `ClosedXmlWorkbookReader` from a fresh `MemoryStream` over those same bytes, and passes the array through unchanged for archiving. `ProcessOxoFileService` archives the source file **systematically, including on rejection** (before the early `return` in the `Equipement is null` branch) and the target file only when generation actually happens, via a new private `TryArchiveAsync` helper wrapped in **its own isolated `try`/`catch`** (not the pre-existing outer one, which rethrows) — a disk-full or DB-down failure there is logged at `Error` (`"Failed to archive generated files for source file {SourceFileName} -- best-effort, HTTP response unaffected"`) and never propagates, so the HTTP response the client already has stays exactly what Lot K would have returned. `GeneratedFileRecord.TargetFileName` stores the same `generatedFileName` already computed for the HTTP response (`TargetWorkbookFileNameBuilder.Build(...)`) — `SourceFileName`/`TargetFileName` are both "the logical/display name", `SourceFilePath`/`TargetFilePath` are the actual on-disk relative paths with the archival timestamp prefix embedded (for the future Blazor page's benefit, see 34.5). `ImportProfileId`/`ExportProfileId` on the record are always populated in practice for this service (both profiles are resolved, successfully, before any archiving call is ever reached) even though the Domain model keeps `ExportProfileId` nullable per the ticket's own literal spec (a future caller reaching this store before export-profile resolution is a real, if currently unexercised, possibility). DI: `IGeneratedFileWriter`/`FileSystemGeneratedFileWriter` (`AddSingleton`, stateless) + `IGeneratedFileArchiveStore`/`EfGeneratedFileArchiveStore` (`AddScoped`, `IDbContextFactory`-backed) registered in **both** hosts (BlazorAdmin needs read access for 34.5) — new config section `GeneratedFilesArchive:RootPath` in all 4 `appsettings*.json` (prod: `D:\AM-OXO-ETL\generated-files`, shared by both hosts since it's the same on-prem filesystem; dev: `C:\ExcelEtlGeneratedFilesArchive`, also shared between the two hosts' dev settings so a locally-run BlazorAdmin can see what a locally-run WebAPI archived). `Moq` added to `ExcelETL.WebAPI.Tests` (previously had zero mocking — every prior WebAPI test used real fixtures/`WebApplicationFactory`) specifically to simulate the "archiving fails" scenario without a hand-rolled fake, since Moq is already an OSS-approved solution-wide dependency.
  - **Fixture-state discovery while writing the integration tests**: the ticket's own worked example assumed the real C7401 fixture would land `Status = Success` — it doesn't, since Lot 032's TYPE-incoherence detector already flags a real non-blocking anomaly in that fixture's PROCEDURE sheet (documented in the Lot 032/033 bullets above) — the C7401 integration test asserts `NonBlockingWarning` instead, with a comment explaining why, and the genuine `Success`-status mapping is covered at the `ProcessOxoFileServiceTests` unit level (a hand-built zero-error `ImportResult`) rather than assumed against a real fixture that could grow a new warning at any time.
  - **34.5 (`GeneratedFiles.razor`, `/generated-files`, admin-only)**: `@attribute [Authorize(Roles = IdentitySeeder.AdminRoleName)]`, nav entry added to `NavMenu.razor`'s existing admin block (after Users) with a new `bi-archive-nav-menu` icon (real Bootstrap Icons `bi-archive` SVG path, inline — no icon font, per this project's established convention). List via `IGeneratedFileArchiveStore.SearchAsync(...)` (sort delegated to the store, never client-side); a text search box (`#generated-files-search-input` + `#generated-files-search-button`/`#generated-files-clear-search-button`) re-queries `SearchAsync` rather than filtering an already-loaded list, consistent with the ticket's own "list can grow over time" reasoning. Status badges reuse Lot 033's exact class mapping (`bg-success`/`bg-warning text-dark`/`bg-danger`). **Download is lazy per row, not eager for the whole list**: unlike `ExportProfileTest.razor`'s single already-in-memory generated file, this page's rows can number in the dozens/hundreds over time with no purge policy yet, so eagerly base64-encoding every archived source (~1-6 MB) and target file on every page load would not scale — each row instead shows a "prepare download" button that reads the file **synchronously** (`File.ReadAllBytes`, not `ReadAllBytesAsync`) on click and swaps itself for a real `<a href="data:...;base64,...">` anchor, matching `ExportProfileTest.razor`'s no-JS-interop data-URI mechanism otherwise. **Synchronous read is a deliberate fix, not an oversight**: the async version raced past bUnit's click-then-render wait in this component's own tests (`ElementNotFoundException` immediately after `.Click()`, despite the file genuinely existing on disk) — a real cross-thread I/O continuation apparently isn't as reliably awaited by bUnit's synchronous `Click()` helper as an EF Core InMemory continuation is elsewhere in this codebase; a synchronous read for these small, occasional, one-shot admin actions sidesteps the question entirely rather than chasing the exact bUnit/Blazor dispatch mechanics. Table (desktop) and card (mobile) views duplicate the download button markup with a `-card-` id suffix (same pattern as `ImportProfiles.razor`'s row actions) since both are unconditionally present in the DOM (CSS-hidden, not conditionally rendered) and would otherwise collide on id.
  - Tests: `GeneratedFilesTests.cs` (bUnit, real temp-directory-backed files for the download tests — never a filesystem mock, matching 34.3's own convention) covers empty-list/loading, 3-varied-status rendering with correct badges, a `Rejected` row's missing target button + `"—"` repère placeholder, search-triggers-`SearchAsync`-with-typed-term-and-updates-list, clear-search-reloads-with-null-term, source/target download producing a correctly-base64-encoded `data:` link, and the table/card responsive split (V2 convention). **`BunitContext.Dispose()` is not virtual** (only its protected `Dispose(bool disposing)` is) — `GeneratedFilesTests`' own temp-directory cleanup needed `protected override void Dispose(bool disposing)`, not a naive `public void Dispose()` (which would silently hide, not override, the base method — xUnit calls through the `IDisposable` interface reference, so a hidden method would simply never run). `NavMenuTests.cs`'s `AdminLinkIds` array and the link-order test both extended with `nav-generated-files-link` (no dedicated "true DOM absence when unauthenticated" test on `GeneratedFiles.razor` itself — confirmed by reading `UsersTests.cs` first that `[Authorize]` route attributes aren't enforced by bUnit's direct `Render<T>()` at all, only `<AuthorizeView>` inside the render tree is; the actual "true absence" guarantee for this page is the pre-existing `NavMenuTests` mechanism, same as every other admin page). Full suite after Lot 034: Domain 278 (unchanged)/Application 152 (was 147, +5 `ProcessOxoFileServiceTests`, net after the 3 pre-existing `ProcessOxoFileCommand` call sites' mechanical arg additions)/Infrastructure 152 (was 136, +16: `EfGeneratedFileArchiveStoreTests`, `FileSystemGeneratedFileWriterTests`, `GeneratedFileNameSanitizerTests`)/WebAPI 19 (was 15, +4)/BlazorAdmin 411 (was 402, +9 `GeneratedFilesTests`)/Hosting 6/legacy 9+15 — all green.
  - **Not live-browser-verified this session** — no admin credentials available (same recurring limitation noted in several prior lots' bullets above); confidence rests on the full bUnit suite plus a clean build.
  - **Hors périmètre, per the ticket**: `ImportProfileTest.razor`/`ExportProfileTest.razor` archiving (test/config tools, not the client's real channel); purge/retention policy (no duration known yet); an API endpoint to retrieve an archived file from the legacy app (Blazor-only consultation this lot); advanced search (multi-filter, CSV export, server pagination — a simple date-descending list + repère text search suffices at today's volume); compression/deduplication/external storage (on-premise, no signal it's needed); any change to `/api/oxo/process`'s existing HTTP contract (archiving is a pure side effect); collision handling beyond millisecond precision (revisit only if a real incident justifies it).
- **Lot 035 (dette légère / polish issue des audits qualité du 25/07, done — 2026-07-25, ticket doc `docs/tickets-tdd-lot-035-polish-dette-legere.md`)**: 8 sous-tickets indépendants, tous "dette légère/cosmétique" per the 5 layered audits (`audit-qualite-{domain,application,infrastructure,webapi,blazoradmin}-2026-07-25.md`) — no observable behavior change anywhere in this lot except 35.7/35.8 which add a capability already present elsewhere in the project (Lot W's pattern).
  - **35.1 (`SheetGenerationRule`, Domain)**: the ~90-line constructor's 6 validation blocks extracted into named private static methods (`ValidateSheetNameNotEmpty`/`ValidateColumnPivotSourceCompatibility`/`ValidateNoPointColumnsForTacheMultiple`/`ValidateNoApplicationColumnsForTacheMultiple`/`ValidateNoDuplicateHeaders`/`ValidateNoDuplicateColonneNom`/`ValidateNoDuplicateApplicationNom`), called in the same order as before — pure refacto, all 23 pre-existing `SheetGenerationRuleTests` pass unmodified.
  - **35.2 (Domain)**: new `Common/ProfileNaming.MaxNameLength` and `Common/ColumnMarking.DefaultMarkValue` — `ImportProfile`/`ExportProfile` and `PointColumnDefinition`/`ApplicationColumnDefinition` each keep their own public `const` but redirect it to the shared source (`public const int MaxNameLength = ProfileNaming.MaxNameLength;`), preserving every existing call site (`ImportProfileConfiguration`, both editors' `maxlength` attributes, test assertions) unmodified.
  - **35.3 (Domain + Application)**: `ExtractionErrorCode.TypeIncoherenceDansTacheMultiple` (Lot 032, the only non-English enum member in Domain) renamed to `TacheMultipleTypeIncoherence` — Domain enum, `ExtractionErrorLogging`, `ProcedureExtractionService`, and both referencing test files updated together; no `.resx` key referenced the literal member name, no user-visible message changed.
  - **35.4 (`ImportPipelineOrchestrator`, Application)**: the repetitive `AddRange` + per-isolement `Localisation`/`Tableaux`/`Applications`/`RepereParent` broadcast loop extracted into a named private static method, `BroadcastEquipementContext` — same call site, same content, all `ImportPipelineOrchestratorTests`/integration tests pass unmodified.
  - **35.5 (BlazorAdmin)**: `PencilIconMarkup`/`CopyIconMarkup`/`TrashIconMarkup` (previously declared identically in both `ImportProfiles.razor` and `ExportProfiles.razor`) extracted to `Shared/AdminIconMarkup.cs`; `BuildAvailableDuplicateName` (also duplicated identically) extracted to `Shared/ProfileDuplicateNaming.cs`, reparametrized on `IReadOnlyList<string> existingNames` instead of a concrete profile type — first files in `src/ExcelETL.BlazorAdmin/Shared/`, a new folder. New dedicated `ProfileDuplicateNamingTests.cs` (3 facts: unchanged-when-available, incremented-when-taken, converges-from-an-already-suffixed-copy) — the helper had only ever been exercised indirectly through page rendering before.
  - **35.6 (`NavMenuTests.cs`)**: 3 tests that selected by `cut.Markup.Should().Contain(...)`/`NotContain(...)` (the exact assertion style that let the Lot L1 "Journaux" regression through) switched to DOM selection by ID — `NavMenu_WhenNotAuthorized_DoesNotShowProfileLink` now asserts `cut.FindAll("#nav-profile-link").Should().BeEmpty()`; the EN/FR culture tests for Register/Login now select `#nav-register-link`/`#nav-login-link` and assert on `.TextContent`. Register's `NavLink` had no stable id before this lot — added `id="nav-register-link"`, consistent with the pre-existing `#nav-login-link`. Same 19/19 `NavMenuTests` pass, same functional scenarios, only the assertion mechanism changed.
  - **35.7 (`ExportProfileEditorTests.cs`, pure test-coverage gap, no production change)**: `SheetGenerationRuleForm.razor`'s Modify/Delete of an already-added `ApplicationColumnDefinition` (Lot U4) had zero test coverage — only the Add path was tested. 5 new facts mirror the existing `PointColumnDefinition` Modify/Delete tests exactly (display buttons, prefill on modify, save-in-place, cancel-discards, delete-removes). All passed on the first run — no real bug found, confirming the audit's static-reading assumption.
  - **35.8 (`ImportProfileEditor.razor`, BlazorAdmin)**: `DefaultTableaux`/`DefaultApplicationNames` (Lot U1, previously add-only, rendered as a single comma-joined `<p>` summary) gained the exact Lot W modify/delete-in-place pattern (`UnconditionalColonneNames`'s own treatment) — per-item `block-field-list` with icon Modify/Delete buttons, a single-input inline edit row with Save/Cancel, and 2 new `.resx` keys (`ImportProfileEditor_EmptyTableauNameError`/`EmptyApplicationNameError`, EN/FR) for the empty-value-on-save guard. 16 new tests (modify/save/empty-error/cancel/delete per list, plus 2 persistence round-trips per list through `SaveProfileAsync`/`Store.GetAllAsync`) — the pre-existing Add-flow tests needed no changes.
  - **35.0's closing-checklist documentary item** (stale `audit-qualite-code-TEMPLATE.md` reference to the typed `HttpClient` of `/upload-test`, removed at Lot K4) was checked and found **already absent** from the template as committed at this lot's reference commit (`82f4c81`) — no edit needed, the ticket's own description of the gap didn't match the file's actual current content.
  - Full suite after Lot 035: Domain 278 (unchanged count, same tests)/Application 152 (unchanged)/Infrastructure 152 (unchanged)/WebAPI 19 (unchanged)/Hosting 6 (unchanged)/legacy 9+15 (unchanged)/BlazorAdmin 435 (was 413 before this lot; +3 `ProfileDuplicateNamingTests`, +5 `ApplicationColumnDefinition` modify/delete, +16 `DefaultTableaux`/`DefaultApplicationNames` modify/delete, +3 `NavMenuTests` net-zero — same 19 tests, assertion style only) — 1066 tests total across the whole solution, all green.
  - **Hors périmètre, per the ticket** (each flagged for a separate dedicated ticket, not touched here): `POST /api/oxo/process` request validation (missing profile ids in the multipart, malformed/empty file, untested at the HTTP level); Import vs. Export sheet-rule-card Modify/Delete parity (plain text on Import, icon on Export — a real/product-impact item, not cosmetic); `ProcessOxoFileService`'s double archiving mechanism (Lot K + Lot 034 coexisting, needs the WebAPI integration test re-evaluated first); `DirectCell` (Domain, never constructed anywhere) — needs Simon's arbitration (dead code vs. prepared-for-future-use) before any action; `IWorkbookReader.SheetExists` (never called by any of the 5 extraction services) — needs an Infrastructure/WebAPI-side check before concluding dead code.
- **Lot 036 (validation explicite du contrat `POST /api/oxo/process`, done — 2026-07-25, ticket doc `docs/tickets-tdd-lot-036-validation-payload-oxo-process.md`)**: closes the "hors périmètre" item Lot 035 itself flagged (untested request validation on this route) — the only lot in this run that changes an *observable* HTTP behavior of the API, not just internal polish. Implemented 36.3 → 36.1 → 36.2 in the ticket's own recommended order (simplest/no-production-change first), one commit per sub-ticket.
  - **36.0 investigation (mandatory, done before any code)**: confirmed `ProcessOxoFileRequest.ImportProfileId`/`ExportProfileId` were plain non-nullable `Guid` with no validation attribute, and `OxoController` passed them straight through with no missing-ness check at all. Reproduced the real ClosedXML behavior on a non-Excel byte stream directly (a throwaway xUnit test, removed after use, not guessed from the audit): `XLWorkbook`'s constructor throws `System.IO.FileFormatException` ("File contains corrupted data.") — a stable, specific **BCL** type, not a ClosedXML-proprietary one. Confirmed `GlobalExceptionHandler.StatusCodeFor`'s switch has no explicit `default` arm of its own (falls through its `_ =>` case to `500`), but more importantly confirmed `BusinessExceptionLocalizer.TryLocalize` returns `null` for *any* exception not implementing `IHasDomainErrorCode`/`IHasApplicationErrorCode` — so `GlobalExceptionHandler.TryHandleAsync` returns `false` and never even reaches `StatusCodeFor` for `FileFormatException`, ruling out option (a) (adding it to the `switch`) as ineffective on its own; option (b) (catch explicitly in `OxoController`, at the same level as the pre-existing empty-file check) was the only one that could actually work, confirmed by reading `BusinessExceptionLocalizer` before choosing. Confirmed `ProcessOxoFileRequest`/`OxoController` have no consumer in the .NET 10 solution beyond `OxoProcessEndpointTests` — `legacy/ExcelProcessingClientService` always sends both profile-id fields as real, non-null `Guid` values (never omits them), so the `Guid?` contract change has zero impact there, nothing to signal to Simon.
  - **36.3 (test-only, no production change)**: added the two missing HTTP tests for the pre-existing `request.File is null || request.File.Length == 0 → 400` path. **Real, non-obvious finding**: a multipart request with the `File` field entirely absent never reaches that check at all — `[ApiController]`'s automatic model-state validation treats the non-nullable `IFormFile` property as implicitly required (nullable-reference-types-aware validation) and short-circuits with ASP.NET Core's own generic `"The File field is required."` `ProblemDetails` body before the action method ever runs; only a **present-but-zero-length** file reaches `OxoController`'s own `EmptyFileUploadRequired`-localized check. Both are `400`, but via genuinely different code paths — documented via two separate tests asserting their own distinct message, not assumed to be identical.
  - **36.1 (`ImportProfileId`/`ExportProfileId` absent from the multipart → 400 explicit)**: `ProcessOxoFileRequest.ImportProfileId`/`ExportProfileId` changed from `Guid` to `Guid?` (the default approach the ticket proposed, confirmed unblocked by 36.0 — no `[FromForm]` binding constraint stood in the way). `OxoController` now checks both for `null` at the very top of `Process`, before the file check and before any store lookup — each with its own localized message (`ImportProfileIdRequired`/`ExportProfileIdRequired`, new `ApplicationMessages.resx`/`.fr.resx` keys), sequential `if`s so when both are missing only the first (`ImportProfileId`) is reported (documented by a dedicated test, not left to guesswork). A syntactically-valid `Guid` matching no profile is completely unaffected — still `404` via the pre-existing `ImportProfileNotFoundException`/`ExportProfileNotFoundException` path, verified by the pre-existing non-regression tests passing unmodified.
  - **36.2 (file content not a valid Excel workbook → 400, not an unqualified 500)**: `OxoController` now wraps the `new ClosedXmlWorkbookReader(...)` construction in a `try`/`catch (FileFormatException)`, translating straight to a `400` with a new localized `InvalidExcelFileFormat` message — at the controller level (option (b) from 36.0), not `GlobalExceptionHandler.StatusCodeFor`, per the investigation's finding that the latter would never be reached for this exception type. Deliberately narrow (`catch (FileFormatException)`, never `catch (Exception)`) per the ticket's own explicit warning against masking a genuine future server error behind a false `400`. Non-regression: all 3 real fixtures (C7401/D8570/G6306B) and the existing rejected/valid-request tests pass unmodified.
  - Tests (`OxoProcessEndpointTests.cs`, +11: 2 for 36.3, 6 for 36.1, 1 for 36.2, plus the pre-existing suite untouched): full `ExcelETL.WebAPI.Tests` suite 25/25 green (was 19 before this lot's first commit, +2/+3/+1 across the three sub-tickets' commits); full solution (Domain 278/Application 152/Infrastructure 152/Hosting 6/legacy 9+15/BlazorAdmin 435, all unchanged) green throughout.
  - **CLAUDE.md's "Web API surface" bullet updated in the same spirit as this lot's own closing note** — this is a public-contract change (`Guid` → `Guid?`), not an internal detail, per the ticket's own explicit instruction.
  - **Hors périmètre, per the ticket**: route name, `ApiKeyAuthenticationHandler`, and the endpoint's synchronous/end-to-end nature (not reopened); Lot 034's best-effort archiving mechanism and its coexistence with Lot K's `IFileStorageService` archiving (untouched, separate subject); any new business-content validation rule beyond "can be opened as a valid Excel workbook" (existing rules — PROCEDURE empty, etc. — not reopened); whether `DomainRuleViolationException` (409) is genuinely reachable from this route (flagged by the audit as "unverified", left for a separate investigation if judged worthwhile).
- **Lot 037 (parité des boutons Modifier/Supprimer de `.sheet-rule-card`, done — 2026-07-25, ticket doc `docs/tickets-tdd-lot-037-parite-boutons-carte-regle-feuille.md`)**: closed a silent styling drift flagged by `audit-qualite-blazoradmin-2026-07-25.md` §2.1 — `ExportProfileEditor.razor`'s sheet-rule-card Modify/Delete buttons were already icon-only (`btn-outline-secondary`/`btn-outline-danger`, inline SVG, `aria-label`/`title`) per `convention-ui-blazor-icones-boutons.md`, but `ImportProfileEditor.razor`'s equivalent buttons were still plain text (`btn-secondary`/`btn-danger`). **Export was confirmed as the reference, not the reverse.**
  - **37.0 investigation found `Shared/AdminIconMarkup` (Lot 035.5) was not yet used by either editor's sheet-rule-card buttons** — only the two list pages (`ImportProfiles.razor`/`ExportProfiles.razor`) referenced it; both editors still declared their pencil/trash SVG inline (`ExportProfileEditor.razor`'s Pencil path was already byte-identical to `AdminIconMarkup.Pencil`, but its Trash path used slightly different — visually equivalent — path-command syntax than `AdminIconMarkup.Trash`). Also confirmed `ImportProfileEditor_ModifyButton`/`_DeleteButton` are **not orphaned** by this change — both keys were already reused as `aria-label`/`title` elsewhere in the same file (`SheetRuleForm.razor`'s block-field buttons, the Lot 35.8 Tableaux/Applications lists), so converting their sheet-rule-card usage from visible text to `aria-label`/`title` is consistent with an existing pattern in the same file, not a new one.
  - **Fix, both editors, not just Import**: `ImportProfileEditor.razor`'s sheet-rule-card Modify/Delete buttons switched from text to `AdminIconMarkup.Pencil`/`.Trash` (`btn-outline-secondary`/`btn-outline-danger block-field-icon-btn`, `aria-label`/`title` from the existing resx keys, IDs unchanged: `modify-sheet-rule-button-{index}`/`delete-sheet-rule-button-{index}`). **`ExportProfileEditor.razor`'s own inline SVG was also replaced by the same `AdminIconMarkup` constants** (a small deviation beyond the ticket's literal "align Import onto Export" instruction, justified by 37.0's own finding that the two icons weren't byte-identical to begin with and by Lot 035.5's own rationale for the `Shared/` folder — single source of truth prevents a third silent divergence rather than merely fixing the second one). Both files gained `@using ExcelETL.BlazorAdmin.Shared` (neither had it before).
  - **Real-but-unrelated test fragility found while verifying non-regression**: `ImportProfileEditorTests.ExistingSheetRule_DisplaysAbsoluteExcelRanges_NotRawRowOffsets`'s page-wide `cut.Markup.Should().NotContain("0-1")` started failing once the sheet-rule-card gained the Pencil icon — `AdminIconMarkup.Pencil`'s own path data coincidentally contains the literal substring `"0-1"` (`"...0 0 0-1 0v6a..."`), nothing to do with the raw-offset regression the assertion was meant to guard against. Fixed by scoping the assertion to the `.block-field-range` elements' own `TextContent` instead of the whole page's markup — same intent, no longer coupled to unrelated icon markup.
  - Tests: `ImportProfileEditorTests.cs` gained `SheetRuleCard_ModifyDeleteButtons_AreIconOnly_WithAriaLabelsAndNoVisibleText` (icon present, `aria-hidden="true"`, `aria-label`/`title` set, no visible text) — every pre-existing functional test for these two buttons (Modify opens edit mode, Delete's confirm/cancel flow) selects by `id` only and needed no changes. `ProfileEditorParityTests.cs` gained `SheetRuleCardModifyDeleteButtons_CssClassAndIconStructure_AreIdenticalBetweenImportAndExportEditors` (37.2's guard-rail — string-equal class comparison plus icon-present/no-visible-text on both sides, the "same style as R1-R3/30.5" pattern the ticket asked for). Full `ExcelETL.BlazorAdmin.Tests` suite: 437/437 green (was 435 before this lot; +2 new, 1 pre-existing test's assertion re-scoped in place, not counted as new). Full solution build green.
  - **Hors périmètre, per the ticket**: `BlockFieldForm.razor`'s own Modify/Delete buttons (already icon-only, different pattern, not reopened); the intermediate Add button, final Save button, responsive grid (Lots R/030, not reopened); `ImportProfiles.razor`/`ExportProfiles.razor`'s row-action buttons (already consistent between the two per the audit, separate subject from Lot 035.5's duplication fix); `convention-ui-blazor-icones-boutons.md` itself (applied, not revised).
- **Lot 038 (`ApiTest.razor` — real HTTP test page for the M2M contract, done — 2026-07-26, ticket doc `docs/tickets-tdd-lot-038-blazor-page-test-api-m2m.md`)**: a **conscious, documented reopening** of the "BlazorAdmin never talks to WebAPI over HTTP" rule Lot K4 established — not a regression of it. Motivated by 3 needs the in-process test pages (`ImportProfileTest.razor`/`ExportProfileTest.razor`) and the existing `WebApplicationFactory` integration tests don't cover: manual post-deployment verification against the real server, a client-facing demo of the contract, and ad hoc production debugging without the legacy app.
  - **38.0 investigation** confirmed the exact contract to reproduce: `ExcelProcessingClientService` (legacy) sends `X-Api-Key` header + multipart fields `ImportProfileId`/`ExportProfileId`/`File` to `api/oxo/process`; `ApiKeyAuthenticationHandler` fails with a bare 401 (no body) on a missing/invalid key; `OxoController` returns 400 (missing profile id / empty file / invalid Excel format), 404 (`ImportProfileNotFoundException`/`ExportProfileNotFoundException`, `Detail` already names which of the two ids), 422 (`ProblemDetails.Extensions["errors"]`, one entry per `Sheet`/`BlockIdentifier`/`Code`/`Message`), or 200 (`FileStreamResult` + `Content-Disposition` filename). No `WebApplicationFactory`-based test project exists for `ExcelETL.BlazorAdmin`, so 38.1's fail-fast validation is exercised via a standalone extracted static method, not by booting `Program.cs`.
  - **38.1 (`src/ExcelETL.BlazorAdmin/Configuration/`)**: `OxoApiTestClientOptions` (`BaseUrl`/`ApiKey`) + `OxoApiTestClientOptionsValidator.ValidateOrThrow(string?, string?)` (extracted specifically so it's unit-testable without a host). **Deliberately deviates from the ticket's literal "Development.json/Production.json" wording**: no `appsettings.Production.json` exists anywhere in this solution today (confirmed by search) — the established convention here is `appsettings.json` = production defaults (no section at all for a secret, same as `ApiKeyAuthentication:ApiKey` in `ExcelETL.WebAPI`), `appsettings.Development.json` = dev override. Followed that existing convention instead: `appsettings.Development.json` gained `OxoApiTestClient:BaseUrl`/`ApiKey` (`https://localhost:7088/`, the WebAPI dev HTTPS port from its own `launchSettings.json`, and the exact same `dev-local-api-key-CHANGE-ME` value as `ExcelETL.WebAPI/appsettings.Development.json`'s `ApiKeyAuthentication:ApiKey`, so two locally-run hosts actually authenticate against each other out of the box); `appsettings.json` gained no section at all, so a real deployment missing it fails fast at startup (`Program.cs`) rather than silently degrading — the real production `BaseUrl`/`ApiKey` are expected via user secrets/env var, never committed here.
  - **38.2 (`src/ExcelETL.BlazorAdmin/Services/`)**: `IOxoApiTestClient`/`OxoApiTestClient` (registered via `AddHttpClient<IOxoApiTestClient, OxoApiTestClient>`, `BaseAddress`/6-minute `Timeout` from options — matches `ExcelProcessingClientService.DefaultTimeout`'s own rationale) + `OxoApiTestResult` (5 variants: `Success`/`BusinessRejection`/`ProfileNotFound`/`Unauthorized`/`TechnicalError`, one static factory each — no business logic in the client itself, it only maps `HttpStatusCode` → variant and reads `ProblemDetails.Detail`/`Extensions["errors"]` verbatim). `TechnicalError` deliberately carries only the raw status code, never a message — the page shows one generic localized string for every non-categorized failure, consistent with the project's "no raw technical detail in the UI" principle. Tested against a hand-rolled `FakeHttpMessageHandler` (`tests/ExcelETL.BlazorAdmin.Tests/Services/`, mirrors `legacy/ExcelProcessingClientService.Tests`' own class verbatim rather than adding a MockHttp-style NuGet dependency for equivalent capability) — one case per real HTTP status `OxoController` can return, plus header/multipart-shape assertions.
  - **38.3 (`ApiTest.razor`, route `/api-test`)**: two read-only dropdowns (`#import-profile-select`/`#export-profile-select`, populated in process via `IImportProfileStore`/`IExportProfileStore` — never over HTTP, only the final `Process` call is) + a mono-file `<InputFile id="source-file-input">` (no `multiple` — the HTTP contract is single-file, unlike Lot 033's in-process batch pages) + `#process-button` (disabled until both profiles and the file are selected). Result rendering switches on `OxoApiTestResult.Status`: success is a `bg-success-subtle` card with a `data:` URI download link (same mechanism as `ExportProfileTest.razor`); business rejection is an `alert-danger` list (same shape as the two in-process test pages' own rejected-file block); profile-not-found renders the API's own `Detail` text verbatim (already names which id, from `ImportProfileNotFound`/`ExportProfileNotFound`'s existing localized messages — no extra logic needed on the Blazor side); unauthorized and technical-error are each one fixed localized message (the former explicitly pointing at the `OxoApiTestClient:ApiKey` server configuration, since the admin can't fix a 401 from the browser).
  - **38.4 (NavMenu)**: new `#nav-api-test-link` (`bi-send` icon, inline SVG per this project's icon convention) added to the admin block, positioned right after `#nav-export-profiles-link` and before `#nav-users-link` — the ticket's own suggested placement ("same section as the `ImportProfileTest`/`ExportProfileTest` NavMenu links") no longer applies literally, since Lot S1 removed those two links from `NavMenu.razor` entirely (moved to buttons on their respective list pages); placed instead by the same "functional proximity" rationale Lot S1 itself used. `NavMenuTests.cs`'s `AdminLinkIds`/ordering test extended; true-DOM-absence-when-unauthenticated already covered by the existing parametrized tests.
  - Tests: `OxoApiTestClientOptionsValidatorTests`/`OxoApiTestClientConfigurationTests` (Configuration), `OxoApiTestClientTests` (12 cases), `ApiTestTests` (6, real EF-backed profile stores + `Mock<IOxoApiTestClient>`, per the ticket's explicit "no real HTTP call in bUnit" instruction), `NavMenuTests` (+1 href check plus the extended `AdminLinkIds`/ordering arrays). Full `ExcelETL.BlazorAdmin.Tests` suite: 462/462 green (was 437 before this lot; +25). Full solution build green.
  - **38.5**: `docs/instructions-systeme-claude-code.md`'s BlazorAdmin bullet, which did **not** already contain the anticipated text the ticket assumed (checked directly — no matching text existed), updated in place to describe the new documented exception (`ApiTest.razor`, `/api-test`, server-side API key) rather than left stale.
  - **Hors périmètre, per the ticket**: load testing / repeated calls; consultation of archived files (Lot 034's own page already covers that); profile creation/editing from this page (read-only dropdowns only); multi-file upload; replacing the existing `WebApplicationFactory` integration tests on `OxoController`; a manual API-key input field in the browser (server-side config only, no exception).
- **Lot 039 (accessibilité clavier du toggler mobile `NavMenu.razor`, done — 2026-07-27, ticket doc `docs/tickets-tdd-lot-039-accessibilite-clavier-navmenu.md`)**: follows `audit-design-blazoradmin-2026-07-27.md` §3.2 / its priority synthesis — the one real total keyboard-access blocker found across the whole audited BlazorAdmin surface (everything else in that audit is comfort/announcement drift, not a hard block).
  - **39.0 investigation**: the mobile toggler `<input type="checkbox" class="navbar-toggler">` had no `id` at all and carried only a `title` — no `aria-label`/`aria-controls`/`aria-expanded`, purely CSS-driven open/close (`.navbar-toggler:checked ~ .nav-scrollable`, `NavMenu.razor.css:164`, confirmed the only such mechanism, no hidden JS). For the second constat (`.nav-scrollable`'s native, non-Blazor `onclick="document.querySelector('.navbar-toggler').click()"`, meant to close the menu when an internal link is activated): confirmed bUnit/AngleSharp has no JS engine, so this attribute is never actually executed in a bUnit test — a live-browser check was attempted (mobile viewport, real dev server, `/Account/Login`, no auth needed) but the Browser pane's own click/event-delivery was unreliable this session (a JS `.click()` fired a real native `change` event that never round-tripped to the Blazor circuit; a `computer`-tool click didn't register at all) — consistent with this project's already-documented Browser-pane flakiness, not a sign of an actual defect in this markup. **Concluded Branch A** (already works) on the strength of standard, spec'd WHATWG platform behavior: activating a focused `<a>` via Enter dispatches a synthetic "click" `MouseEvent` that bubbles exactly like a real mouse click, reaching the same ancestor `onclick` handler either way — documented in `NavMenuTests.cs` and in the ticket doc itself (39.0's checklist), not silently assumed. No code change for this second point, per the ticket's own explicit branch structure.
  - **39.1**: the checkbox gained `id="nav-menu-toggler"`, `aria-label`/`title` both reusing the pre-existing `NavMenu_ToggleTitle` resx key (its value, "Navigation menu"/"Menu de navigation", was already exactly the right `aria-label` text — no duplicate key added), `aria-controls="nav-scrollable"` (new `id` added to the `.nav-scrollable` div, none existed before, confirmed unique in the project), and `aria-expanded="@(_isNavExpanded ? "true" : "false")"` driven by a new private `_isNavExpanded` field updated via `@onchange` (`OnTogglerChanged`, reading `ChangeEventArgs.Value is true`) — purely additive to the pre-existing CSS-only mechanism, the checkbox itself and its `:checked` behavior are untouched. No JS interop introduced.
  - **39.2**: pure non-regression per 39.0's Branch A conclusion — no markup change on `.nav-scrollable` itself (still the native `onclick`, no `role`/`tabindex` added, since it's a delegated event container, not an interactive control of its own).
  - Tests (`NavMenuTests.cs`, +6): `aria-label` non-empty, `aria-controls` matches the real rendered `.nav-scrollable` id, `aria-expanded="false"` by default, `aria-expanded="true"` after `cut.Find("#nav-menu-toggler").Change(true)`, non-regression on the `navbar-toggler` class/`type="checkbox"`, and a `.nav-scrollable` structural non-regression test (`onclick` attribute unchanged, still no `role`/`tabindex`). All pre-existing `NavMenuTests` (20) pass unmodified. Full `ExcelETL.BlazorAdmin.Tests` suite: 472/472 green (was 466 before this lot's 6 additions). Full solution build green.
  - **Hors périmètre, per the ticket**: `aria-live` announcements for error/batch-summary messages and icon-convention consistency (separate audit items, other lots); color-contrast/M3 theme (needs real browser verification, not this static lot); any new `tabindex` beyond what's strictly necessary (the audit confirms none exist today and DOM focus order is left untouched).

### Test conventions in practice
- bUnit (v2.7.2): `class FooTests : BunitContext`, `Render<Foo>()`. Examples across `Components/Pages/Admin/*`, `Account/*`, `Layout/*`.
- HTTP-calling code is tested with a hand-rolled `FakeHttpMessageHandler`, not RichardSzalay.MockHttp — avoids a new dependency for equivalent capability. Colocated per test project (e.g. `legacy/ExcelProcessingClientService.Tests/FakeHttpMessageHandler.cs`); BlazorAdmin no longer has any HTTP-calling code of its own since Lot K4 removed `ExcelProcessingClient`.
- WebAPI endpoints are tested via `WebApplicationFactory<Program>` integration tests (e.g. `OxoProcessEndpointTests.cs`), not mocked HTTP.
- bUnit + `InputFile`: `cut.FindComponent<InputFile>()` + `InputFileContent.CreateFromText(...)` + `.UploadFiles(...)`. Note: triggering `InputFile.OnChange` in bUnit blocks synchronously until the whole async handler chain completes — a `TaskCompletionSource`-gated fake HTTP response that you only complete *after* calling `UploadFiles` will deadlock the test. Use immediately-resolved fake responses instead; the transient "Uploading" state isn't observable this way.
- bUnit auth with a specific user id (not just username/roles): `this.AddAuthorization().SetAuthorized("user@example.com")` then `.SetClaims(new Claim(ClaimTypes.NameIdentifier, "user-1"))` on the same `BunitAuthorizationContext` — `AddAuthorization()` already wires `CascadingAuthenticationState` into the render tree, so components consuming `[CascadingParameter] Task<AuthenticationState>` work without extra setup. See `ProfileTests.cs`.
- `UserRepository` is now a hybrid repository (plain EF reads via `IDbContextFactory` + `UserManager`-backed writes): its test class mixes both conventions from the top-level rule above — `GetByIdAsync`/`GetAllAsync` tests seed the real EF InMemory provider directly, `UpdateProfileAsync`/`ChangePasswordAsync` tests mock `UserManager<ApplicationUser>` via `IdentityManagerMocks.CreateUserManagerMock()`. Both fixtures live side by side in the same `UserRepositoryTests` class.

### Maintenance rule
Whenever a milestone adds new DI registrations, HTTP endpoints, config keys, JS modules, or resource files, update the relevant bullet above in the same commit. Treat this section as a cache of the exploration an agent would otherwise have to redo — keep it accurate rather than exhaustive.
